# Build artifacts
build/
build_debug/

*.gba
*.elf
*.map
linker_debug.ld

tools/agbcc/
# 生成的报表 (gen_reports.py 可随时重建, 不入库)
docs/reports/

*.dump
*.dump.dmp
*.dmp
gccdump*
# docs/
# docs/*.md
*.json
Screenshots
# scripts
scripts/ida/
scripts/data/
data/raw_data
# scripts/

asm/nonmatchings/
asm/matchings/
# permuter 套件与搜索产物 (output-<score>-<n>, 实测 ~280MB) 一律不入库;
# 挂起函数的最优候选必须内联进 docs/progress.md 或写进 TSV note, 不能只留在 permuter/ 里
permuter

# Generated context (make ctx)
ctx.c
ctx.c.m2c

# Python
.venv/
__pycache__/

# OS
.DS_Store

# clangd
.cache
compile_commands.json

# ida
*.i64
*.id0
*.id1
*.id2
*.til
*.nam


code.s
baserom.o

# 各智能体临时目录 (禁止 rm -rf 整个目录, 只清自己的子目录)
.scratch/

# 认领锁 (claim.sh 的运行时状态, 机器/会话相关, 不入库)
.claims/

.vscode/

.zcode
.workbuddy
.Trash-0
# agbcc -dl RTL 寄存器分配转储 (诊断产物, 不入库)
gccdump.lreg