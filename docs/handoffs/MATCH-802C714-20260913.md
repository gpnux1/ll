# MATCH-802C714-20260913

## 目标
- address: 0x0802C714
- current_name: sub_802C714
- physical_tu: src/event_actor.c
- match_status: **1 (已合入, bytecmp/fncheck 双绿)**
- entry_kind: task-table (handler 指针表 0x0839CD5C, 同 sub_802BD94 族)
- args: r0 = BattleObj *obj
- return: r0 = u8 result (0 继续 / 1 动作完成)

## 机器契约
- callees: sub_80187E8, sub_80444A4(BattleObj*), sub_801CE80(BattleObj*,u8,u16,u8,u8),
  sub_803F5B4(BattleObj*), Sfx_Play(u16,u8,u8), Sfx_StopTrack(u8),
  sub_8020CC4(void*,u8,u8,u16,u8,u16,u16), sub_804BF14(...,s8,...), sub_8044514(s16),
  sub_804C728(u8,u8,u8), sub_803F658(BattleObj*)
- memory: gUnk_03000820 (state u8) / gUnk_03000822 (u16) / gUnk_03000824 (u8) /
  gUnk_03000825 (u8 帧/子计数) / gUnk_03000828/29 (u8 posX/posY 暂存) /
  gUnk_03000844/45/56 (u8 占用标志)
- obj 偏移: +0x24=headA.kindFlags, +0x28=headA.frameIdx, +0x2A=headA.f_1E,
  +0x35=headA.palSlot, +0x54=headB.kindFlags, +0x58=headB.frameIdx, +0x66=headB.f_2A,
  +0xB0=state, +0xB4=f_B4, +0xB6=f_B6, +0xBF=posX, +0xC0=posY

## 语义 (状态机)
- 0: 存 posX/posY/headA.f_1E/palSlot; sub_80444A4; sub_801CE80(obj,5,0x1B4,0xD,0);
     sub_803F5B4; 计数=0; f_B6=2; f_B4=0 → 0x12。
- 0x12: headA.kindFlags&0x800 落 → Sfx(0x5A) → 0x13。
- 0x13: headA.frameIdx==0x6E 时 Sfx(0x68); 到 0x1000 → Sfx_StopTrack(1) +
       sub_801CE80(obj,0,0822,0824,0) → 0x14。
- 0x14: 等 0x800 → 0x15。
- 0x15: sub_8020CC4(obj,0x82,0x7D,0x1B4,0xA,0x313,5); headB.f_2A=0 → 0x16。
- 0x16: 等 headB&0x800 → Sfx(0x4E,1,1) → 0x17。
- 0x17: headB.frameIdx>0x18 → sub_804BF14(0,3,0x10,0x1C,0x1F,4,4,-1,2); 计数=0;
       sub_8044514(0x32) → 0x18。
- 0x18: 计数>3 → state=0x19 + sub_804C728(0,3,0x10); 否则计数++。
- 0x19: headB.frameIdx==0x5B 时 Sfx_StopTrack(1); headB&0x1000 → obj->state&=0xDFFF + state=9; 计数++。
- 9: gUnk_03000844/45/56 全 0 → result=1。
- 尾: sub_803F658(obj) (无帧计数自增)。

## 关键形状
1. **case24 必须写成 `if (计数 > 3) { state=0x19; sub_804C728; break; } 计数++;`**
   这样 GCC 才把 case24 的 `计数++` 与 case25 尾部的 `计数++` **跨跳合并**到共享尾块
   (目标 `cmp #3; bls _0802C99E`)，否则 case24/case25 各留一份 3 指令 `+=1` 块，多 3 条指令。
2. **case24 else 分支无 `计数=0`** (与同族 sub_802BD94 case24 不同！) —— 我最初照抄 sibling 加了
   `计数=0` 导致 case24 体多 1 条 `movs r0,#0; strb`。原始顺序是 `state=0x19` 先于 `sub_804C728`。
3. case22 是 `Sfx_Play(0x4E,1,1)` (不是 0x34，也不是 arg2=2 —— sibling sub_802BD94 的 0x4E 在其 case25，
   本函数搬到了 case22)。
4. case23 阈值 0x18 + `sub_8044514(0x32)`。
5. 本函数只有 0x12..0x19 (无 0x1A/0x1B)，switch 上限 0x19，故无 case26/27。

## 验证
- `scripts/bytecmp.sh` (经 `.scratch/bcfn.sh` 对称链接, 使用 `permuter/sub_802C714/sym.ld`): **OK 904 bytes**
- `python3 scripts/fncheck.py sub_802C714`: **OK (724 bytes @0x0802c714, 15 bl 槽忽略)**
- 合入: `src/event_actor.c` INCLUDE_ASM → C; `include/code_0.h:718` `void sub_802C714();` →
  `u8 sub_802C714(BattleObj *);` (无 C/非C 调用点, 仅任务表分发, 原型改动零影响)。
- `functions.tsv:598` status 0→1。

## 全量验证
- `make` + `sha1sum -c ll.sha1`: **通过 (匹配进度 833/1059, 78.7%)**。
- 说明: 排障过程中发现一次 `linker.ld:804 cannot move location counter backwards` 红。
  根因是我在隔离本函数时反复在 INCLUDE_ASM↔C 之间切换 + 残留的 `asm/nonmatchings/sub_802C714.s`，
  产生了一个 `.text=45252` 的陈旧 event_actor.o。源文件稳定后重编即回 45248，全量绿。
  **与本函数源码无关**，正常重建即恢复。

## 改动文件
- `src/event_actor.c`: sub_802C714 C 体 + 家族注释。
- `include/code_0.h`: sub_802C714 原型。
- `functions.tsv`: 0x0802c714 status 1 + note。
- `permuter/sub_802C714/{base.c,compile.sh,settings.toml,target.s,target.o,sym.ld}`: 候选与工具。
