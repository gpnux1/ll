# MATCH-sub_804EC04-20260916.md

## 1. 基础信息
- **函数名**: `sub_804EC04` (语义名: `BattleCards_Roll`)
- **ROM 地址**: `0x0804EC04`
- **物理 TU**: `src/battle_itemuse_rewards.c`
- **函数大小**: 704 字节 (351 行 ASM)
- **匹配状态**: 100% Match (fncheck OK 704B, SHA1 全绿, 899/1059)
- **日期**: 2026-09-16
- **认领 Agent**: `antigravity`

---

## 2. 机器契约 (Machine Contract)
- **输入**:
  - `u32 *arg0`: 战后卡片掉落表指针输出槽 (`*arg0 = (u32)gBattleCardDrops`)
- **输出**:
  - `u8`: 产生的有效掉落卡片条目数 (`gBattleCardDropCount`)
- **寄存器分配**:
  - `r4`: 循环索引 / 卡片 ID `k` / 临时指针
  - `r5`: 对象池指针 (`objs`) / 怪物列表循环索引 `i` / 外循环计数
  - `r6`: 收集槽位数量 / 命中标志 `ok` / 卡片表基址指针 `&gBattleCardDrops`
  - `r7`: 幸运判定命中值 `found` / 临时指针 `&gBattleCardDropCount`
  - `r8`: 装备加成掉率模式 (`rateMode`: 0/1/2)
  - `sb` (`r9`): 怪物列表指针 (`list`, 来自 `sub_8020E68()`)
  - `sl` (`r10`): 卡片表基址指针 (`&gBattleCardDrops`, `0x03000E38`)
- **外部依赖与调用 (Callees)**:
  - `GetObjPool()` (`battle_task_services.h`): 获取全局战斗对象池基址 (调用 3 次)
  - `sub_8020E68()` (`battle_object_engine.h`): 获取当前敌方怪物槽位配置列表 (`list[0]` 为数量，条目位于 `list + 1 + i * 4`)
  - `sub_8020B48()` (`battle_object_engine.h`): 获取特定行动对象指针 (`gUnk_03000718`)
  - `sub_80489E8(...)` (`battle_flow_rules.h`): 筛选参战槽位。通过模块内 `static inline Battle_CollectSlots` 封装调用 2 次
  - `sub_804E76C(...)` (`battle_itemuse_rewards.h`): 检查对象装备属性 (分别以参数 `5, 2` 和 `5, 3` 调用两次，检测不同档位的掉率提升装备)
  - `Rng_LcgNext()`: 随机数发生器，`((int (*)(void))Rng_LcgNext)() % 100` 产生有符号模运算 `__modsi3`
  - `sub_80187B4()`: 读取战斗全局状态标志 `gGstate324`，用于分支判定 (`& 0x20`)
  - `SaveTimer_Inc(u8)` (`menu.h`): 半字节递增存档中卡片图鉴收集数量 (上限 5)
- **全局状态与数据表**:
  - `gBattleCardDrops` (`0x03000E38`, `BattleCardDropEntry[10]`): 战后卡片掉落记录表
  - `gBattleCardDropCount` (`0x03000E60`, `u8`): 战后卡片掉落有效条目数
  - `gUnk_0839D9B8` (`0x0839D9B8`, `[][5]`): 掉落概率表 (此处索引 `[k][4]` 获取卡片基础掉落阈值，常量基址为 `0x0839D9BC`)

---

## 3. 业务逻辑与语义建模 (Semantic Modeling)
本函数与 `BattleDrops_Roll` 并列，是战斗胜利后的**怪物卡片（Monster Card）/图鉴掉落计算与存档结算核心驱动**:
1. **重置表与计数器**:
   - `rateMode = 0`。
   - 清空 `gBattleCardDrops[0..9]` 的 `cardId` 和 `count`。
   - `gBattleCardDropCount = 0`。
2. **多档位卡片装备检测 (Rate Mode Detection)**:
   - 第一次扫描友方参战角色: `sub_804E76C(obj, 5, 2)`，若命中则 `rateMode = 1`。
   - 第二次扫描友方参战角色: `sub_804E76C(obj, 5, 3)`，若命中则 `rateMode = 2`。
3. **逐怪物卡片掉落掷骰**:
   - 遍历各敌方怪物 `i = 0 .. list[0] - 1`，跳过 `pool[i * 0xC8 + 0x493] == 7` 的怪物。
   - 若为普通怪物 (`v <= 0x70`):
     - `k = (u8)(v - 0xC)`。
     - 若该怪物属性 `pool[i * 0xC8 + 0x494]` 与行动对象属性 `obj20B48[0xAC]` 匹配:
       - `rateMode == 0`: 查表掉率判定 `Rng % 100 < gUnk_0839D9B8[k][4]`。
       - `rateMode == 1`: 50% 命中门槛 `Rng % 100 <= 49`。
       - `rateMode == 2`: 100% 必然命中 (`ok = 1`)。
     - 若属性不匹配: 统一走查表掉率判定 `Rng % 100 < gUnk_0839D9B8[k][4]`。
   - 若为特殊怪物 (`v > 0x70`):
     - `k = (u8)(v - 0x71)`; 若 `k > 0xB` 则 `k--`。
     - 若 `pool[i * 0xC8 + 0x4A6] != 0x79` 则判定命中 (`ok = 1`)。
4. **查重合并写入卡片表**:
   - 若判定命中 (`ok == 1`)，在 `gBattleCardDrops` 中查找是否已存在卡片 `k`。
   - 若存在则其 `count++`；否则新建条目并递增 `gBattleCardDropCount`。
5. **存档卡片图鉴计数递增 (SaveTimer_Inc)**:
   - 遍历 `gBattleCardDrops`:
     - 若 `sub_80187B4() & 0x20` 置位:
       - 调用 `SaveTimer_Inc(cardId + 0xA0)`。
       - 若 `cardId > 0xA`，则将表中的 `cardId` 递增 1 (`cardId++`)。
     - 若未置位:
       - 调用 `SaveTimer_Inc(cardId + 0x3B)`。
6. **输出与返回**:
   - `*arg0 = (u32)gBattleCardDrops`，返回 `gBattleCardDropCount`。

---

## 4. 关键实现技术与编译器细节 (GCC 2.91 CodeGen)
1. **`Battle_CollectSlots` 内联工具复用**:
   - 再次证实并复用了我们在 `BattleDrops_Roll` 中确立的 `static inline Battle_CollectSlots`。本函数内两次连续调用均严格生成 `mov r1, sp; movs r2, #0; ldr r3, =0x1FF; bl sub_80489E8`。
2. **列表索引变量调度**:
   - 沿用 `BattleDrops_Roll` 的列表读取结构：
     ```c
     i4 = i * 4;
     items = list + 1;
     vtmp = items[i4];
     v = vtmp;
     ```
     彻底压制了 GCC 2.91 在进入 else 分支时多余的寄存器拷贝指令 `adds r0, r1, #0`。
3. **查表常量基址折叠**:
   - C 代码自然书写 `gUnk_0839D9B8[k][4]`，GCC 2.91 将 `+4` 自动折叠为字面池常量 `0x0839D9B8 + 4 = 0x0839D9BC`，与反汇编逐字节严密吻合。
4. **IWRAM 内存布局完整解密**:
   - 完美填补了 `0x03000E08` (`gBattleDrops` 10 项) 到 `0x03000E68` (`gScriptReturnSetId`) 之间的空白：
     - `0x03000E38`: `gBattleCardDrops[10]` (40 字节)
     - `0x03000E60`: `gBattleCardDropCount` (1 字节)

---

## 5. 验证结果
- `python3 scripts/fncheck.py sub_804EC04`: **OK (704 bytes @0x0804ec04, 0 池重定位, 18 bl 槽忽略)**
- `src/battle_itemuse_rewards.c` 全部 25 个函数验证: **25/25 OK**
- `make` + `sha1sum -c ll.sha1`: 全工程编译绿，SHA1 验证通过 (匹配进度: 899/1059, 84.9%)
