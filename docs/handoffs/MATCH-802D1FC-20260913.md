# MATCH sub_802D1FC — 2026-09-13 (接管自 gpnux / analyze-agent)

## 结果
- 地址: 0x0802D1FC (thumb, module event_actor)
- 状态: **已匹配** (functions.tsv status=1)
- 字节: 600 B, bytecmp 0 diff (施加 18 个绝对符号后链接 744B 全等), fncheck OK
- 全量: `make` 通过, `sha1sum -c ll.sha1` 成功, 进度 836/1059

## 接管说明
- 用户指示 "接管匹配 sub_802D1FC"; 原锁持有者 gpnux (since 2026-09-13T15:16:54)。
- 完成后 `scripts/claim.sh --release sub_802D1FC` 已释放该锁 (claim.sh 无 agent 作用域,
  会删除锁文件)。gpnux 若仍在本地工作请知悉锁已释放。

## 机器契约
```
address: 0x0802D1FC
entry_kind: indirect (事件演出函数指针表, 同 sub_802CE90/sub_802D454 族)
callers: 间接 (event_actor 演出派发表); 直接源未在 src 出现
callees: sub_80187E8, sub_80444A4, sub_801CE80, sub_803F5B4, Sfx_Play,
         Sfx_StopTrack, sub_8020CC4, sub_8044514, sub_803F658
args: r0 = BattleObj *obj
return: r0 (u8): 0 继续 / 1 结束 (仅 case9 返回 1)
```

## 语义 (gUnk_03000820 状态机)
0 → 0x12 → 0x13 → 0x14 → 0x15 → 0x16 → 0x17 → 9。
- case0: 暂存 posX/posY/headA.f_1E/palSlot; sub_80444A4; sub_801CE80(obj,5,0x1B4,0xA,0);
  sub_803F5B4; 计数=0; f_B6=2; f_B4=0; → 0x12。
- 0x12: 等 headA.kindFlags 0x800 落 → 0x13。
- 0x13: headA.frameIdx>0x37 → kindFlags|=0x100 +
  sub_8020CC4(obj,(u8)(posX+0x3C),(u8)(posY-0x18),0x27C,0xC,0x320,5) → 0x14。
- 0x14: 等 headB.kindFlags 0x800 落 → Sfx(0x64) → 0x15。
- 0x15: headB.kindFlags 0x1000 → Sfx_StopTrack(1); obj->state&=0xDFFF;
  headA.kindFlags&=0xFEFF; sub_8044514(0x28) → 0x16。
- 0x16: headA.kindFlags 0x1000 → sub_801CE80 复位 → 0x17。
- 0x17: 等 headA.kindFlags 0x800 落 → 9。
- 9: gUnk_03000844/45/56 全 0 → result=1。
- 尾: sub_803F658(obj)。

## 与姊妹函数差异 (sub_802CE90 对照)
- 本函数 `sub_801CE80` 第5参 = 0 (sub_802CE90 为不同调用形态)。
- case19 锚点用偏移 (posX+0x3C, posY-0x18) + 表项 0x320 (sub_802CE90 为 +0x46/-0x20/0x31C)。
- case21 多一次 sub_8044514(0x28) (sub_802CE90 在更后的状态)。
- 状态集合到 0x17 结束 (无 sub_802CE90 的 0x18/0x19/0x1A 段)。

## 影响面
- include/code_0.h:721: `void sub_802D1FC();` → `u8 sub_802D1FC(BattleObj *);`
  (原 K&R 声明与真调用约定冲突)
- 未改动共享 iwram.h / linker.ld / 其它函数区段。

## 锁定状态
- 已释放 (见上)。
