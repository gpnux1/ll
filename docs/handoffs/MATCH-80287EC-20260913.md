# MATCH-80287EC-20260913

## 目标
- address: 0x080287EC
- current_name: sub_80287EC
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 748B)
- entry_kind: task-table (handler 指针表 0x0839CD5C, sub_803F444 间接调用)
- args: r0 = BattleObj *obj
- return: r0 = u8 result (0 继续 / 1 完成)

## 机器契约
- callees: sub_80187E8(), sub_80444A4(BattleObj*), sub_801CE80(BattleObj*,u8,u16,u8,u8),
  sub_803F5B4(BattleObj*), sub_804B834(u8,u8,u8,s8,u8), sub_804B8E8(u8,u8),
  sub_8020CC4(void*,u8,u8,u16,u8,u16,u16), Sfx_Play(u16,u8,u8),
  sub_8044514(s16), sub_803F658(BattleObj*)
- memory_reads: obj +0xBF posX, +0xC0 posY, +0x2A headA.f_1E, +0x35 headA.palSlot,
  +0x24 headA.kindFlags, +0x28 headA.frameIdx, +0x54 headB.kindFlags;
  gUnk_03000820/22/24/25/28/29/44/45/56
- memory_writes: obj headA.kindFlags, headB.kindFlags, +0xB0 state;
  gUnk_03000820 状态, gUnk_03000825 计数, gUnk_03000828/29/22/24 暂存

## 语义 (状态机, 0/9/0x12..0x19)
- 0: 暂存位置/头字段; sub_80444A4; sub_801CE80(obj,5,0x1B4,0xD,0); sub_803F5B4;
     计数=0 → 0x12。
- 0x12: headA.kindFlags & 0x800 落 → sub_804B834(palSlot,1,3,-0xB,5) → 0x13。
- 0x13: frameIdx==0x32 → Sfx(0x64); frameIdx>0x4F → sub_804B8E8(palSlot,1);
       sub_801CE80(obj,0,0822,0824,0) → 0x14。
- 0x14: headA.kindFlags & 0x800 落 → sub_804B834(palSlot,1,3,-0xB,5) → 0x15。
- 0x15: sub_8020CC4(obj, 0xB4, 0x78, 0x1E6, 0xD, 0x34A, 2); 计数=0 → 0x16。
- 0x16: headB.kindFlags & 0x800 落 → Sfx(0x64) → 0x17。
- 0x17: headB.kindFlags & 0x1000 → &= 0xEFFF; obj->state &= 0xDFFF; 计数=0 → 0x18。
- 0x18: 计数 != 0 → sub_8044514(0x14) → 0x19。
- 0x19: 无其他演出占用 → sub_801CE80 复位 → 9。
- 9: headA.kindFlags & 0x800 落 → sub_804B834(palSlot,1,3,-0xB,5); result=1。
- 尾: gUnk_03000825 += 1; sub_803F658(obj)。

## 与 sub_80285A0 的关系
同一"槽位恢复"子族 (都用 sub_804B834/sub_804B8E8 + sub_8020CC4)。sub_80285A0 双参、
sub_804B834 首参为字面 0xD; 本函数单参、首参为 obj->headA.palSlot (可见 ROM 运行时实际
从对象读调色板槽), 且状态推进顺序不同 (无插值归位段)。

## 字节匹配关键点
- sub_804B834 首参必须是 `obj->headA.palSlot` (ldrb [r5,#0x35]), 不是字面 0xD。
- 参数构造顺序: `sub_804B834(palSlot,1,3,-0xB,5)` → r0=palSlot, r3=-0xB, str [sp]=5, r1=1, r2=3。
- 尾部 `gUnk_03000825 += 1` 无条件, 与 case18/20/9 的清槽共用尾块。
- 无寄存器分配卡点 (单参, 自然通过)。

## 验证
- scripts/bytecmp.sh: 仅 15 个 bl 占位字节不同 (relocation), 归一化后 0 diff (748B)。
- python3 scripts/fncheck.py sub_80287EC: OK (748 bytes @0x080287ec, 27 池重定位已施加, 15 bl 槽忽略)。
- make + sha1sum -c ll.sha1: 通过 (进度 817→819)。

## 改动文件
- src/event_actor.c: 仅 0x080287EC 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 原型 void sub_80287EC() → u8 sub_80287EC(BattleObj *)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_80287EC/base.c: 候选。
