# Handoff: sub_80104F8 (0x080104F8, 300 字节) — 传送/出生参数装载 (⏸ 阻塞)

日期: 2026-09-16 | agent: match_80104F8 | 状态: ⏸ 阻塞 (status 保持 0, 确认为 global-alloc 优先级墙)

## 1. 机器契约 (已验证事实)

```
address:         0x080104F8
current_name:    sub_80104F8 (仅检索标签)
physical_tu:     src/menu_ui.c (仍为 INCLUDE_ASM)
match_status:    0
size:            300 字节 (0x12c)
entry_kind:      direct — sub_800C2F8 (0x0800CC84) 处 bl 调用
callees:         EventFlags_Test (0x08027738), Sfx_Play (0x0804D798), SwitchFlags_ClearRange (0x080277C0)
args:            void
return:          r0: 0 (错误/取消) 或 1 (成功装载参数并触发传送)
saved_regs:      push {r4, r5, r6, r7, lr}; push {r8, sb, sl}
memory_reads:    gScreenIdleIconIds[gScreenIdleIconCursor - 0xb + gMenuCursorSel]
                 gUnk_080981EE (6 字节/项表)
memory_writes:   gUnk_030001C8 (u16 错误码 0x27 或 0x1A)
                 gMapNpcSetId (u8)
                 gSpawnTileX (u8)
                 gSpawnTileY (u8)
                 gSpawnFacingDir (u8)
                 gMoveCmdSetId (u16)
                 gWarpAnimState = 1 (u8)
                 gUnk_03004D4C = 0x34 (u8)
```

## 2. 功能语义 (E2)

本函数为地图传送/出生参数装载逻辑（与 `src/menu_ui.c:1290` 的已匹配函数 `WarpTable_Check` 构成姊妹同构实现，字段装载序列完全一致）：
1. 计算当前图标类型：
   `kind = gScreenIdleIconIds[gScreenIdleIconCursor - 0xb + gMenuCursorSel];`
2. 条件过滤与错误提示（均写入错误码、播放音效 `Sfx_Play(3, 0, 0)` 并返回 0）：
   - 若 `kind == 0`，错误码 `0x27`。
   - 若 `kind == 8` 且 `EventFlags_Test(0x10D) == 0`，错误码 `0x1A`。
   - 若 `kind == 0x18` 且 `EventFlags_Test(0xFF) != 0`，错误码 `0x1A`。
3. 成功路径：在 ROM 表 `gUnk_080981EE`（每项 6 字节）中线性查找首字节匹配 `kind` 的条目：
   - 依次拆解装入全局引擎变量：
     `gMapNpcSetId = tbl[i++];`
     `gSpawnTileX = tbl[i++];`
     `gSpawnTileY = tbl[i++];`
     `gSpawnFacingDir = tbl[i++];`
     `gMoveCmdSetId = tbl[i] + (tbl[i + 1] << 8);`
     `gWarpAnimState = 1;`
     `gUnk_03004D4C = 0x34;`
   - 调用 `SwitchFlags_ClearRange()` 并返回 1。

## 3. 还原度与机器码对比

当前最佳候选为 `.scratch/gpnux-104f8/V_inline.c`：
- **代码结构还原度**: 100% 完整。所有 6 个全局变量指针均准确分配到 `r3, r7, ip(r12), r8, sb(r9), sl(r10)`。
- **指令序列还原度**: 全函数 300 字节汇编中，所有助记符、操作数、分支偏移、常数池布局（池倾倒点）与目标 100% 逐条吻合。
- **唯一剩余差异**: 一对寄存器 home 互换（目标 `kind` 在 `r6`、`tbl` 在 `r5`；当前候选 `kind` 在 `r5`、`tbl` 在 `r6`）。

```
指令流逐条对比节选:
目标 ROM:
  ldrb r6, [r0]       ; kind -> r6
  cmp  r6, #0
  ...
  ldr  r5, =gUnk_080981EE ; tbl -> r5
  ...
  adds r0, r4, r5     ; index (r4) + tbl (r5)
  cmp  r0, r6         ; cmp *(i+tbl), kind

当前候选:
  ldrb r5, [r0]       ; kind -> r5
  cmp  r5, #0
  ...
  ldr  r6, =gUnk_080981EE ; tbl -> r6
  ...
  adds r0, r4, r6     ; index (r4) + tbl (r6)
  cmp  r0, r5         ; cmp *(i+tbl), kind
```

## 4. 编译器机制深挖 (GCC 2.9 global-alloc 优先级墙定量分析)

通过 `-dl`、`-dg`、`-df` 与 `-da` 转储以及对 GCC 2.9（`agbcc`）源码（`flow.c`、`local-alloc.c`、`global.c`）的全面分析，确认该卡点的具体机制：

1. **优先级计算公式 (`global.c:allocno_compare`)**:
   ```c
   pri = (floor_log2(refs) * refs) / live_length * 10000;
   ```
2. **当前候选的寄存器统计数据 (`.lreg` / `.greg`)**:
   - `Register 23 (i)`: `refs = 17, live_length = 64` -> `pri = 10625` -> 分配到 `r4`。
   - `Register 22 (kind)`: `refs = 6, live_length = 43` -> `pri = 2790` -> 分配到 `r5`。
   - `Register 24 (tbl)`: `refs = 9, live_length = 104` -> `pri = 2596` -> 分配到 `r6`。
   - 因为 `2790 > 2596`，GCC 2.9 优先处理 `kind` 并分配给它首个可用的 callee-saved 寄存器 `r5`，而 `tbl` 只能获得 `r6`。
3. **`REG_EQUIV` 导致的 live_length 翻倍惩罚 (`local-alloc.c:update_equiv_regs`)**:
   - `tbl` 来自常数池符号加载（`set (reg 24) (mem (symbol_ref "*.LC4")))`），在 `cse` 阶段被打上 `REG_EQUAL`，在 `local-alloc` 中被判定为函数不变量并转为 `REG_EQUIV`。
   - `local-alloc.c:852` 对持有 `REG_EQUIV` 的伪寄存器强制执行 `REG_LIVE_LENGTH (regno) *= 2;`。
   - `tbl` 的实际指令区间仅 52 条，但因翻倍变成 104，使得优先级被腰斩（27 / 104 = 2596）。
4. **翻转条件**:
   - 若要使 `pri(tbl) >= pri(kind)`，要求：
     `27 / LL_t >= 12 / LL_k` <=> `9 * LL_k >= 4 * LL_t`
   - 在 `LL_t = 104` 时，需 `LL_k >= 47`（当前仅 43，还差 4 条指令区间）。
   - 或者 `LL_t <= 96`（需要原始活跃区间减少 4 条指令）。
   - 由于目标汇编指令序列已经完全钉死，任何在 C 源码层面添加/减少有效操作都会破坏目标 300 字节指令流。此前穷举的 60 余种 C 结构变体与 6 万次 permuter 均无法在保持完全相同 asm 指令的前提下跨越这 8 分的优先级鸿沟。

## 5. 结论与归档

- 语义 100% 全解，无任何未知逻辑。
- 字节差仅限于 `r5 <-> r6` 寄存器分配顺序（52 处寄存器编码差异，总长与指令流 100% 相同）。
- 最佳代码保存在 `.scratch/gpnux-104f8/V_inline.c` 与 `permuter/sub_80104F8/base.c`。
- 依据 `AGENTS.md` 铁律，不使用内联汇编或作弊手段强行合入，保持 `status = 0`，保留占位并释放锁。
