# MATCH sub_8032948 — 2026-09-13 (analyze-agent)

## 结果
- 地址: 0x08032948 (thumb, module event_hub)
- 状态: **已匹配** (functions.tsv status=1)
- 字节: 1068 B, bytecmp 0 diff (施加 25 个绝对符号后链接 1296B 全等), fncheck OK
- 全量: `make` 通过, `sha1sum -c ll.sha1` 成功, 进度 857/1059 (80.9%)

## 机器契约
```
address: 0x08032948
entry_kind: indirect (对话/演出函数指针表, 同 sub_8032548/sub_803272C 族)
callees: sub_80444A4, sub_803F5B4, sub_803E58C, sub_8020974, sub_801B954,
         sub_804C3A4, Sfx_Play, sub_804BF14, sub_804C728, sub_8044514,
         sub_801768C, Sfx_StopTrack, sub_8045B90, sub_803F658
args: r0 = BattleObj *arg0 (演出/对话对象); r1 = u8 *arg1 (关联对话对象)
return: r0 (u32): 0 继续 / 1 结束 (case9 返回 1)
```

## 语义 (gUnk_03000820 状态机)
0 → 1 → 2 → 0x12 → 0x13 → 0x14 → 0x15 → 0x16 → 0x17 → 0x18 → 0x19 → 0x1A → 0x1B → 9。
- case0: 存 posX/posY; 计数=0; sub_80444A4; sub_803F5B4; f_B6=f_B4=0; → 1;
  gUnk_0300086B=0。
- case1: sub_803E58C(arg0,arg1,3)==1 → b4 = slot==0 ? 0x35F : 0x36E;
  sub_8020974(&headA, b4, 0x1B4, 0xD, 2); arg1[0xBE]<=0xA → headA.kindFlags|=0x20 → 2。
- case2: headA 0x800 落 → 0x12。
- 0x12: headA 0x1000 → sub_804C3A4(palSlot,(u8)sub_801B954(&headA)); headA.kindFlags|=0x100;
  计数=0; Sfx(0x31,1,0); sub_804BF14(0,3,7,0xE,0x1C,4,4,-1,2) → 0x13。
- 0x13: 计数>3 → 停; 否则 sub_804C728(0,3,0x10) → 0x14。
- 0x14: b4 = slot==0 ? 0x360 : 0x36F; sub_8020974(&headA, b4, 0x1B4, 0xD, 0x402) → 0x15。
- 0x15: headA 0x800 落 → 计数=0; sub_804BF14(同 0x12) → 0x16; Sfx(0x31,1,1)。
- 0x16: 计数<=0x13: 计数==4 时 sub_804C728(0,3,0x10);
  posX = sub_801768C(gUnk_0300086E, -0x70, 0x14, 计数, 2); 计数++;
  否则 Sfx_StopTrack(1) → 0x17。
- 0x17: b4 = slot==0 ? 0x361 : 0x370; sub_8020974(&headA, b4, 0x1B4, 0xD, 2) → 0x18。
- 0x18: headA 0x800 落 → sub_8044514(0x14); 计数=0 → 0x19。
- 0x19: headA 0x1000 → sub_804C3A4(palSlot,(u8)sub_801B954(&headA)); headA.kindFlags|=0x100;
  计数=0; gUnk_0300086E=posX; gUnk_03000828=gUnk_08393A48[memberIdx];
  gUnk_03000829=gUnk_08393A4D[memberIdx] → 0x1A。
- 0x1A: gUnk_03000844/45/56 全 0 → gUnk_0300086B=0xC → 0x1B; 否则 计数++。
- 0x1B: sub_803E58C(arg0,arg1,0)==1 → 9。
- 9: sub_8045B90(arg0, arg0->pad_A1); result=1。
- 尾: sub_803F658(arg0)。

## 关键 GCC2.9 形状点
1. case1/20/23 的 `b4` 必须写成三目 `slot == 0 ? X : Y` (X=基址-0xF)。写成
   `b4 = BASE; if (kind == 0) b4 -= 0xF;` 会少 2 条 staging 拷贝
   (`adds r3,r1,#0`), 整体短 4B, 分支偏移全错。
2. `keys` 必须是独立 `int` 载体 (headA.kindFlags|0x20 → 写回), 否则体量骤变。
3. `sub_803E58C` 返回值需 `(u8)` 截断比较 (调用点隐含 u8 返回)。

## 结构体/全局
- obj+0xBE = slot; obj+0x35 = headA.palSlot; obj+0xBB = memberIdx;
  obj+0xA1 = pad_A1; obj+0x24 = headA.kindFlags。
- gUnk_08393A48/gUnk_08393A4D: ROM u8 表 (memberIdx 索引, X/Y 坐标)。

## 影响面
- include/code_0.h:758: `void sub_8032948();` → `u32 sub_8032948(BattleObj *, u8 *);`
- linker.ld:85 后新增 `gUnk_08393A48` / `gUnk_08393A4D` 符号定义。
- src/event_hub.c: 新增文件内 `extern u8 gUnk_08393A48[]/gUnk_08393A4D[]`。
- 未改动其它函数区段。

## 锁定状态
- 已释放 (analyze-agent)。
