# Handoff: sub_802DE04 (0x0802DE04, 472 字节) — 吸血/回复状态机 (BLOCKED, 剩 5 字节)

日期: 2026-09-13 | agent: opencode-agent | 状态: ⏸ 阻塞 (status 保持 0)

## 1. 机器契约 (已验证事实)

```
address:         0x0802DE04
current_name:    sub_802DE04 (仅检索标签)
physical_tu:     src/event_actor.c (仍为 INCLUDE_ASM)
match_status:    0
entry_kind:      indirect — sub_803F444 对 slot 0xB..0x70 间接调用 (单参)
callees:         sub_80187E8, sub_80444A4, sub_801CE80, sub_803F5B4, sub_8020CC4,
                 sub_803F658, sub_8048D64
args:            r0 = BattleObj*
return:          r0 = r6 (0 或 2; case9 返回 2)
persistent_state: gUnk_03000820 状态机 (0 → 0x12..0x15 → 9), gUnk_03000825++
```

## 2. 功能语义 (E2)

case0 记位置/调色板, sub_801CE80(obj,5,0x1B4,0xC,1), f_B6=2 → 0x12。
case18/19 等 headA.kindFlags 0x800 或 frameIdx>0x66 → 0x14 (case19 播
sub_8020CC4(obj,0x78,0x50,0,0xE,0x32A,4))。
case20: headA.kindFlags&0x1000 时 **HP 回复** `obj->hp += sub_8048D64(obj, obj->maxHp>>1)`,
再 sub_801CE80 复位 + kindFlags|=0x100 → 0x15。
case21: 清 0x800 → state=9 + kindFlags&=0xFEFF。
case9: result=2。尾部 sub_803F658(obj) + gUnk_03000825++。

## 3. 精确剩余差异 — 仅 5 字节

`permuter/sub_802DE04/base.c` bytecmp (忽略 bl 槽) 只剩 5 字节 (3 条指令的寄存器 home):

```
target case20:
  adds r2, r5, #0     ; 地址 → r2
  adds r2, #0x6c
  ldrh r1, [r2, #0]   ; HP 值 → r1
  adds r1, r1, r0     ; r1 = HP + 返回值
  strh r1, [r2, #0]
candidate:
  adds r1, r5, #0     ; 地址 → r1
  adds r1, #0x6c
  ldrh r2, [r1, #0]   ; HP 值 → r2
  adds r0, r0, r2     ; r0 = 返回值 + HP
  strh r0, [r1, #0]
```

指令数/形状/立即数/分支/池全对, 仅 HP 地址伪寄存器与 HP 值伪寄存器的 r1/r2 home 互换。

## 4. 已排除实验 (全部仍 5 字节, 或更差)

- 加数顺序 `sub(...) + obj->hp` / `obj->hp + sub(...)` / `+=` / 强转 `(u16)(s16)(s32)`:
  仍 5 (或 51)。
- 局部 `u16 heal/v` 赋值再相加: 影响全函数 qty, 变差 (134+)。
- 局部指针 `u16 *hp=&obj->hp` / `u8 *p=(u8*)obj+0x6C`: 变差 (28/44)。
- 死临时 `u16 dead` / `new_var` 复用 / 空语句: 仍 5。
- `obj->maxHp / 2` vs `>> 1`: 仍 5。
- case20 前置/后置语句调换: 无效或变差。

### 4b. oc_12615 2026-09-13 补充实验 (全部仍 5 字节或更差)

- `(u16)(obj->hp + sub(...))` / `obj->hp + (u16)sub(...)` / `(s32)obj->hp + sub(...)` /
  `(s32)obj->hp + (s32)sub(...)` 再截断: 仍 5。
- `-(-sub(...) - obj->hp)` 负负技巧: 仍 5。
- 局部 `s32 half = obj->maxHp>>1` / `u16 half` / `u16 half = maxHp/2` 作实参: 仍 5。
- 局部 `s32 hp = obj->hp; hp = hp + sub(...); obj->hp = hp`: 165。
- 局部 `u16 hp` 同式: 67。
- **permuter 重跑**: 用自包含 5 字节 base (`permuter/sub_802DE04/base_sc.c`, 需 toml;
  本机无 pip, 用 `/tmp/oc_pylib/toml.py` shim) 跑 `permuter.py --stop-on-zero`,
  base score = 110, 110 次迭代无一改善 (全部 ≥115)。随机变换同样打不破 home 交换。

结论: 这是稳定的 GCC2.9 加法目的寄存器分配墙 — 目标把 `hp`(r1) 作 PLUS 目的、
候选把调用返回值(r0) 作目的。源码层所有自然写法都被 GCC 的交换/累加器启发式规约到候选形态;
permuter 也无法在自包含 TU 中翻转。需要 RTL 级或编译器 flag 级手段 (超出当前规则允许)。

## 5. 先前 agent 记录

`permuter/sub_802DE04/` 下有**前人**的 167 个 `output-*` 目录 (最佳 score 690),
其 base 用 `u8 *arg` 自包含版 + `new_var = arg` 复用; 未收敛 (690 远比本候选差)。
本候选 (5 字节, 近似 score 极小) 应作为新的搜索起点。

## 6. 下一步建议

1. 用 `tools/decomp-permuter` 以本候选 `permuter/sub_802DE04/base.c` 为 base 做局部搜索
   (结构/形状全对, 只差 home; 经验 87/88 的伪寄存器生死边界调整是最可能出路)。
2. 试「HP 地址伪寄存器兼职另一值」(经验 87): 例如让 `ptr` 同时用于本 case 的
   sub_801CE80 首参, 再看 home 是否翻到 r2。
3. 与已匹配的 sub_802DFDC (0x0802DFDC, 同族, `u32 ret`) 对照其 case 内 `+=` 写法。

## 7. 构建影响

无。本次未改任何共享文件 (header/linker 未动), 仓库 make+SHA1 绿 (853/1059)。
