# MATCH-801E690 — sub_801E690 匹配交接 (2026-09-12, agent-e690/gpnux)

## 结果

- `sub_801E690` @ `0x0801E690` (438 B 代码 / 440 B 含字面池): **bytecmp OK 一次通过**, fncheck OK。
- 源码落位 `src/battle_obj_core.c` (原 INCLUDE_ASM 占位替换为 C 体, 复用 TU 内 `Inl_QueuePushObj`)。
- `include/code_0.h:577` `void sub_801E690()` → `u32 sub_801E690(BattleObj *, BattleObj *)` (无调用者, 无其他影响)。
- `functions.tsv` status=1, asm 已移入 matchings。
- 全量 `make` + `sha1sum -c ll.sha1` 通过; 进度 801/1059。

## 机器契约 (E0/E1 级取证)

```text
address: 0x0801E690
current_name: sub_801E690
physical_tu: src/battle_obj_core.c
match_status: 1
entry_kind: dead code —— 全 ROM 无 BL / 无 4 字节指针引用 (与 801E30C/801E4D4 同族, 见 MATCH-801E4D4 handoff)
args: r0 = arg0 (BattleObj*, 效果选择子), r1 = arg1 (BattleObj*, 扣减目标/成员数组基址)
return: r0 = 0/1 —— 7 字节栈上 flags 数组是否存在回绕项
memory_reads:  arg0[0xBC](s8 选择子), arg0->animPtr, *(u16*)(animPtr+2) [mode0],
               animPtr+8+f_C2*2 处 u16 [mode1], entry->field_10 (idx*0x14 表 gUnk_08393B28),
               arg1->slot, arg1->f_AB, *(s16*)&f_6C, *(s16*)&f_B2, *(u16*)&f_6C
memory_writes: *(u16*)&f_6C (成员循环内为 member->f_6C), gUnk_030006F8[count], gUnk_03000714(+1),
               gUnk_03000718 (slot>0xB 时)
persistent_state: 入队三件套, 消费者 sub_801E040 / sub_8020B48 (同 E4D4)
script_relation: 无 (死代码)
```

## 语义模型

与 `sub_801E4D4` 完全相同的"效果扣减引擎": flags[7] 清零 → `(s8)arg0[0xBC]` 选查表入口 →
`gUnk_08393B28[idx].field_10` 模式分派 (0=arg1 单体 `f_6C -= f_B2` 下溢清零; 1=按 `arg1->slot>0xA`
分 7/5 档的 0xC8 步长成员循环, 跳过 slot==0xFF 与 f_AB==8) → 回绕项经 Inl_QueuePushObj 入队 →
尾部扫描 flags 返回 0/1。

**与 E4D4 的唯一差异 (查表入口, 即 E4D4 case 0/1 的 idx 计算不同):**

| mode | sub_801E4D4 | sub_801E690 |
|------|-------------|-------------|
| 0 | `*(u16*)(animPtr+0x1A) + 3` | `*(u16*)(animPtr+2)` |
| 1 | `*(u16*)(animPtr+0x1A) + 3 + *(u8*)(animPtr+0x29)` | `*(u16*)(animPtr+8+f_C2*2)` |

mode1 中 `f_C2 = *(u8*)(arg0+0xC2) = *(u8*)(animPtr指针槽+0x3A)`, 与已匹配 `sub_801DF90`
(以及 sub_801DEDC) 的 dispatch **字节同构** —— 三函数共用同一"从效果表选入口"模式族
(文档 FAMILIES.md 的 3x T3 家族: 801E30C/801E4D4/801E690, 通用处理者 801A5EC)。

## 复刻关键 (全部在 bytecmp 中验证)

1. **拼装来源**: dispatch 逐行取自已匹配 `sub_801DF90` 的源码 (case 0 内联
   `entry = &gUnk_08393B28[*(u16 *)(arg0->animPtr + 2)]`; case 1 三语句
   `anim = arg0->animPtr; off = arg0->f_C2 * 2; anim += 8;` 锁住 +8 的运行时加);
   主体/尾部逐行取自已匹配 `sub_801E4D4` 的源码 (去掉其 idx/t 计算)。
2. **沿用 E4D4 形状规则**: ① `switch (*(u16 *)((u8 *)entry + 0x10))` 直派发
   (r3 直落, 防寄存器分裂); ② limit 三元式 `(arg1->slot > 0xA) ? 7 : 5`;
   ③ 回绕判断 `if (diff <= 0) {清零; wrapped=1}` 在前, `bgt` 跳减法块;
   ④ 尾部 `while (++i <= 6)` + flags[0] 重载形状; ⑤ Inl_QueuePushObj static inline。
3. 本函数 **无** E4D4 的 "s32 t 两语句" 问题 (该规则针对 `h+3+b` 加法链, 此处无此链);
   也无 u16 截断序列 (目标 case 0/1 直接 `ldrh rX,[..]` 进 `lsls #2` 倍乘, 无 lsls/lsrs)。

## 工具事实

- bytecmp 命令: `scripts/bytecmp.sh sub_801E690 <候选.c> "gUnk_08393B28 = 0x08393B28;" "gUnk_030006F8 = 0x030006F8;" "gUnk_03000714 = 0x03000714;" "gUnk_03000718 = 0x03000718;"`
- 候选备份: `.scratch/agent-e690/sub_801E690_base.c` (自包含版, 用局部 typedef; 合入版用真实头文件, 两者字节一致)。
- E4D4 handoff 的工具提醒仍然有效: 字节对比用 baserom/全量 make, 不信陈旧 ll.gba。

## 家族收官状态

- [x] sub_801E4D4 (2026-09-12, gpnux)
- [x] sub_801E690 (本次)
- [ ] sub_801E30C (237 asm 行, status=0) —— 同族最后一个, 预计同套路:
      先 diff 其 dispatch 与本函数/E4D4 的异同, 主体大概率仍是同一扣减引擎变体。
- .claims/sub_801E690.lock 未释放 (锁属 gpnux, 由其自行管理)。
