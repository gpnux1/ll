# MATCH-8027D9C-20260913

## 目标
- address: 0x08027D9C
- current_name: sub_8027D9C
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 764B)
- entry_kind: task-table (handler 指针表 0x0839CD5C, sub_803F444 间接调用)
- args: r0 = BattleObj *obj, r1 = BattleObj *arg1 (同池另一对象)
- return: r0 = u32 result (r8 承载; 0 继续 / 1 完成)
- 锁交接: 原锁 claude (进行中, 空 note); 本包由 gpnux 接管完成并释放。

## arg1 证据 (E2)
- 同 sub_80282EC: sub_803F444 用 `_call_via_r2` 传 (obj, r6=第二参); 调用点 0x0801BFD8 第二参
  = 对象池基址 + obj->f_BD*0xC8 → 同池 BattleObj。
- 本函数访问 arg1: +0xBF posX, +0xC0 posY。

## 机器契约
- callees: sub_80444A4(BattleObj*), sub_801CE80(BattleObj*,u8,u16,u8,u8),
  sub_803F5B4(BattleObj*), sub_8020CC4(void*,u8,u8,u16,u8,u16,u16), Sfx_Play(u16,u8,u8),
  sub_8044514(s16), sub_801768C(s16,s16,s16,s16,u8), sub_803F658(BattleObj*)
- memory_reads: obj +0xBF/+0xC0, +0x2A headA.f_1E, +0x35 headA.palSlot,
  +0x24 headA.kindFlags, +0x28 headA.frameIdx, +0x54 headB.kindFlags;
  arg1 +0xBF/+0xC0; gUnk_03000820/22/24/25/28/29/44/45/56
- memory_writes: obj +0x67 headB.f_2B, +0x68 headB.f_2C, +0x24 headA.kindFlags, +0xB0 state;
  gUnk_03000820 状态, gUnk_03000825 计数, gUnk_03000828/29/22/24 暂存

## 语义 (状态机, 0/9/0x12..0x18)
- 0: 暂存位置/头字段; sub_80444A4; sub_801CE80(obj,1,0x1B4,0xC,0); sub_803F5B4; 计数=0 → 0x12。
- 0x12: headA.kindFlags & 0x800 落 → 0x13。
- 0x13: headA.frameIdx==0x20 → sub_8020CC4(obj, posX+0xA, posY-0x14, 0x300, 0xE, 0x2DA, 5);
       headA.kindFlags |= 0x100; 计数=0 → 0x14。
- 0x14: headB.kindFlags & 0x800 落 → Sfx_Play(0x31,1,0) → 0x15。
- 0x15: 计数<=9 → headB.f_2B = interp(obj->posX+0xA, arg1->posX-(obj->posX+0xA), 0xA, 计数, 2);
       headB.f_2C = interp(obj->posY-0x14, (arg1->posY+0xC)-obj->posY, 0xA, 计数, 2);
       否则 计数=0; sub_8044514(0x14); → 0x16。
- 0x16: 计数<=9 → headB.f_2B = interp(arg1->posX, (obj->posX+0xA)-arg1->posX, 0xA, 计数, 1);
       headB.f_2C = interp(arg1->posY-8, (obj->posY-0xC)-arg1->posY, 0xA, 计数, 1);
       否则 计数=0; headA.kindFlags &= 0xFEFF; obj->state &= 0xDFFF; → 0x17。
- 0x17: headA.kindFlags & 0x1000 → sub_801CE80(obj,0,0822,0824,0) → 0x18。
- 0x18: 无其他演出占用 → headA.kindFlags &= 0xFEFF; → 9。
- 9: result = 1 (注意本变体无 0844/45/56 判定, 直接置 1)。
- 尾: gUnk_03000825 += 1; sub_803F658(obj)。

与 sub_80282EC 差异: 插值写 obj 的 headB.f_2B/f_2C (精灵滑动起点), 斜向绕行; case0 用
sub_801CE80(obj,1,...) (arg1=1); 9 态直接 result=1。

## 字节匹配关键点
- 9 态不与 0x18 共用三全局判定, 直接 `result = 1`。
- 插值 counter 直接读 gUnk_03000825 传入 (不缓存到局部 cnt), 否则多一个活跃变量抢占寄存器
  (cnt→r6 会把 arg1 挤到 r7, 全局寄存器分配无法对齐)。
- 寄存器布局 (目标): r5=obj, r6=arg1, r7=&gUnk_03000825, r8=result, r3=&gUnk_03000820。
- 4 个插值各自独立表达式, 尾块 `subs r1,r1,r2` + call 由 GCC 自动按 0x15/0x16 两处合并 (mode=2/1)。

## 验证
- scripts/bytecmp.sh: 仅 11 个 bl 占位字节不同 (relocation), 归一化后 0 diff (764B)。
- python3 scripts/fncheck.py sub_8027D9C: OK (764 bytes @0x08027d9c, 26 池重定位已施加, 11 bl 槽忽略)。
- make + sha1sum -c ll.sha1: 通过 (进度 815→817)。

## 改动文件
- src/event_actor.c: 仅 0x08027D9C 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 原型 void sub_8027D9C() → u32 sub_8027D9C(BattleObj *, BattleObj *)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_8027D9C/base.c: 候选 (含 claude 初稿的修正: sub_8020CC4 第4参 0→0x300)。
