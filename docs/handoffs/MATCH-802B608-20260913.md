# MATCH-802B608-20260913

## 目标
- address: 0x0802B608
- current_name: sub_802B608
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 692B)
- entry_kind: task-table (handler 指针表 0x0839CD5C)
- args: r0 = BattleObj *obj
- return: r0 = u8 result (0 继续 / 1 动作完成)

## 机器契约
- callees: sub_80187E8(void), sub_80444A4(BattleObj*), sub_801CE80(BattleObj*,u8,u16,u8,u8),
  sub_803F5B4(BattleObj*), sub_801A348(void), sub_8019B98(u8,u8,u8,u8)->u8,
  Sfx_Play(u16,u8,u8), sub_801A2AC(int,int,int),
  sub_804BDD8(u8,u8,u8,s8,u8), sub_8044514(s16), sub_801768C(s16,s16,s16,s16,u8),
  Sfx_StopTrack(u8), sub_804BE90(u8,u8), sub_803F658(BattleObj*)
- memory_reads:
  - obj+0xBF posX, obj+0xC0 posY, obj+0x2A headA.f_1E, obj+0x35 headA.palSlot,
    obj+0x24 headA.kindFlags
  - gUnk_03000820/0822/0824/0825/0828/0829/0867, gUnk_03000844/45/56
  - REG_DISPCNT (0x04000000)
- memory_writes:
  - obj+0xB6 f_B6 = 2, obj+0xB4 f_B4 = 0
  - obj+0x24 headA.kindFlags ((|0x100) & 0xEFFF)
  - gUnk_03000820 状态, gUnk_03000825 计数, gUnk_03000867 渐变量, gUnk_03000828/29/22/24 暂存
  - REG_DISPCNT &= 0xFDFF (清 bit9)

## 语义 (状态机)
- 0: 暂存 posX/posY/f_1E/palSlot; sub_80444A4; sub_801CE80(obj,5,0x1B4,0xC,0);
     sub_803F5B4; 计数=0; f_B6=2; f_B4=0 → 0x12。
- 0x12: headA.kindFlags & 0x800 落 → 0x13。
- 0x13: headA.kindFlags 到 0x1000 → kindFlags = (kindFlags|0x100)&0xEFFF;
       sub_801A348() → 0x14。
- 0x14: sub_8019B98(0xD,3,0xE,2) != 0 → Sfx_Play(0x55,1,0);
       sub_801A2AC(0x1447,9,0xF); sub_804BDD8(0xE,1,1,-3,0xC);
       sub_8044514(0x3C) → 0x15。
- 0x15: 计数 <= 0x3B → 计数++; 否则 计数=0 → 0x16。
- 0x16: 计数 <= 0x1D → gUnk_03000867 = sub_801768C(9,-9,0x1E,计数,2);
       sub_801A2AC(0x1447,gUnk_03000867,0xF); 计数++。
       否则 Sfx_StopTrack(1); sub_804BE90(0xE,1); sub_801A2AC(0,0,0);
       REG_DISPCNT &= 0xFDFF; sub_801CE80(obj,0,0822,0824,0) → 0x17。
- 0x17: headA.kindFlags & 0x800 落 → 0x18。
- 0x18: 计数=0 → 9。
- 9: 无其他演出占用 → result = 1。
- 尾部: sub_803F658(obj) (无帧计数自增)。

## GCC2.9 形状要点 (字节匹配必需)
- `sub_8019B98` 返回类型必须为 u8: 目标在调用后 `lsls r0,r0,#0x18; cmp r0,#0`
  (对返回值做 u8 截断判断); 用 u32 会少这条 lsls。
- case19 的 kindFlags 必须拆成两条语句 `kindFlags |= 0x100;` + `kindFlags &= 0xEFFF;`
  (或等价 local 变量); 写成单表达式 `(x|0x100)&0xEFFF` 会多一次寄存器拷贝
  (`ldr r2,=0xEFFF; adds r1,r2,#0`)。
- DISPCNT 必须写成 `*(volatile u16 *)(0x80 << 0x13)` (字面移位) 而非 0x04000000 常量,
  目标用 `movs r2,#0x80; lsls r2,r2,#19` 现场计算。
- gUnk_03000822 为 u16。

## 验证
- python3 scripts/fncheck.py sub_802B608: OK (692 bytes @0x0802b608, 49 池重定位已施加, 17 bl 槽忽略)。

## 改动文件
- src/event_actor.c: 0x0802B608 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 原型 void sub_802B608() → u8 sub_802B608(BattleObj *);
  sub_8019B98 原型 void() → u8(u8,u8,u8,u8)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_802B608/base.c: 候选。
