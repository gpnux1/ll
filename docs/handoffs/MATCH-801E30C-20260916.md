# Handoff: MATCH-801E30C (2026-09-16, opencode-1) — 战斗效果扣血引擎第 3 变体 已匹配

## 结果

- `sub_801E30C` (0x0801E30C, 456B): status 0→1。bytecmp **OK 456B 逐字节全等**（函数无 bl，零豁免基线）;
  fncheck OK; gen_asm 迁移 matchings/; `make clean && make -j4 && sha1sum -c ll.sha1` 全绿; 904/1059。
- 合入: `src/battle_object_engine.c` 占位处; `include/battle_object_engine.h:102` 原型 `void()`→`u32(BattleObj*,BattleObj*)`。
- 回归: 同 TU 的 sub_801E4D4 / sub_801E690 fncheck OK（Inl_QueuePushObj 定义位置提升无影响）。

## 机器契约

```
address: 0x0801E30C; entry_kind: 死代码 (全 ROM 无 bl、无 4 字节指针引用; 同族 E4D4 亦标死代码)
args: r0=arg0 BattleObj* 施法者 (fxKind/equipSlots/skills/animSubIdx), r1=arg1 BattleObj* 目标/队首
return: u32 0/1 = flags[0..6] 中是否出现回绕(致死)标志
callees: 无 (队列逻辑 ROM 内联展开 Inl_QueuePushObj, 与 E4D4/E690 三处同构)
persistent_state: gFxQueueObjs(0x030006F8)/gFxQueueWriteIdx(0x03000714)/gUnk_03000718(0x03000718),
  读 ROM 表 gUnk_0839CC4C (道具效果配对, u32 项, 取低 le16)/gUnk_08393B28 (ObjAnimEntry)
```

## 关键取证（零信任复核）

- gbadisasm 与 ROM 原始字节逐指令核对 (force-thumb objdump): `adds r0, r6, #3` 真实存在，r6 在函数内
  **任何定义之前**被读 → 序言 `mov r6, sb` 残留 = 真 UB（原始源码的未赋值指针局部），非反汇编丢指令。
- 排除的错误假设: ①r6=arg1（另一 agent 的 `arg1[3]` 解读——它把地址加法读成数值加法，×100 缩放自爆）;
  ②r6=animPtr 漏 load（若如此应见 `ldr r6,[r3,#0x88]` 残形）。E4D4/E690 的 animPtr 路径均有显式 ldr 对照。
- 索引语义: case0 装备槽1 道具的效果配对 id (le16) 与 case1 skills[animSubIdx] 都再过 `[r6+3]` 字节间接 → 效果表项。

## 并发备注

开工时 `sub_801E30C` 存在 main801E30C (2026-09-10 认领) 陈旧锁，且 14:4x 有并发写入覆盖了
`permuter/sub_801E30C/base.c`（其 arg1[3] 假设已被上述证据否定）。本工作全程在
`.scratch/opencode-e30c/` 完成 (v1..v8)，未触碰该 permuter 目录与对方锁；仅按用户指令
完成匹配并合入。`0x0801E30C` 锁已释放。候选终版 = `.scratch/opencode-e30c/v8.c`（与 src 一致）。
