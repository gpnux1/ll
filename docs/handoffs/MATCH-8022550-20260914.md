# sub_8022550 匹配记录 (2026-09-14)

## 1. 基础信息
- **地址**: `0x08022550` (ROM 偏移 `0x22550`)
- **大小**: 448 字节 (193 指令/字)
- **TU**: `src/scene_obj_fx.c`
- **原型**: `void sub_8022550(BattleObj *objs, u8 arg1, u8 arg2, s8 *out)`
- **状态**: 1 (完全匹配, bytecmp OK, fncheck OK)

## 2. 机器契约与语义建模
该函数根据战斗对象的属性字段（`+0xAC` 双 nibble）与有效性标记（`slot != 0xFF`），按阵营范围筛选对象并分类写入输出数组：
1. **输入参数**:
   - `objs` (`r0` -> `r7`): 战斗对象数组首地址（步长 0xC8 = 200 字节，对应 `BattleObj`）。
   - `arg1` (`r1`): 目标分类特征码（与 `objs[i].pad_AC[0] & mask1` 比较）。
   - `arg2` (`r2`): 检索阵营模式（`0` = 玩家侧对象 `0..4`；`1` = 敌方侧对象 `5..11`）。
   - `out` (`r3` -> `r4`): 长度为 3 的 `s8` 输出缓冲区，用于接收按次级分类提取的最多 3 个对象槽位索引。
2. **处理流程**:
   - **初始化**: `out[0..2] = -1;`
   - **一阶掩码生成**: `mask1 = (arg1 <= 3) ? 0x0F : 0xF0;`
   - **对象过滤入栈**:
     - `switch (arg2)` 分流：
       - `case 0`: 遍历 `objs[0..4]`；
       - `case 1`: 遍历 `objs[5..11]`；
     - 满足 `(objs[i].pad_AC[0] & mask1) == arg1 && objs[i].slot != 0xFF` 者，将其下标 `i` 存入局部栈缓冲区 `stack_buf[count++]`。
   - **二阶反向掩码生成**: `mask2 = (arg1 <= 2) ? 0xF0 : 0x0F;`
   - **次级分类分派 (Switch)**:
     - 遍历所有收集到的索引 `stack_buf[i]`，读取 `objs[stack_buf[i]].pad_AC[0] & mask2`：
       - 值为 `1` 或 `0x10`: `out[0] = stack_buf[i];`
       - 值为 `2` 或 `0x20`: `out[1] = stack_buf[i];`
       - 值为 `3` 或 `0x30`: `out[2] = stack_buf[i];`

## 3. GCC2.9 关键形状与寄存器分配
- **寄存器布局**:
  - `objs` -> `r7`
  - `out` -> `r4`
  - `count` -> `r5`
  - `mask` -> `r6`
  - `i` -> `r3`
  - `arg1` -> `r1` (常驻)
  - `arg2` -> `r2` (switch 后释放)
- **破局关键**:
  1. `switch (arg2)` 生成 `cmp r2, #0; beq; cmp r2, #1; beq; b;` 形状。
  2. `stack_buf[count] = i; count++;` 必须写为单独语句，而非 `stack_buf[count++] = i;`（后者会在递增后才写栈并拆散指令）。
  3. `switch (objs[stack_buf[i]].pad_AC[0] & mask)` 内部，`case 1` 与 `case 0x10`、`case 2` 与 `case 0x20`、`case 3` 与 `case 0x30` 必须展开为独立的 case 语句块（即使逻辑相同），使 `out` 在 AST 中产生 6 处引用，从而在 GCC 2.9 全局寄存器分配（`global_alloc`）的 QTY 优先级计算中压过 `count`，成功将 `out` 锁定在 `r4`、`count` 锁定在 `r5`。

## 4. 验证结果
```
fndiff: 0
bytecmp: OK (448 bytes)
fncheck: sub_8022550 OK (448 bytes @0x08022550, 49 池重定位已施加, 0 bl 槽忽略)
```
