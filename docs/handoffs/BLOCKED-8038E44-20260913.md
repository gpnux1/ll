# Handoff: sub_8038E44 (0x08038E44, ~480 字节) — NPC 对话状态机变体 (BLOCKED, 剩 2 字节)

日期: 2026-09-13 | agent: opencode-agent | 状态: ⏸ 阻塞 (status 保持 0)

## 1. 机器契约 (已验证事实)

```
address:         0x08038E44
current_name:    sub_8038E44 (仅检索标签)
physical_tu:     src/event_hub.c (仍为 INCLUDE_ASM)
match_status:    0
entry_kind:      indirect (状态机 handler)
args:            r0 = arg0 (BattleObj*), r1 = arg1 (BattleObj* / 目标对象)
                 case1 读 arg1+0xBE (slot)
return:          r0 = ret (0 或 1)
callees:         sub_80187E8, sub_80444A4, sub_803F5B4, sub_803F658, sub_803E58C,
                 sub_8020974, sub_804C3A4, sub_801B954, sub_8044514, sub_8045B90,
                 Sfx_Play
persistent_state: gUnk_03000820 (0→1..9), gUnk_03000825, gUnk_0300086B,
                 gUnk_03000828/29
```

## 2. 功能语义

与已匹配 sub_8038C84 (0x08038C84) 同族 (NPC 对话状态机), 差异:
- case1: `sub_803E58C(arg0, arg1, 2)` (C84 为 0), 动画 `0x3C8` (C84 为 0x3C7),
  `arg1->slot <= 0xA` 时 kindFlags|=0x20。
- case3: 两段帧检查 —— frameIdx==0x24 → Sfx(0x31)+sub_8044514(0x1E);
  frameIdx>0x43 → Sfx(0x31)+sub_8044514(0x28) → 5。
- case5: 确认 0x1000 → sub_804C3A4(palSlot, sub_801B954(&headA)) + g6B=0xC → 8。
- case8: sub_803E58C(arg0,arg1,0)==1 → 9。
- case9: g44/45/56 全 0 → sub_8045B90(arg0, pad_A1) + ret=1。
- 尾部 sub_803F658(arg0)。

## 3. 精确剩余差异 — 仅 2 字节

`permuter/sub_8038E44/base.c` bytecmp (忽略 bl 槽) 只剩 2 字节:

```
case0 (target):
  adds r0, r5, #0
  adds r0, #0xb6
  movs r2, #0        @ 0x7e  (zero → gUnk_0300086B)
  movs r1, #0        @ 0x80  (zero2 → f_B6/f_B4)
candidate:
  movs r1, #0        @ 0x7e
  movs r2, #0        @ 0x80
```

两条独立 `movs rX,#0` 的**调度顺序**互换 (地址计算已完成, 之后 2 条 movs 顺序差)。

## 4. 已排除实验 (全部仍 2 字节, 或更差)

- zero/zero2 赋值顺序 (4 种) / 类型 u8/u16/u32 / 角色互换 / 依赖赋值。
- b6ptr 位置 (前置/后置) / `b4=0` 字面量 / `*b6ptr=0` 字面量 / 直接 `f_B4=0`。
- `spr` 局部 (对齐 C84 inventory) / 死变量插入 / 语句迁移 (state=1 提前等)。
- 孤立复现 (仅 case0 的函数体) 生成 `mov r1,#0; add r3,...; mov r2,#0` (与候选同向),
  证明顺序由**全函数伪寄存器 inventory**决定,非 case0 局部写法。

## 5. 下一步建议

1. 用 `tools/decomp-permuter` 以本候选为 base 做局部搜索 (只差 1 条 movs 调度)。
2. 经验 87: 让 `zero`/`zero2` 之一兼职第二个值 (如 g6B 的 zero 同时作
   sub_803E58C 的第三参常量 0/2 的载体), 改变 qty 优先级。
3. 对照已匹配 sub_8038C84 的**完整**源码探针: C84 的其他 case 使同一 case0 写法排成
   target 顺序; 可尝试在候选里加入 C84 case2/5 的等价中间变量以对齐 inventory。

## 6. 构建影响

候选需要的原型 `u32 sub_8038E44(BattleObj *, BattleObj *)` 本次临时加入后已回退,
未污染仓库。make+SHA1 绿 (855/1059)。
