# sub_8047024 匹配记录 (2026-09-16)

## 1. 基础信息
- **地址**: `0x08047024` (ROM 偏移 `0x47024`)
- **大小**: 392 字节 (0x188)
- **TU**: `src/battle_engine.c`
- **原型**: `u16 sub_8047024(BattleObj *obj, u8 kind)`
- **状态**: 1 (完全匹配, fncheck OK 392B, make 全量 SHA1 一致通过)

## 2. 机器契约与语义建模
该函数是战斗系统中战斗对象的**综合属性值结算器**（带装备特技被动修正在内）。
与上游 `sub_8046F0C`（根据 `gStatRecalcKind` 纯读基础属性 + 能力修正）共享完全相同的属性槽映射（0..12）：
- 0: `obj->lv` (等级)
- 1: `obj->hp` (当前HP)
- 2: `obj->maxHp` (最大HP)
- 3: `obj->mp` (当前MP)
- 4: `obj->maxMp` (最大MP)
- 5: `obj->atc + obj->statMods[0]` (攻击)
- 6: `obj->def + obj->statMods[1]` (防御)
- 7: `obj->agl + obj->statMods[2]` (敏捷)
- 8: `obj->men + obj->statMods[3]` (精神)
- 9: `obj->res + obj->statMods[4]` (抗性)
- 10: `obj->noa` (攻击段数)
- 11: `obj->posX` (战斗坐标X)
- 12: `obj->posY` (战斗坐标Y)
- >12: 返回 0

### 2.1 装备特技被动修正机制
在获取 5..9 项主攻防抗性时，调用 `sub_804E76C(obj, 0, effectId)` 检查对象装备栏各槽是否具有特定的被动能力：
1. **攻击 (kind 5)**:
   - 检查 `0x1E`：若满足，攻击提升 10% (`ret += ret / 10`)；
   - 检查 `0x0C`：若满足，攻击降低 40% (`ret -= (ret / 10) * 4`)；
   - 检查 `0x0D`：若满足，攻击降低 50% (`ret -= (ret / 10) * 5`)。
2. **防御 (kind 6)**:
   - 检查 `0x1E`：若满足，防御提升 10% (`ret += ret / 10`)。
3. **敏捷 (kind 7)**:
   - 无装备特技被动修正。
4. **精神 (kind 8)**:
   - 检查 `0x1F`：若满足，精神提升 10% (`ret += ret / 10`)。
5. **抗性 (kind 9)**:
   - 检查 `0x1F`：若满足，抗性提升 10% (`ret += ret / 10`)。

### 2.2 GCC 2.9 代码生成细节
- 局部变量 `u16 bonus; u16 ret = 0;`。
- 整数除以 10 调用 `__udivsi3`。
- `bonus = (ret / 10) * 4; ret = ret - bonus;` 触发 `lsls r0, r0, #18; lsrs r1, r0, #16; subs r0, r5, r1`。
- `bonus = ret / 10; bonus = bonus * 5; ret = ret - bonus;` 触发标准 GCC `lsls r0, r1, #2; adds r0, r0, r1` 并截断。
- Case 6、8、9 的加成部分由 GCC 跳转优化器自动交叉尾合并（cross-jumping）到 `_08047172`，并共享 `adds r0, r5, r1` 直落 `_0804718A`（`lsls r0, r0, #16; lsrs r5, r0, #16; b _080471A2`）。
- Case 0、10、11、12 统一从 `ldrb r5, [r0]` 尾部返回。

## 3. 验证结果
- `python3 scripts/gen_asm.py`：asm 提取同步正常。
- `python3 scripts/fncheck.py sub_8047024`：OK (392 bytes @0x08047024, 14 池重定位已施加, 8 bl 槽忽略)。
- `sub_8044F4C`、`sub_8046F0C`、`sub_8047B1C` 等上下游邻近函数：fncheck 均保持 100% OK。
- `timeout 900 make 2>&1 | tail -10`：全量构建成功，`sha1sum -c ll.sha1` 校验通过（894/1059，84.4%）。
