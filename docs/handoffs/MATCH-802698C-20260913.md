# MATCH-802698C-20260913

## 目标
- address: 0x0802698C
- current_name: sub_802698C
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 892B)
- entry_kind: task-table (handler 指针表 0x0839CD5C, sub_803F444 间接调用)
- args: r0 = BattleObj *obj
- return: r0 = u32 result (0 继续 / 1 完成)

## 机器契约
- callees: sub_80444A4, sub_803F5B4, sub_801CE80, sub_801B81C, sub_8044514,
  sub_803F658, GetObjPool, Rng_LcgNext (经 ((u32(*)(void))f)()), __umodsi3
  (无 sub_80187E8)
- memory_reads: obj +0x24 headA.kindFlags, +0x28 headA.frameIdx, +0x2A headA.f_1E,
  +0x35 headA.palSlot, +0x54 headB.kindFlags, +0x58 headB.frameIdx;
  gUnk_03000820/22/24/25/3D/40/44/45/56/58, gUnk_03004F90
- memory_writes: obj headA.kindFlags, headB.kindFlags, headB.frameIdx, headB.f_2A (+0x66),
  obj state (+0xB0), obj f_B6 (+0xB6); gUnk_03000820/22/24/25, gUnk_03004F90
- 只读数据: 0x08393B28 表项两处
  - +0x3714 (sub_801B81C case2): u32@+0, u32@+4, u16@+8, u16@+A
  - +0x3728 (case1 二次装配): u32@+0, u32@+4, u16@+8, u16@+A

## 语义 (状态机 0/1/2/3/4/5/6/7/9)
- 0: sub_80444A4 清同组伤害; sub_803F5B4 → 3
- 3: 存 headA.f_1E/palSlot; sub_801CE80(obj,5,0x1B4,0xD,0); 计数=0 → 4
- 4: headA.frameIdx>0x46 → 2
- 2: sub_801B81C(&headB, 0x80,0x77,0x2EA,0xE, 表项+0x3714, 4); state|=0x2000;
     headB.f_2A=2; 计数=0 → 1
- 1: headB.frameIdx==9 → headB/headA.kindFlags|=0x100; 计数=0; frameIdx=0xA
     headB.frameIdx>9 → frameIdx=(frameIdx+1)%8+0xA; 计数++
       计数==0x10 → headA.kindFlags&=0xFEFF
       计数>0x17 → headB.kindFlags&=0xEFFF; sub_801B81C(&headB,0x74,0x80,0x2EA,0xE, 表项+0x3728,4);
                   headB.kindFlags|=0x2000; state|=0x2000; headB.f_2A=2 → 5
- 5: headB.kindFlags&0x1000 → &=0xEFFF; state&=0xDFFF; obj->f_B6=1; sub_8044514(0x28) → 6
- 6: gUnk_03000858!=0 → 9 否则 → 7
- 7: headA.kindFlags&0x1000 → &=0xEFFF; sub_801CE80 复位 → 9
- 9: 无其他演出占用 (0844/45/56==0) → GetObjPool(); for i<083D:
       (0830-40[i]&0xF0)==0x10 且 Rng%100<=0x27 → gUnk_03004F90[0830-40[i]&0xF]=1; result=1
- 尾: sub_803F658(obj); headA.kindFlags&0x1000 → 清位 + sub_801CE80 复位; return result

## 字节匹配关键点
- **case1 frameIdx 回绕必须写** `s32 v = f + 1; f = v - (v / 8) * 8 + 0xA;`
  目标生成 `r2=v; r0=v; asrs r0,#3; lsls r0,#3; subs r0,r2,r0; +0xA`。
  写成 `(v >> 3) << 3` 或 `v % 8` 会少一次 `adds r0,r2,#0` 拷贝 (差 40B)。
- 两次表项读取用**同符号 u32 数组索引** (`gUnk_08393B28[0xDC5]` 等), agbcc 才生成 base+off
  运行时相加 (见 sub_8026D08 解法); raw 常量/typed struct 会折成单一 reloc。
- `Rng_LcgNext` 必须 `((u32 (*)(void))Rng_LcgNext)()` 强转调用, 才能得到 unsigned `%` → `__umodsi3`
  (u16 返回会被截断成不同序列)。
- case9 循环用 `gUnk_03000840[i]` (u8* 指针) 双次读取, 与 sub_802761C case9 同构。

## 验证
- scripts/bytecmp.sh: 仅 12 个 bl 占位字节不同 (relocation), 归一化后 0 diff (892B)。
- python3 scripts/fncheck.py sub_802698C: OK (892 bytes @0x0802698c, 11 池重定位已施加, 12 bl 槽忽略)。
- make + sha1sum -c ll.sha1: 通过 (进度 824→828)。

## 改动文件
- src/event_actor.c: 仅 0x0802698C 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 原型 void sub_802698C() → u32 sub_802698C(BattleObj *)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_802698C/base.c: 候选。
