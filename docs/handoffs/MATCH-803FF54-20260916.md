# sub_803FF54 匹配记录 (2026-09-16)

## 1. 基础信息
- **地址**: `0x0803FF54` (ROM 偏移 `0x3FF54`)
- **大小**: 600 字节 (含 57 项 jump table 与字面池)
- **TU**: `src/battle_stage_effects.c`
- **原型**: `u8 sub_803FF54(BattleObj *obj)`
- **状态**: 1 (完全匹配, fncheck OK 600B, make 全量 SHA1 绿)

## 2. 机器契约与语义建模
本函数是**战斗我方角色逃跑演出状态机**（由 `sub_8044680` 在 `obj->slot <= 10` 时分发进入；`slot > 10` 敌方逃跑则进入姊妹函数 `sub_80405A4`）。
驱动状态 PC 为 `gObjActStep` (0x03000820)，内部按步骤流转。

### 2.1 依赖关系与生命周期
- **上游调用者**:
  - `sub_8044680(BattleObj *arg0)`: `if (arg0->slot <= 10) return sub_803FF54(arg0); else return sub_80405A4(arg0);`
- **被调子函数**:
  - `sub_801CBA4(BattleObj *obj, u8 kind, u16 f2a, u8 pal, u8 arg5)`: 角色动作/调色板装配与复位。
  - `sub_8048458(BattleObj *obj)`: 逃跑成功率检定（返回 1 成功，0 失败）。
  - `sub_801768C(s16 start, s16 delta, s16 totalFrames, s16 currentFrame, u8 mode)`: 浮点插值计算位移。

### 2.2 全局变量与跨帧状态
- `gObjActStep` (`0x03000820`): 状态机主步骤 PC。
- `gObjActSavedF2A` (`0x03000822`): 暂存 `obj->headA.f_1E`。
- `gObjActSavedPal` (`0x03000824`): 暂存 `obj->headA.palSlot`。
- `gObjActStepTimer` (`0x03000825`): 帧计时器。
- `gObjActSavedX` (`0x03000828`): 暂存 `obj->posX`。
- `gObjActSavedY` (`0x03000829`): 暂存 `obj->posY`。
- `gUnk_03000864` (`0x03000864`): 逃跑判定结果（0=成功，1=失败）。在 `linker.ld` 与 `include/iwram.h` 登记。
- `gObjActDoneCount` (`0x03000865`): 对象演出完成计数，逃跑离场时自增。

### 2.3 状态机流转流程
1. **case 0 (初始化起手)**:
   - 暂存 `palSlot`、`f_1E`、`posX`、`posY`；
   - 调用 `sub_801CBA4(obj, 8, 0x1B4, 0xD, 0)` 配置逃跑起跑动画；
   - `gObjActStep = 0x30; gObjActStepTimer = 0; break;`
2. **case 0x30 (检定逃跑判定)**:
   - 调用 `sub_8048458(obj)`；
   - 若返回 1 则 `gUnk_03000864 = 0` (成功)，否则 `gUnk_03000864 = 1` (失败)；
   - `gObjActStep = 0x33; break;`
3. **case 0x33 (等待 50 帧并分流)**:
   - 若 `gObjActStepTimer <= 0x31` 则自增；
   - 否则根据 `gUnk_03000864` 是否为 0 分流：
     - 为 0 (成功) -> `gObjActStep = 0x34;`
     - 非 0 (失败) -> `gObjActStep = 0x36;`
     - `gObjActStepTimer = 0;`
4. **case 0x34 (成功向右逃跑位移)**:
   - 若 `gObjActStepTimer <= 0x18` (25 帧)：
     - `obj->posX = sub_801768C(gObjActSavedX, 0x100 - gObjActSavedX, 0x19, gObjActStepTimer, 1);` 向屏幕右侧 (256) 移动出屏；
     - `gObjActStepTimer++;`
   - 超时后 `gObjActStep = 0x35;`
5. **case 0x35 (成功脱离战斗槽)**:
   - `obj->slot = 0xFF; obj->variantClass = 7; gObjActStep = 0x38; gObjActDoneCount++; result = 1; break;`
6. **case 0x36 (失败复位姿态)**:
   - `sub_801CBA4(obj, 0, gObjActSavedF2A, gObjActSavedPal, 0);` 恢复角色待机；
   - `gObjActStep = 0x38; break;`
7. **case 0x38 (收尾)**:
   - `result = 1; break;`

## 3. 验证结果
- `scripts/fncheck.py sub_803FF54`:
  ```
  sub_803FF54              OK   (600 bytes @0x0803ff54, 76 池重定位已施加, 4 bl 槽忽略)
  ```
- 关联函数自测:
  - `sub_80405A4`: OK
  - `sub_8042200`: OK
  - `sub_8042AB4`: OK
  - `sub_8044680`: OK
- `make` 与 `sha1sum -c ll.sha1`:
  ```
  ll.gba: 成功
  匹配进度: 908/1059 (85.7%)
  ```

## 4. 符号与文件影响
- `include/battle_stage_effects.h`: 将 `sub_803FF54(u8 *)` 修正为规范结构体指针 `sub_803FF54(BattleObj *obj)`。
- `include/battle_flow_rules.h`: 将 `sub_8048458()` 声明修正为 `u8 sub_8048458(BattleObj *obj)`。
- `include/iwram.h`: 声明 `extern u8 gUnk_03000864;`。
- `linker.ld`: 增加 `. = 0x00000864; gUnk_03000864 = .;`。
- `src/battle_stage_effects.c`: 替换 `INCLUDE_ASM` 为真 C 实现，引入 `#include "battle_flow_rules.h"`。
- `functions.tsv`: `sub_803FF54` 状态标记为 1 并附详细 note。
