# BLOCKED-8046E18 — 战斗队列降序快速排序分区函数 (Hoare Partition)

## 1. 结论与基本信息
- **address**: `0x08046E18` (122 字节, Thumb)
- **current_name**: `sub_8046E18`
- **physical_tu**: `src/battle_engine.c:1317`
- **match_status**: 0 (未匹配，逻辑/控制流已 100% 对齐，剩余 GCC 2.9 全局寄存器分配与预头外提差异，score 3110 -> 1860)
- **entry_kind**: direct (内部算法辅助函数，非虚表/Opcode/Task)
- **caller**: `sub_8048A88` (快排递归体), `sub_8048ACC` (快排主入口)
- **candidate**: `permuter/sub_8046E18/base.c` (最新最优 score = 1860)

---

## 2. 核心语义与参数建模澄清 (回答用户关键提问)

### 2.1 用户疑问：第一个参数是不是 BattleObj？
**明确结论：不是。第一个参数 `slots` 是战斗槽位索引数组 `u8 *slots`，不是 `BattleObj` 或 `BattleObj *`。**

### 2.2 证据链：
1. **指令级证据 (E1)**：
   - 目标汇编第 22-23 行：`adds r0, r7, r0; ldrb r1, [r0]`（以单字节步长加载槽位 ID）；
   - 换位代码第 37-42 行：`adds r2, r7, r1; ldrb r3, [r2]; adds r1, r7, r0; ldrb r0, [r1]; strb r0, [r2]; strb r3, [r1];`（完全是对 `u8` 元素进行 byte swap）；
   - 若第一个参数是 `BattleObj *` (`sizeof(BattleObj) == 0xC8 == 200`)，指针变址必为 `0xC8` 乘法或基址偏移，而 `r7` 上的指针加法步长恒为 1。
2. **多调用方调用图证据 (E2)**：
   - `sub_8048A88(u8 *arg0, s8 arg1, s8 arg2)`（行 1761）：`val = sub_8046E18(arg0, arg1, arg2);`；
   - `sub_8048ACC(u8 *arg0, u8 arg1, u8 arg2)`（行 1778）：传参 `sub_8046E18(arg0, 0, val);`，其中 `arg0` 为上层传入的 12 元素对象槽位列表 `u8 slots[12]`。
3. **`BattleObj` 的真正位置**：
   - 全局变量 `gStatRecalcPool`（`0x030008EC`）才是 `BattleObj` 数组池首地址；
   - 本函数通过 `slots` 数组里的槽号 `slots[i]` 索引该池：
     `&((BattleObj *)gStatRecalcPool)[slots[i]]`（即 `gStatRecalcPool + slots[i] * 0xC8`）；
   - 接着传给属性评估函数 `sub_8046F0C(BattleObj *obj)`，根据当前排序维度 `gStatRecalcKind` 取出敏捷/属性值 `key` 用于大小比较。

---

## 3. 机器契约 (Machine Contract)
- **prototype**: `u8 sub_8046E18(u8 *slots, s8 arg1, s8 arg2)`
- **args**:
  - `r0`: `u8 *slots` (待排序的槽位索引数组)
  - `r1`: `s8 arg1` (区间左边界 low)
  - `r2`: `s8 arg2` (区间右边界 high / pivot 索引)
- **return**: `(u8)b` (分区完成后的划分点索引)
- **registers in target (100% 已反解)**:
  - `r7`: `slots` (全程保持)
  - `r6`: `c` (右扫描游标，u8，递减)
  - `r5`: `(s8)b` (左游标带符号值，供 loop 2 比较)
  - `r4`: `(s8)c` (右游标带符号值，供 loop 2 计算 `c - 1`)
  - `r8`: `b << 24` (b 移位值，跨 loop 2 保持，供结尾 return 和 blt 比较)
  - `sb (r9)`: `b` (左扫描游标，s8，递增)
  - `sl (r10)`: `keyA` (基准值，即 pivot 元素属性值)
  - `[sp, #0]`: `saved = arg2 << 24` (末尾与 pivot 换位用)
  - `[sp, #4]`: `&gStatRecalcPool` (跨 loop 2 内部 `sub_8046F0C` 调用溢出保存)

---

## 4. 控制流图 (CFG)
1. **Prologue**:
   - `b = arg1 - 1; c = arg2;`
   - 计算基准 `keyA = sub_8046F0C(slots[arg2]);`
   - `saved = arg2;`
   - `goto loop1;` (跳过首轮 swap 直接进入 loop 1)
2. **Swap (`0x08046E5C`)**:
   - `swap(slots[b], slots[c])`
   - 直落 (fallthrough) 进入 `loop1`
3. **Loop 1 (`0x08046E68`)**:
   - `do { b++; } while (sub_8046F0C(slots[b]) > keyA);` (降序比较)
4. **Mid-Step**:
   - `c--;`
   - `if ((s8)b >= (s8)c) goto loop2_end;`
5. **Loop 2 (`0x08046EAC`)**:
   - `if (keyA <= sub_8046F0C(slots[(s8)c])) goto loop2_end;`
   - `c = (s8)c - 1;`
   - `if ((s8)b < (s8)c) goto loop2;`
6. **Loop 2 End (`0x08046ED8`)**:
   - `if ((s8)b < (s8)c) goto swap;`
   - `swap(slots[b], slots[saved]);`
   - `return (u8)b;`

---

## 5. 当前卡点与精确差异

当前候选 `permuter/sub_8046E18/base.c` 指令流与基本块拓扑已经 100% 还原，栈深 `#8` 完全对齐，主要卡在 GCC 2.9 的**全局寄存器分配优先律 (Register Allocation Priority)**：
1. **目标态**:
   - `r7`: `slots`, `r6`: `c`, `r5`: `(s8)b`, `r4`: `(s8)c` (4 个低寄存器全占满)
   - `r8`: `b_shift`, `sl`: `keyA`, `sb`: `b` (3 个高寄存器全占满)
   - 无任何空闲 callee-saved 寄存器，因此 `&gStatRecalcPool` 无法被外提 (hoist)，必须落栈 `[sp, #4]`。
2. **候选态**:
   - 候选目前把 `slots` 分到 `r5`，导致 `r4` 与 `sl` 发生互换；
   - 寄存器一旦出现微弱自由度，GCC 2.9 的 loop optimizer 就会试图把 `c--` 和池地址外提到 loop 1 之前（生成 8 字节 pre-header），破坏了 swap 直落 loop 1 的 0 延迟结构。

---

## 6. 后续攻坚建议
1. 保持当前控制流结构不变（已证明该结构是唯一能产生 `blt 44` 且末尾完全对齐的形态）。
2. 在 `permuter/sub_8046E18/` 目录下以当前 `base.c` (score 1860) 为种子，限制或引导寄存器分配权重；
3. 重点微调 loop 2 内部 `(s8)c` 与 `(s8)b` 的局部变量生命周期 (RTL live-range)，迫使 GCC 认为 `(s8)c` 跨调用存活必须占用 `r4`，从而把 `b_shift` 挤入 `r8`。
