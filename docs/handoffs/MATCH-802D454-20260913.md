# MATCH sub_802D454 — 2026-09-13 (analyze-agent)

## 结果
- 地址: 0x0802D454 (thumb, module event_actor)
- 状态: **已匹配** (functions.tsv status=1)
- 字节: 724 B, bytecmp 0 diff (施加 23 个绝对符号后链接 904B 全等), fncheck OK
- 全量: `make` 通过, `sha1sum -c ll.sha1` 成功, 进度 835/1059

## 机器契约
```
address: 0x0802D454
entry_kind: indirect (事件演出函数指针表, 同 sub_802CE90/sub_802D1FC 族)
callers: 间接 (event_actor 演出派发表); 直接源未在 src 出现
callees: sub_80444A4, sub_801CE80, sub_803F5B4, Sfx_Play, sub_804BF14,
         sub_804C728, sub_8044514, GetObjPool, Rng_LcgNext, __umodsi3, sub_803F658
args: r0 = BattleObj *obj (演出对象)
      r1 = BattleObj *obj2 (同池目标对象; 仅 case22 用其 headB.kindFlags)
return: r0 (u8): 0 继续 / 1 结束 (仅 case9 返回 1)
```

## 语义 (gUnk_03000820 状态机)
0 → 0x12(0x13) → 0x14 → 0x15 → 0x16 → 0x17 → 0x18 → 0x19 → 9。
- case0: 暂存 posX/posY/headA.f_1E/palSlot; sub_80444A4; sub_801CE80(obj,5,0x1B4,0xA,2);
  sub_803F5B4; 计数=0; f_B6=0x31D; f_B4=0xC; → 0x12。
- 0x12: 等 headA.kindFlags 0x800 落 → 0x13。
- 0x13: headA.frameIdx>0x27 → Sfx(0x9A) + sub_804BF14(0,3,0x10,0x1C,0x1F,4,4,-1,2); 计数=0 → 0x14。
- 0x14: 计数<=3 → ++; 否则 sub_804C728(0,3,0x10) → 0x15。
- 0x15: headA.frameIdx>0x57 → kindFlags|=0x100; 计数=0; sub_8044514(0x32) → 0x16。
- 0x16: 等 obj2->headB.kindFlags 0x800 落 → Sfx(0x69) → 0x17。
- 0x17: gUnk_03000844/45/56 全 0 → headA.kindFlags&=0xFEFF → 0x18。
- 0x18: headA.kindFlags 0x1000 → sub_801CE80 复位 → 0x19。
- 0x19: 等 headA.kindFlags 0x800 落 → 9。
- 9: GetObjPool(); 遍历 i<gUnk_0300083D: 若 (gUnk_03000840[i]&0xF0)==0x10 且
  ((u32(*)(void))Rng_LcgNext)()%0x64 <= 0x27 → gUnk_03004F90[gUnk_03000840[i]&0xF]=2;
  result=1。
- 尾: sub_803F658(obj)。

## 关键取证点
1. 参1 (r1→r2) 只用于 case22 的 `obj2->headB.kindFlags & 0x800`;
   headB 在 BattleObj+0x3C, kindFlags 在 ObjHead+0x18 → obj2+0x54, 与 ASM 一致。
2. case9 的 `%0x64` 必须写 `((u32 (*)(void))Rng_LcgNext)()` (同 cutscene_mgr.c:31 /
   battle_rewards.c:37 的既有已匹配写法), 否则 u16 返回类型会多出
   `lsls/lsrs #0x10` 截断 (2 处), 字节不符。
3. GetObjPool() 返回值被丢弃 (ASM `bl GetObjPool` 后 r0 未用, 循环改用 gUnk_03000840),
   但调用保留以复现字节。
4. 本函数无 `sub_80187E8()` 前导 (与 sub_802CE90/sub_80287EC 不同), 已核对 ASM。

## 影响面
- include/code_0.h:721: `void sub_802D454();` → `u8 sub_802D454(BattleObj *, BattleObj *);`
  (原 K&R 声明与真调用约定冲突, 候选编译报 conflicting types; 已更新原型)
- 未改动共享 iwram.h / linker.ld / 其它函数区段。

## 锁定状态
- scripts/claim.sh sub_802D454 → 完成后释放 (analyze-agent)。
