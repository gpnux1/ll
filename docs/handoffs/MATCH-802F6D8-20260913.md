# MATCH-802F6D8 — 战斗对象"换装/形态切换"演出状态机

## 结论
- **address**: `0x0802F6D8` (345 asm 行, `.text` 788 字节)
- **current_name**: `sub_802F6D8` (status=1, byte 匹配)
- **physical_tu**: `src/event_actor.c`
- **match_status**: 1 — `fncheck.py` OK (788B, 29 池重定位已施加, 16 bl 槽忽略); `make` + `sha1sum -c ll.sha1` 全绿 (844/1059)
- **entry_kind**: 单参 handler (gUnk_03000820 状态机), 家族表 handler
- **推理依据**: E1 (ASM) + E2 (与 `sub_802E6C8`/`sub_802BD94` 同族)

## 机器契约
- **args**: `r0 = BattleObj *obj`
- **return**: `r0` = status (0 继续 / 1 完成); 尾部 `adds r0, r7, #0`
- **callees**: `sub_80187E8`, `sub_80444A4`, `sub_803F5B4`, `sub_801CE80`,
  `sub_804B96C(9参)`, `sub_804C4D8`, `sub_801B954`, `sub_801768C`,
  `sub_8044514`, `Sfx_Play`, `Sfx_TrackBusy`, `sub_803F658`
- **memory_reads**: `obj->headA.kindFlags(+0x24)`, `obj->headA.frameIdx(+0x28)`,
  `obj->headA.f_1E(+0x2A)`, `obj->headA.palSlot(+0x35)`, `obj->posX(+0xBF)`, `obj->posY(+0xC0)`
- **memory_writes**: `obj->headA.kindFlags`, `obj->posX`, `obj->posY`, `obj->pad_C1(+0xC1)`,
  `obj->f_B4/f_B6`, `gUnk_03000820/22/24/25/28/29`

## 状态机 (gUnk_03000820)
`0 → 0x12 → 0x13 → 0x14 → 0x15 → 0x16 → 0x17 → 0x18 → 0x19 → 9`

### case 语义要点
- **case0**: 暂存 posX/posY/f_1E/palSlot, 计数=0, `sub_80444A4`, `sub_803F5B4`,
  `sub_801CE80(obj,1,0x1B4,0xD,0)`; `f_B6=0x339`, `f_B4=0` → 0x12。
- **0x12**: headA 的 0x800 落 → `Sfx_Play(0x59,0,0)` → 0x13。
- **0x13**: headA.frameIdx > 0x21 → headA.kindFlags |= 0x100 +
  `sub_804B96C(palSlot, (u8)sub_801B954(&headA), 0x10, 0x1C, 0x1F, 4, 4, -1, 2)` → 0x14。
- **0x14**: 计数<=3 → 计数++; 否则 `sub_804C4D8(palSlot, (u8)sub_801B954(&headA), 0x10)`,
  计数=0 → 0x15 + `Sfx_Play(0x64,1,0)`。
- **0x15**: 计数<=0xF: `posX = sub_801768C(g8628, 0xF0-g8628, 0x10, 计数, 2)`, 计数++;
  否则 计数=0, `pad_C1 = posY`, `posX = g8628`, `sub_8044514(0x28)` → 0x16。
- **0x16**: 无其他演出占用 → `sub_801CE80` 复位 + headA.kindFlags |= 0x100 → 0x17。
- **0x17**: headA 的 0x800 落 → headA.kindFlags &= 0xFEFF → 0x18。
- **0x18**: `Sfx_TrackBusy(1)==0` → 0x19。
- **0x19**: 计数<=0x1D: `pad_C1 = sub_801768C(posY, -posY, 0x1E, 计数, 2)`, 计数++;
  否则 计数=0 → 9。
- **case26/27**: 空。
- **case9**: result=1。
- **尾部**: `sub_803F658(obj); return result;`

## 关键 GCC2.9 形状
- **case26/27 必须显式声明为空 case**: 目标 switch 上界为 0x1B (`cmp r0,#27`), jump table 28 项。
  源码省略 26/27 会让上界收到 0x19、jump table 少 2 项 (整段 +4B 偏移/表错位)。
- **case21 与 case25 共享 `strb + 计数++` 尾块**: 两 case 的赋值都紧接 `计数 += 1`,
  GCC 跨跳合并到 `_0802F9B8`。
- **case20 的 `if (计数<=3){计数++;break}` 正极性**让 `0x14` 与其它计数++ 合并。
- `sub_804B96C` 第二参、`sub_804C4D8` 第二参均需 `(u8)sub_801B954(&obj->headA)` 显式截断。
- `Sfx_TrackBusy` 返回 `u8`，`== 0` 判定。

## 验证
```
scripts/bytecmp.sh sub_802F6D8 permuter/sub_802F6D8/base.c $(cat permuter/sub_802F6D8/sym.ld)
# => DIFF: 64 bytes / 888; 归一化 bl 遮罩后 = 0 差 (16 bl 槽)
python3 scripts/fncheck.py sub_802F6D8
# => OK (788 bytes @0x0802f6d8, 29 池重定位已施加, 16 bl 槽忽略)
make && sha1sum -c ll.sha1   # => ll.gba: 成功
```

## 共享文件改动
- `include/code_0.h`: `void sub_802F6D8();` → `u8 sub_802F6D8(BattleObj *);` + 注释
- `src/event_actor.c`: 替换 `INCLUDE_ASM` 为 C 实现 + 状态机文档注释

## 锁
- claim by `oc_12615`; 完成, 待释放。
