# Handoff: sub_804B080 (0x0804B080, 186 行 / 364 字节) — 战斗对象呼吸/浮动光效 OAM 精灵装配

日期: 2026-09-14 | agent: agent_sub_804B080 | 状态: ✅ 100% 字节完全匹配 (status=1)

## 1. 机器契约 (已验证事实)

```text
address:         0x0804B080
current_name:    sub_804B080
physical_tu:     src/battle_anim.c
match_status:    1 (OK 364 bytes @0x0804b080, fncheck OK, 全量 make+SHA1 绿, 进度 879/1059)
entry_kind:      direct (src/sio_link.c:490 sub_80184A8 第二趟 OAM 扫描唯一 C 调用点)
callers:         sub_80184A8 (战斗任务队列流水线刷新尾部)
callees:         sub_8045F10 (对象状态判定，返回 2 时放行)
indirect_tables: 跳转表 6-case (0x0804B124: 1->0x180, 2/4/def->0x174, 3->0x184, 5->0x178, 6->0x17C)
args:            r0 = BattleObj *obj (战斗对象指针)
                 r1 = u8 index (递减分配的 OAM 表项槽下标)
                 r2 = u16 flags (战斗控制标志字, 传入 gGstate324)
return:          r0 = u8 (分配后剩余的 index, 满足条件则为 index - 1, 否则为原 index)
memory_reads:    [obj + 0xB0] u16 (obj->state, 检查 bit2 及 bit9 0x200)
                 [obj + 0xA8] u8  (obj->pad_A4[4], 呼吸动画计数器)
                 [obj + 0xAB] u8  (obj->variantClass, 变体类别 1..6)
                 [obj + 0xC0] u8  (obj->posY, 屏幕 Y 坐标)
                 [obj + 0xBF] u8  (obj->posX, 屏幕 X 坐标)
memory_writes:   [obj + 0xA8] u8  (计数递增/递减回写)
                 [obj + 0xB0] u16 (state bit9 0x200 置位/清除)
                 gOamBuffer[index] (8 字节完整 OAM 表项写入)
persistent_state: gOamBuffer (0x030035C0)
script_relation: 无直接脚本字节驱动，由战斗流水线按状态机逐帧驱动
```

## 2. 语义与业务逻辑 (E2 级证据)

为战斗对象装配**一条 16×16 OBJ 呼吸/浮动动画精灵**写入 `gOamBuffer[index]`，并返回 `index - 1`。

- **三联守卫**:
  `flags & 0x100` 置位、`!(obj->state & 4)` 且 `sub_8045F10(obj, 0x6E) == 2` 时才触发。任一条件不满足则直接返回原 `index`。
- **呼吸三角波计数器**:
  - 若 `obj->state & 0x200` 置位：递增 `obj->pad_A4[4]`，当计数值大于 0xF 时清除 `0x200`（波峰转向）；
  - 否则：递减 `obj->pad_A4[4]`，当计数值归 0 时置位 `0x200`（波谷转向）。
  - 位移步进：`sh = obj->pad_A4[4] >> 2`（0~3 像素步进）。
- **变体图块查表 (6-case 跳转表)**:
  - 1 -> 0x180
  - 3 -> 0x184
  - 5 -> 0x178
  - 6 -> 0x17C
  - 2, 4, default -> 0x174
- **OAM 属性写入 (`GameOamData.fields` 位域视图)**:
  - `VPos = obj->posY - (u8)(sh + 0x22)`
  - `AffineMode = 0`, `ObjMode = 0`, `Mosaic = 0`, `ColorMode = 0`, `Shape = 0` (8 位直接清零)
  - `HPos = obj->posX + 4`
  - `AffineParamNo_L = 0`, `HFlip = 0`, `VFlip = 0`, `Size = 1` (16×16 尺寸)
  - `CharNo = tile`
  - `Priority = 1`, `Pltt = 0xF` (调色板 15)
  - `index--` 递减。

## 3. 破局关键与 GCC2.9 匹配机制

前任分析（2026-09-11）卡在 permuter score 2350，误以为是纯寄存器着色互换不可破。本轮重构通过底层 RTL/Reload 分析彻底破局：

1. **单大 `if` 结构统一汇聚点**:
   将守卫写为：
   `if ((flags & 0x100) != 0 && !(obj->state & 4) && ((s32 (*)(BattleObj *, u16))sub_8045F10)(obj, 0x6e) == 2) { ... index--; } return index;`
   让整个函数只有一个统一返回点 `return index;`，从而生成前导三处不满足时直接 `b _0804B1D0` 的标准跳转流，完美收敛末尾单点 `mov r0, sb`。
2. **结构体直接字段访问消除多余临时指针**:
   计数器与标志字直接通过 `obj->pad_A4[4]++` / `obj->pad_A4[4]--` 访问，GCC 自动利用公共子表达式生成 `adds r1, r7, #0; adds r1, #0xa8` 和 `adds r2, r1, #0`，消除了一切手写临时指针变量造成的寄存器竞争。
3. **位域结构体赋值与掩码常量自动提取**:
   OAM 采用 `&gOamBuffer[index]` 结合 `GameOamData.fields` 逐字段赋值：
   GCC2.9 自动将位清零掩码 `~13`, `~17`, `~33`, `0x3F` 提取至块头部物化并放入 `r4`, `ip`, `r5`, `r8`。前任漏写 `AffineParamNo_L = 0` 导致掩码少提取一组，是寄存器错位的根本诱因之一。
4. **`&gOamBuffer[index]` 替换强转字面量**:
   使用已登记的 `gOamBuffer` 符号（`symbol_ref`）使全局分配器和 reload pass 正确识别基址变量生命周期，消除 `last_spill_reg` round-robin 偏斜，使得 `r0/r1` 寄存器着色 100% 吻合目标 ROM。
5. **`VPos` 减法表达式写成 `(u8)(sh + 0x22)`**:
   显式写 `obj->posY - (u8)(sh + 0x22)` 阻断常数折叠，生成 `adds r1, r2, #0; adds r1, #0x22; subs r0, r0, r1` 完整时序。

## 4. 验证结果

- `fncheck`: `sub_804B080 OK (364 bytes @0x0804b080, 7 池重定位已施加, 1 bl 槽忽略)`
- 全量构建: `make && sha1sum -c ll.sha1` -> `ll.gba: 成功`
- 进度推进: 879/1059 (83.0%)
