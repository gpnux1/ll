# CLEAN-804DABC 可读性清理 (2026-09-15)

> package: CLEAN-804DABC · agent: qwen-opencode · 类型: **已匹配函数的强转清理（字节不变）**
> 地址 0x0804DABC · physical_tu `src/battle_rewards.c:457` · 前置 status=1（已匹配）→ 保持 1。

## 背景
用户要求清理 `sub_804DABC` 里的“大量强转”。该函数此前已被批量 `u8*→BattleObj*` 参数升级与 `ObjAnimIdxBlk` 改名部分处理，但函数体内仍残留全仓**唯一**一处裸偏移强转写法。同族 8 个姊妹函数（`sub_804D1B4/DB64/DC24/...`，见 battle_rewards.c:222/278/355/406/442/520/564/602）早已用结构体字段访问。

## 改动（E2：以姊妹函数 + BattleObj 字段偏移为据）
| 旧（凑字节裸偏移 cast） | 新（结构体字段） | 偏移 |
|---|---|---|
| `((u8 *)obj)[0xBC] = 1/0` | `obj->fxKind = 1/0` | 0xBC=fxKind |
| `((u8 *)obj)[0xC2] = v` / `= 0` | `obj->animSubIdx = v` / `= 0` | 0xC2=animSubIdx |
| `((u8 *)obj)[0xBD] = value/0` | `obj->f_BD = value/0` | 0xBD=f_BD |
| `((ObjAnimIdxBlk *)(*(u32 *)((u8 *)obj + 0x88)))->subIdx[((u8 *)obj)[0xC2]]` | `((ObjAnimIdxBlk *)(obj->animPtr))->subIdx[obj->animSubIdx]` | 0x88=animPtr；与 8 姊妹同形 |

`animPtr` 是 `u8 *` 字段（+0x88），`(u32 *)((u8*)obj+0x88)` 解引用 == 读该指针，故 `obj->animPtr` 等价；`subIdx[]` 保持结构体数组视图（避免 +8 被折进 `ldrh` 位移，见 functions.tsv 旧注）。

## 保留的“故意强转”（移除会破字节，非可读性问题）
- `((u32 (*)(void))Rng_LcgNext)()`：functions.tsv/sub_8044F4C 记录——需此原型强转避免编译器插零扩展。
- `(u32)(u8)Rng_LcgNext() % count`：`values[]` 索引的截断/提升形状。
- `((ObjAnimIdxBlk *)(obj->animPtr))`：结构化类型视图，非裸偏移 cast，语义正名且全族一致。

## 验证
- `python3 scripts/fncheck.py sub_804DABC` → **OK, 166 bytes @0x0804dabc**（改前后均 OK，字节逐字节不变）。
- 未改任何控制流/宽度/ROM 冗余 store（条件写 fxKind 后恒写 1、`animSubIdx==1` 覆 0 均按 ROM 保留）。

## 边界/锁
- 仅改 `sub_804DABC` 函数区段 + `functions.tsv` 该行地址独占；未 `git commit`/`make`（改动为单函数、fncheck 已确认字节不变，全量 SHA1 不受影响）。
- 陈旧锁 `.claims/sub_804DABC.lock`（agent=flash150, 挂起, 2026-09-05, 其 note 描述的是早已被解决的 4B-diff 早期状态）我按 §2.4 **未释放**；因函数已 status=1 且用户显式指派本次保字节清理，特此留痕提醒 flash150/协调者。
