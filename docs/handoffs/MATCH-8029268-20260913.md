# MATCH-8029268-20260913

## 目标
- address: 0x08029268
- current_name: sub_8029268
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 680B)
- entry_kind: task-table (handler 指针表 0x0839CD5C, idx 12 = 0x08029269, sub_803F444 间接调用)
- args: r0 = BattleObj *obj, r1 = BattleObj *arg1 (同池目标对象)
- return: r0 = u8 result (0 继续 / 1 动作完成)

## 机器契约
- callees: sub_80444A4(BattleObj*), sub_803F5B4(BattleObj*),
  sub_801CE80(BattleObj*,u8,u16,u8,u8), sub_8020CC4(void*,u8,u8,u16,u8,u16,u16),
  Sfx_Play(u16,u8,u8), sub_801768C(s16,s16,s16,s16,u8), sub_801EC3C(BattleObj*,u8),
  sub_8044514(s16), sub_8020974(ObjHead*,u16,u16,u8,u16), sub_80207DC(BattleObj*,u8,u8,u16,u8),
  sub_803F658(BattleObj*)
- memory_reads:
  - obj+0x35 headA.palSlot, obj+0x2A headA.f_1E, obj+0xBF posX, obj+0xC0 posY,
    obj+0x24 headA.kindFlags
  - arg1+0xBF posX, arg1+0xC0 posY
  - gUnk_03000820/0822/0824/0825/0828/0829, gUnk_03000844/45/56
- memory_writes:
  - arg1+0xB2 dmgAmount = 0
  - obj+0x24 headA.kindFlags (清 0x1000 置 0x100)
  - obj+0x67 headB.f_2B / obj+0x68 headB.f_2C (插值)
  - obj+0xB0 state &= 0xDFFF
  - gUnk_03000820 状态, gUnk_03000825 计数, gUnk_03000828/29/22/24 暂存

## 语义 (状态机)
- 0: 暂存 palSlot/f_1E/posX/posY → 1 (不清伤害、不切动画)。
- 1: arg1->dmgAmount = 0; sub_80444A4 清同组伤害; sub_803F5B4;
     sub_801CE80(obj,1,0x1B4,0xD,0); 计数=0 → 2。
- 2: headA.kindFlags & 0x800 落 → Sfx_Play(0x31,1,0) → 5。
- 5: headA.kindFlags 到 0x1000 → 清 0x1000 置 0x100;
     sub_8020CC4(obj, posX, (u8)(posY-0x10), 0x1F4, 0xE, 0x8D, 0x25);
     计数=0; Sfx_Play(0x34,1,0) → 3。
- 3: 计数<=9 → headB.f_2B/f_2C = sub_801768C(...) 向 arg1 插值
     (X 目标 = arg1->posX + ((u8)sub_801EC3C(arg1,0)>>1);
      Y 基准 = obj->posY-0x10, 目标 = arg1->posY - ((u8)sub_801EC3C(arg1,1)>>1));
     计数++。否则 计数=0, sub_8044514(0x14), obj->state &= 0xDFFF → 6。
- 6: 无其他演出占用 → sub_8020974(&headA,0x8C,0x1B4,0xD,0x22) → 8。
- 8: → 7。
- 7: headA.kindFlags 到 0x1000 → sub_80207DC(obj, 0828,0829,0822,0824) → 9。
- 9: headA.kindFlags & 0x800 落 → result = 1。
- 尾部: sub_803F658(obj) (无帧计数自增)。

## GCC2.9 形状要点 (字节匹配必需)
- case5 必须拆成两条语句:
  `keys = obj->headA.kindFlags & 0xEFFF; zero = 0; obj->headA.kindFlags = keys;`
  `obj->headA.kindFlags = obj->headA.kindFlags | 0x100;`
  合并成 `(kindFlags & 0xEFFF) | 0x100` 会让 `movs r4,#0` 排到 0x100 掩码之后, 差 1 指令。
- switch 源顺序必须 `case 8` 在 `case 7` 之前, 才得到目标基本块布局
  (target case7=0x242 / case8=0x23c)。
- case3 的 if 极性写成 `if (计数 <= 9) { ... } else { ... }` (插值做 fallthrough)。
- `sub_801EC3C` 返回值必须 `(u8)` 截断再 `>> 1`。
- gUnk_03000822 为 u16。

## 验证
- python3 scripts/fncheck.py sub_8029268: OK (680 bytes @0x08029268, 32 池重定位已施加, 14 bl 槽忽略)。

## 改动文件
- src/event_actor.c: 0x08029268 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 原型 void sub_8029268() → u8 sub_8029268(BattleObj *, BattleObj *)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_8029268/base.c: 候选。
