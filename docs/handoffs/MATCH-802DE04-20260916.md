# Handoff: MATCH-802DE04 (2026-09-16, opencode-1) — 吸血/回复演出状态机 已匹配

## 结果

- `sub_802DE04` (0x0802DE04, 472B): **status 0→1**。bytecmp 32B = 8×4 bl 基线, **0 真差**;
  fncheck OK 472B (25→38 池重定位); `make clean && make -j4 && sha1sum -c ll.sha1` 全绿; 进度 901/1059。
- 合入位置: `src/battle_stage_actor.c` (原 INCLUDE_ASM 占位处)。

## 语义 (E1+E2 验证, 与 include/battle_stage_actor.h 旧注释一致处已复核)

0x0839CD5C 演出指针表 idx 0x28 (thumb 0x0802DE05) 的唯一 handler, 由 `sub_803F444` 分派器
(slot 0xB..0x70 对象) 单参调用。`gObjActStep`: 0 → 0x12 → 0x14/0x15 → 9, case9 **无条件返回 2**
(区别于同族其他演出的"等 gActWaitBusy* 全 0 返回 1" — 2 = 吸血已入账, 消费方 sub_803F444
以 u8 返回值继续分派)。case 0x12 在 headA.kindFlags&0x800 落下后**直跳 0x14**, 0x13 分支
(frameIdx>0x66 → sub_8020CC4(obj,0x78,0x50,0,0xE,0x32A,4)) 为 ROM 保留死路径, 照抄不删。
case 0x14: `hp += sub_8048D64(obj, maxHp>>1)` — sub_8048D64 = min(maxHp-hp, arg), 即"至多回一半、
按缺口夹取"的吸血入账; 随后 sub_801CE80(obj,0,gObjActSavedF2A,gObjActSavedPal,0) 复位动画头。

## 破墙机制 (推翻 BLOCKED-802DE04-20260913 的"RTL 级不可解"结论)

5 字节 = case20 的 HP 地址/值伪寄存器 home 互换。根因不在本函数源码形状, 而在
**`sub_8048D64` 的共享原型 u16 与原始调用 TU 声明 (宽返回) 不符**:
ROM bl 后 r0 直入 `adds r1,r1,r0`, 无 HI 伪拷贝 → egcs 只有 u32 返回原型才生成。

处理 (用户规则: 禁止 fnptr 强转):
1. `include/battle_flow_rules.h`: `u16→u32 sub_8048D64(BattleObj*, u16)`。
2. `src/battle_flow_rules.c` 体内 `return diff;` → `return (u16)diff;` 保住 ROM 的
   `lsls/lsrs #16` 截断指令, sub_8048D64 自身 fncheck OK 30B 不变。
3. 全仓 grep: sub_8048D64 **无其他调用者**, 零波及。
4. 本函数直调 `sub_8048D64(obj, obj->maxHp >> 1)` → bytecmp 0 真差。

## 同日附带清理 (用户指令)

`Rng_LcgNext` (原型已是 u32, 体内自掩码) 的 52 处历史 `((u32/int/s32 (*)(void))Rng_LcgNext)()`
函数指针强转全部改写: u32-cast→直调; int/s32-cast→`(int)`/`(s32)` 值 cast (ROM `__modsi3`
证据, 见 BattleDrops_Roll/sub_804C8E0); **每处单独 make clean && make -j4 && sha1sum -c 全绿**。
battle_object_engine.c 的 10 处 `(u8)Rng_LcgNext()` 与 special_targets 直调本就合规, 未动。
驱动脚本 `.scratch/opencode-de04/site_edit.sh` (⚠ 该目录被 gitignore, 复现时用
"字面量 python 替换", 勿再引入 BRE 转义 — 首版 sed 转义把 BRE 的 `(`/`)` 转成分组元字符,
导致 52 次假 SITE-OK 零替换)。

## 教训沉淀

docs/EXPERIENCE.md **262**: "调用点无截断"指纹 → 升级共享原型 + 体内显式值截断; 禁 fnptr 强转。

## 锁

`sub_802DE04` 认领 (opencode-1) 已释放。
