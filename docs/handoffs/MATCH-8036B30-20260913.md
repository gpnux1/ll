# MATCH sub_8036B30 — 2026-09-13 (analyze-agent)

## 结果
- 地址: 0x08036B30 (thumb, module event_hub)
- 状态: **已匹配** (functions.tsv status=1)
- 字节: 884 B, bytecmp 0 diff (施加 26 个绝对符号后链接 1176B 全等), fncheck OK
- 全量: `make` 通过, `sha1sum -c ll.sha1` 成功, 进度 860/1059 (81.2%)

## 机器契约
```
address: 0x08036B30
entry_kind: indirect (演出/对话函数指针表, 同 sub_802B608/sub_803272C 族)
callees: sub_80444A4, sub_803F5B4, sub_8020974, sub_801B954, sub_804C3A4,
         Sfx_Play, sub_801A348, sub_8019B98, sub_801A2AC, sub_804BDD8,
         sub_8044514, Sfx_TrackBusy, Sfx_StopTrack, sub_804BE90, sub_801CBA4,
         sub_803F658, sub_8045B90, sub_801768C
args: r0 = BattleObj *obj
return: r0 (u32): 0 继续 / 1 结束 (case9 返回 1)
```

## 语义 (gUnk_03000820 状态机)
0 → 1 → 2 → 5 → 0x12 → 0x13 → 0x14 → 0x15 → 0x16 → 0x1A → 0x1B → 9。
- case0: 存 headA.f_1E/palSlot; sub_80444A4; sub_803F5B4; f_B6=2, f_B4=0 → 1。
- case1: sub_8020974(&headA, 0x3A0, 0x1B4, 0xD, 2) → 2。
- case2: headA 0x800 落 → Sfx(0xA5) → 5。
- case5: headA 0x1000 → sub_804C3A4(palSlot,(u8)sub_801B954(&headA)); headA.kindFlags&=0xEFFF;
  sub_8020974(&headA, 0x3A1, 0x1B4, 0xD, 2) → 0x12。
- 0x12: headA 0x800 落 → sub_801A348 → 0x13。
- 0x13: sub_8019B98(0x10,3,0xE,1)!=0 → Sfx(0x50); gUnk_03000867=0;
  sub_801A2AC(0x1C42,0,0x10); gUnk_03000825=0; sub_804BDD8(0xE,1,1,1,0xF);
  sub_8044514(0x46) → 0x14。
- 0x14: 计数<=0x13 → gUnk_03000867=sub_801768C(0,0xC,0x14,计数,2); sub_801A2AC;
  计数++; 否则 计数=0, gUnk_03000867=0xC, sub_801A2AC(0x1C42,0xC,0x10) → 0x15。
- 0x15: Sfx_TrackBusy(1)==0 → 计数=0 → 0x16。
- 0x16: 计数<=0x13 → gUnk_03000867=sub_801768C(0xC,-0xC,0x14,计数,2); sub_801A2AC;
  计数++; 否则 计数=0; Sfx_StopTrack(1); sub_801A2AC(0,0,0); `obj->state &= 0x2000`;
  sub_804BE90(0xE,1); `*(volatile u16*)(0x80<<0x13) &= 0xFDFF` → 0x1A。
- 0x1A: 无其他演出占用 → sub_801CBA4(obj,0,f_1E,palSlot,0) → 0x1B; 否则 计数++。
- 0x1B: headA 0x800 落 → 9。
- 9: sub_8045B90(obj, pad_A1); result=1。
- 尾: sub_803F658(obj)。

## 关键 GCC2.9 形状点
1. case19 与 case21 必须用 **两个独立载体变量** `zero` / `busy`。若共用同一变量,
   case21 的 `Sfx_TrackBusy` 结果会落到 callee-saved r4 (目标为 caller-saved r1), 差 3B。
2. case19 的 `zero = 0` 必须写成 **第二实参内嵌赋值** `sub_801A2AC(0x1C42, zero = 0, 0x10);`,
   再 `gUnk_03000825 = zero;`。写成独立 `zero = 0;` 语句会被 GCC 提到 call 前, 体量/调度不符。
3. case20/22 的 if-branch 被 GCC 交叉跳转 (cross-jump) 共享同一尾块
   `sub_801768C(..,0x14,..); sub_801A2AC(0x1C42,..,0x10); gUnk_03000825++;` — 两 case 写法须一致。
4. case22 else 的 state 操作为 `obj->state = obj->state & 0x2000` (非 &=0xDFFF)。
5. `sub_8019B98` 返回 u8 (调用点 lsls/lsrs #0x18)。

## 影响面
- include/code_0.h:780: `void sub_8036B30();` → `u32 sub_8036B30(BattleObj *);`
- 未改动 linker.ld / iwram.h / 其它函数区段。

## 锁定状态
- 已释放 (analyze-agent)。
