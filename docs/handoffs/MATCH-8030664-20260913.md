# MATCH-8030664 — 战斗对象"双头换装/形态切换"演出状态机

## 结论
- **address**: `0x08030664` (371 asm 行, `.text` 844 字节)
- **current_name**: `sub_8030664` (status=1, byte 匹配)
- **physical_tu**: `src/event_actor.c`
- **match_status**: 1 — `fncheck.py` OK (844B, 32 池重定位已施加, 11 bl 槽忽略); `make` + `sha1sum -c ll.sha1` 全绿 (850/1059)
- **entry_kind**: 函数指针表 `0x0839CE30` (thumb = 0x08030665)
- **推理依据**: E0 (指针表命中) + E1 (ASM) + E2 (与 `sub_802C9E8`/`sub_802E6C8` 同族)

## 机器契约
- **args**: `r0 = BattleObj *obj`
- **return**: `r0` = status (0 继续 / 1 完成); 尾部 `adds r0, r6, #0`
- **callees**: `sub_80187E8`, `sub_80444A4`, `sub_803F5B4`, `sub_801CE80`,
  `sub_8020CC4`, `sub_801768C`, `sub_8044514`, `__modsi3`, `sub_803F658`
- **memory_reads**: `obj->headA.kindFlags(+0x24)`, `obj->headA.frameIdx(+0x28)`,
  `obj->headA.f_1E(+0x2A)`, `obj->headA.palSlot(+0x35)`, `obj->headB.kindFlags(+0x54)`,
  `obj->headB.frameIdx(+0x58)`, `obj->posX(+0xBF)`, `obj->posY(+0xC0)`
- **memory_writes**: `obj->headA/headB.kindFlags`, `obj->headB.frameIdx/f_2B`,
  `obj->state(+0xB0)`, `obj->f_B4/f_B6`, `gUnk_03000820/22/24/25/28/29/6E/6F`

## 状态机 (gUnk_03000820)
`0 → 0x12 → 0x13 → 0x14 → 0x15 → 0x16 → 0x17 → 0x18 → 0x19 → 9`

### case 语义要点
- **case0**: 暂存 posX/posY/f_1E/palSlot, 计数=0, `sub_80444A4`, `sub_803F5B4`;
  `f_B6=0`, `f_B4=0`, 计数=0 → 0x12。
- **0x12**: `sub_801CE80(obj,1,0x1B4,0xD,0)` → 0x13。
- **0x13**: headA 的 0x800 落 → 0x14。
- **0x14**: headA.frameIdx > 7 → `g86E=posX+4`, `g86F=posY+4`,
  `sub_8020CC4(obj,g86E,g86F,0x27C,0xE,0x334,5)`; 两头 kindFlags |= 0x100 → 0x15。
- **0x15**: headB 的 0x800 落 → 两头 kindFlags &= 0xFEFF → 0x16。
- **0x16**: `headB.frameIdx = (headB.frameIdx+1) % 0x1F`; headA.frameIdx > 0x2E →
  计数=0, headB.frameIdx=0x28, headB.kindFlags |= 0x100 → 0x17。
- **0x17**: 计数<=0x18: 计数==0xF → `sub_8044514(0x1E)`;
  `headB.f_2B = sub_801768C(g86E, 0xFA-g86E, 0x19, 计数, 2)`, 计数++;
  否则 `state &= 0xDFFF` → 0x18; 之后 headA 到 0x1000 → 清位并置 0x100。
- **0x18**: headA 的 0x100 落 → `sub_801CE80` 复位 → 0x19;
  否则 headA 到 0x1000 → `headA.kindFlags &= 0xEFFF` + `sub_801CE80` 复位 → 9。
- **case9**: 无其他演出占用 → result=1。
- **尾部**: `sub_803F658(obj); return result;`

## 关键 GCC2.9 形状
- **case25-30 必须显式声明为空 case**: 目标 switch 上界 0x1E (`cmp r0,#30`), jump table 31 项。
- case0 中 `gUnk_03000825 = 0` 出现两次 (ASM 里 `strb r0,[r4]` 在 sub_803F5B4 前后各一次),
  源码需保留两次赋值才能对齐。
- case24 用 `if / else if` 结构: 第一个 `if (headA.kindFlags & 0x100)` 体尾部 `b end`,
  第二块是 `else if`, 二者互斥 (ASM 分支证实)。
- case23 的 kindFlags 检查在 if/else 之后的共享尾块内执行 (两路都到达 `_080308DA`)。
- case23 的 `obj->headB.f_2B` 是 f_2B (+0x67)。

## 验证
```
scripts/bytecmp.sh sub_8030664 permuter/sub_8030664/base.c $(cat permuter/sub_8030664/sym.ld)
# => DIFF: 44 bytes / 920; 归一化 bl 遮罩后 = 0 差 (11 bl 槽)
python3 scripts/fncheck.py sub_8030664
# => OK (844 bytes @0x08030664, 32 池重定位已施加, 11 bl 槽忽略)
make && sha1sum -c ll.sha1   # => ll.gba: 成功
```

## 共享文件改动
- `include/code_0.h`: `void sub_8030664();` → `u8 sub_8030664(BattleObj *);` + 注释
- `src/event_actor.c`: 替换 `INCLUDE_ASM` 为 C 实现 + 状态机文档注释

## 锁
- claim by `oc_12615`; 完成, 待释放。
