# STRUCT-CALLEES-802761C — 被调函数同步升级为 `BattleObj *`（完成，全量 SHA1 绿）

- 包 ID：`STRUCT-CALLEES-802761C-20260913`
- 触发：承接 `STRUCT-802761C-BATTLEOBJ-20260913`（`sub_802761C` 的 `obj` 确认为 `BattleObj *` 并落地）
- 状态：**已完成**。4 个被调函数的第 1 实参类型确定为 `BattleObj *`，定义/原型已改，全部调用点加显式 cast。
- 验证：`fncheck` 全部 OK；`make clean && make` + `sha1sum -c ll.sha1` 绿（812/1059，76.7%）
- 日期：2026-09-13

---

## 1. 结论

`sub_802761C` 调用的 4 个函数，其接收的 `obj` **全部是 `BattleObj *`**。已按仓库既有惯例（`event_hub.c` 里已有 22 处 `(BattleObj *)arg` 显式 cast）统一：

| 函数 | 旧参型 | 新参型 | 判定证据（E1，函数体） |
|---|---|---|---|
| `sub_80444A4` (0x080444A4) | `u8 *arg0` | `BattleObj *` | 内部 `sub_80462E4(arg0, …)` 读 **`arg0+0xBD` = `f_BD`**；`0xBD` 是 BattleObj 的目标选取字段 |
| `sub_803F5B4` (0x0803F5B4) | `u8 *obj` | `BattleObj *` | 直接转交 **`sub_8020DF0(obj)`（原型已是 `void sub_8020DF0(BattleObj *)`）**；循环写池 `entry+0xB2` = `dmgAmount` |
| `sub_803F658` (0x0803F658) | 无参 K&R | `BattleObj *` | 读 **`obj+0xB4` / `obj+0xB6` = `f_B4`/`f_B6`**，并转交 `sub_801DB3C`（BattleObj 系） |
| `sub_80462E4` (0x080462E4) | 无参 K&R | `BattleObj *, u8 *, u16` | 体内 **`arg0+0xBD` = `f_BD` 分组**；第3参 `lsls#16/lsrs#16` → `u16` |

**这 4 个函数本身都不是新匹配**（`sub_80444A4`/`sub_803F5B4`/`sub_80462E4` 已 status=1，`sub_803F658` 仍 status=0 的 `INCLUDE_ASM`）——本次只做类型/原型升级，字节零变化。

---

## 2. 改了什么

### 定义
```c
/* src/obj_state.c:101  */ void sub_80444A4(BattleObj *arg0)
/* src/event_hub.c:1919 */ void sub_803F5B4(BattleObj *obj)
```

### 原型（include/code_0.h）
```c
void sub_803F5B4(BattleObj *);
void sub_803F658(BattleObj *);
void sub_80444A4(BattleObj *);
u32  sub_80462E4(BattleObj *, u8 *, u16);
```

### 调用点加显式 cast（共 45 处）
- `src/event_actor.c`：10 处（`sub_802761C` 自身 `(u8*)obj` → 直接传 `obj`；`sub_802DFDC`/`sub_802F480`/`sub_80309B0` 的 `arg` → `(BattleObj *)arg`）
- `src/event_hub.c`：33 处（`sub_8032548`…`sub_8038C84` 等 `arg`/`arg0`）
- `src/battle_engine.c`：1 处（`sub_8046480`）
- `src/battle_obj_core.c`：1 处（`sub_8020B04`）
- `src/obj_state.c`：`sub_80444A4` 体内 `arg0` 已是 `BattleObj *`，无需 cast

**cast 是唯一正确手段**：这些调用方（`sub_8032548` 族等）自身仍是 `u8 *`，但它们的大量字段访问（`obj[0x24]` 等裸偏移）让"整体改 BattleObj*"成为另一个大工程，且会改变每个访问的 IR。**显式 cast 只影响类型检查，不产生任何指令** —— 实测字节与警告均可控。

---

## 3. 验证

```
python3 scripts/fncheck.py sub_80444A4 sub_803F5B4 sub_803F658 sub_802761C \
      sub_80462E4 sub_8032548 sub_8032D74 sub_8034F00 sub_80392C0 \
      sub_8046480 sub_8020B04
→ 全部 OK（68 / 164 / 2300 / 324 / 412 / 484 / 298 / 560 / 286 / 216 / 66 bytes）

make clean && make && sha1sum -c ll.sha1
→ ll.gba: 成功（匹配进度 812/1059）
```

对被改名的 4 个函数，clean 重编**无任何新警告**（`make 2>&1 | rg 'sub_80444A4|sub_803F5B4|sub_803F658|sub_80462E4'` 为空）——因为所有调用点都已加 cast。

其余 TU 里 `sub_801CBA4`/`sub_8020974`/`sub_8045F94` 的 `incompatible pointer type` / `makes pointer from integer` 警告是**既有的**（那批调用点本来就没 cast），与本次改动无关，未触碰。

---

## 4. 保留决策（未做，及原因）

1. **没有把这批函数的调用者也改成 `BattleObj *`**。`sub_8032548` 族等已匹配函数目前写作 `u8 *arg`，体内大量 `arg[0xXX]` 裸偏移；改成 `BattleObj *` 需要逐字段核对并把所有裸偏移结构体化，属于独立工作包，且对每个函数都要重跑 bytecmp。
2. **`sub_803F658` 仍是 `INCLUDE_ASM`**（status=0，2300 B）。仅原型升级，未写真 C —— 它是个 9 路 switch 的大状态机，需要单独立项。
3. **`sub_80462E4` 真 C 仍待攻**（status=1 靠 matchings ASM 保绿，见其 TSV 原 note：231 指令 + r8/sb/sl 三连 + 栈 buf[12] + 三调用）。本次只改原型。
4. **`event_actor.c:1944` 附近 `/* ... */` 注释块**里那份 `sub_803F5B4(u8 *arg0)` 旧稿保留未动（注释块，不参与编译）。

---

## 5. 家族提示

`0x0839CD5C` 表共 89 项（`0x0839CD5C..0x0839CEBC`），指向 `0x080257D8..0x0804E2AC` 一族对象演出函数，全部经 `sub_803F444` 间接调用、传 `BattleObj`。**该族所有函数（含已匹配的 `u8 *` 写法者）的入参语义都是 `BattleObj *`**，后续做逐个结构体化时可直接采用。
