# MATCH-80282EC-20260913

## 目标
- address: 0x080282EC
- current_name: sub_80282EC
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 692B)
- entry_kind: task-table (handler 指针表 0x0839CD5C, sub_803F444 分派器间接调用)
- args: r0 = BattleObj *obj, r1 = BattleObj *arg1 (同池另一对象)
- return: r0 = u8 result (0 继续 / 1 动作完成)

## arg1 证据 (E2)
- sub_803F444 读 obj+0xbe/0xbc 选表, 用 `_call_via_r2` 调 handler 时传 `(r4=obj, r6=第二参)`:
  asm/nonmatchings/sub_803F444.s 中多处 `adds r0,r4,#0; adds r1,r6,#0; bl _call_via_r2`。
- 调用点 0x0801BFD8 (code.s 56231 前): `r1 = obj->f_BD * 0xC8 + r6` → arg1 来自对象池另一 BattleObj。
- 本函数内 arg1 访问: +0xC0 posY (插值参照), +0x54 headB.kindFlags (0x800 判定)。

## 机器契约
- callees: sub_80444A4(BattleObj*), sub_803F5B4(BattleObj*), sub_801768C(s16,s16,s16,s16,u8),
  sub_801CE80(BattleObj*,u8,u16,u8,u8), Sfx_Play(u16,u8,u8), sub_8044514(s16), sub_803F658(BattleObj*)
- memory_reads: obj +0xBF posX, +0xC0 posY, +0x2A headA.f_1E, +0x35 headA.palSlot,
  +0x24 headA.kindFlags, +0x28 headA.frameIdx, +0x2C gfxTotal, +0x2E gfxPos;
  arg1 +0xC0 posY, +0x54 headB.kindFlags;
  gUnk_03000820/0822/0824/0825/0828/0829, gUnk_03000844/45/56
- memory_writes: obj posX/posY, headA.kindFlags (|=0x100, &=0xEEFF);
  gUnk_03000820 状态, gUnk_03000825 计数, gUnk_03000828/29/22/24 暂存

## 语义 (状态机, 0/9/0x12..0x19)
- 0: 暂存位置/头字段; sub_80444A4 清同组伤害; sub_803F5B4; 计数=0 → 0x12。
- 0x12: headA.kindFlags & 0x800 落 → 0x13。
- 0x13: 计数<=9 → posY = interp(gUnk_03000829, (arg1->posY+0x10)-gUnk_03000829, 0xA, 计数, 2), 计数++;
       否则 → 0x14。
- 0x14: sub_801CE80(obj,5,0x1B4,0xC,1) → 0x15。
- 0x15: headA.gfxPos >= headA.gfxTotal-1 → posX += 0x47; 计数=0 → 0x16。
- 0x16: frameIdx==0x19→Sfx(0x19); ==0x6B→Sfx(0x5A); ==0xD1→sub_8044514(0x1E) + →0x17; 计数++。
- 0x17: headA.kindFlags&0x1000 → |=0x100 → 0x18; 计数++。
- 0x18: !(arg1->headB.kindFlags & 0x800) → headA.kindFlags &= 0xEEFF; sub_801CE80 复位; posX -= 0x47;
       → 0x19; 计数=0。
- 0x19: 计数<=9 → posY = interp(arg1->posY+0x10, gUnk_03000829-(arg1->posY+0x10), 0xA, 计数, 2), 计数++;
       否则 posY = gUnk_03000829; → 9。
- 9: 无其他演出占用 → result=1。
- 尾: sub_803F658(obj)。

与 sub_8028098 差异: 双参 (arg1), 位置插值围绕 arg1 换位; case0 无 sub_801CE80;
0x13 每帧插值到位, 0x15 posX 偏移 ±0x47 实现左右换位。

## 字节匹配关键点
- case19/25 必须写成 `if (gUnk_03000825 <= 9) { 插值 } else { ... }` (fallthrough=插值),
  否则 GCC 反转分支极性 (bls vs bhi), 差约 8 字节。
- case24 的 `u16 f = arg1->headB.kindFlags & 0x800;` 必须保持 u16 类型与复用 (stack 第5参),
  才能复现 `lsls #0x10; lsrs r6,#0x10; str r6,[sp]`。
- case19 与 case25 的插值共享 `subs r1,r1,r0; bl sub_801768C` 尾块 (GCC 自动合并)。

## 验证
- scripts/bytecmp.sh: 仅 9 个 bl 占位字节不同 (relocation), 归一化后 0 diff (692B)。
- python3 scripts/fncheck.py sub_80282EC: OK (692 bytes @0x080282ec, 50 池重定位已施加, 9 bl 槽忽略)。
- make + sha1sum -c ll.sha1: 通过 (进度 814→815)。

## 改动文件
- src/event_actor.c: 仅 0x080282EC 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 原型 void sub_80282EC() → u8 sub_80282EC(BattleObj *, BattleObj *)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_80282EC/base.c: 候选。
