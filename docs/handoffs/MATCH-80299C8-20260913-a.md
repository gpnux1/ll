# MATCH sub_80299C8 (战斗对象"归位到目标"演出状态机)

- 地址: `0x080299C8` (物理 TU: `src/event_actor.c`, module: event_actor)
- 状态: **✅ 已匹配** (status=1), `fncheck OK 560 bytes`
- 认领: `agent-80299c8`
- 原型: `u8 sub_80299C8(BattleObj *obj, BattleObj *arg1)` (已升级 `include/code_0.h:706`)

## 1. 机器契约

```
entry_kind:       indirect (0x0839CD5C 指针表成员之一, sub_803F444 对 slot 0xB..0x70 的对象间接调用;
                  与 sub_80285A0/sub_8029784 同族; arg1 = 同池伙伴对象)
args:             r0 = BattleObj *obj
                  r1 = BattleObj *arg1 (插值目标)
return:           r0 = u8 (1 = 完成, 仅 case9; 否则 0)
callees:          sub_80444A4, sub_801CE80, sub_803F5B4, sub_804B834, sub_804B8E8,
                  sub_801768C, sub_8044514, sub_803F658
memory_reads:     obj->posX/posY, headA.f_1E(+0x2A), headA.palSlot(+0x35), headA.kindFlags(+0x24),
                  headA.frameIdx(+0x28); arg1->posX/posY
memory_writes:    obj->posX/posY; gUnk_03000822/24/25/28/29; gUnk_03000820
persistent_state: 0x03000820 (状态游标) / 0x03000822/24 (f_1E/palSlot 暂存) / 0x03000825 (帧计数) /
                  0x03000828/29 (posX/posY 暂存)
```

## 2. 语义 (已验证)

`switch (gUnk_03000820)`:

- **0**: 暂存 `posX/posY/headA.f_1E/headA.palSlot`; `sub_80444A4(obj)`;
  `sub_801CE80(obj,1,0x1B4,0xD,0)`; `sub_803F5B4(obj)`; 计数=0 → 1。
- **1**: `headA.kindFlags & 0x800` 未落则等; 落则 `sub_804B834(palSlot,1,3,-0xB,5)` → 2。
- **2**: `headA.frameIdx <= 0x2E` 时插值 posX/posY 向 `(arg1->posX-0x30)/arg1->posY`,
  帧长 `0x2F`, 帧号 `(s16)headA.frameIdx`, mode 2; 否则 → 3。
- **3**: `headA.frameIdx > 0x4F` → `sub_8044514(0x14)` → 4。
- **4**: `headA.kindFlags & 0x1000` → `sub_804B8E8(palSlot,1)`,
  `sub_801CE80(obj,0,gUnk_03000822,gUnk_03000824,0)`, posX/posY 还原暂存 → 5。
- **5**: `headA.kindFlags & 0x800` 落 → `sub_804B834(palSlot,1,3,-0xB,5)` → 6。
- **6**: `gUnk_03000844/45/56` 全 0 → 9。
- **9**: `result = 1`。

尾部 `gUnk_03000825 += 1; sub_803F658(obj); return result;` (无 `sub_80187E8`)。

## 3. 关键形状点

- case2 必须写成 `if (frameIdx <= 0x2E) { 插值... } else { gUnk_03000820 = 3; }`,
  目标结构是 `bhi 前跳到 state=3`(插值块 fallthrough)。写成 `if (>0x2E){state=3}else{...}`
  会产生 `bls` 反极性, 多 4 条指令 (已实测 29→23 差分)。
- case2 的帧号须 `(s16)obj->headA.frameIdx` (目标 `ldrsh`)。
- 字段偏移全部结构体化, 由同族已匹配 sub_80285A0/sub_8029784 (functions.tsv 580/705) 交叉确认。

## 4. 复验

```bash
python3 scripts/fncheck.py sub_80299C8
# -> sub_80299C8  OK   (560 bytes @0x080299c8, 11 池重定位已施加, 11 bl 槽忽略)
```

- 候选: `permuter/sub_80299C8/base.c` (含 include) / `scripts/fndiff.sh sub_80299C8 ...`
- `include/code_0.h` 仅升级本函数原型 (void() → u8(BattleObj*,BattleObj*))。
- 锁: 已释放。
