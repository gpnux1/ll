# MATCH-sub_8025DA8-20260916.md

## 1. 基础信息
- **函数名**: `sub_8025DA8`
- **ROM 地址**: `0x08025DA8`
- **物理 TU**: `src/battle_menu_windows.c`
- **函数大小**: 788 字节 (354 行 ASM)
- **匹配状态**: 100% Match (fncheck OK 788B, SHA1 全绿, 907/1059)
- **日期**: 2026-09-16
- **认领 Agent**: `agent_sub_8025da8`

---

## 2. 机器契约 (Machine Contract)
- **输入**:
  - `BattleObj *obj` (`r0` -> `r6`): 发起动作的战斗对象指针
  - `BattleObj *arg1` (`r1` -> `r5`): 协同/目标战斗对象指针 (通常为同池伙伴或目标)
- **输出**:
  - `u32` (`r8` -> `r0`): 0 = 动画状态机推进中; 1 = 演出完成且无等待互斥占用
- **调用约定与寄存器分配**:
  - `r4`: 临时常量与标志 (0, 1, 0x36D8 偏移量)
  - `r5`: 保存 `arg1`
  - `r6`: 保存 `obj`
  - `r7`: 保存 `&gObjActStepTimer`
  - `r8`: 保存状态机返回值 `result` (初始置 0, 结束前若放行则置 1)
- **栈空间**: `sub sp, #0x18` (24 字节，为 `sub_801B81C` 提供 6 个栈参数空间)
- **外部调用 (Callees)**:
  - `sub_801CE80(BattleObj *obj, u32, u32, u32, u32)`: 动作切换 / 复位
  - `sub_80444A4(BattleObj *obj)`: 动作前置重置
  - `sub_803F5B4(BattleObj *obj)`: 动作发起
  - `sub_801B81C(...)`: 装配次头动画 `headB`
  - `Sfx_Play(u16 sfxId, u32, u32)`: 音效播放
  - `sub_801768C(...)`: 坐标多帧插值函数
  - `sub_8044514(u32)`: 释放/移交演出控制权
  - `sub_803F658(BattleObj *obj)`: 每帧动作收尾与状态机统一推进
- **全局状态与数据表**:
  - `gObjActStep` (`0x03000820`): 状态机主步骤 (0, 3, 0x12..0x17, 5, 6, 9)
  - `gObjActSavedF2A` (`0x03000822`): 暂存 `headA.f_1E`
  - `gObjActSavedPal` (`0x03000824`): 暂存 `headA.palSlot`
  - `gObjActStepTimer` (`0x03000825`): 步骤帧计数器 (用于插值 $t$)
  - `gActWaitBusy0` (`0x03000844`): 演出互斥等待锁 0
  - `gActWaitBusy1` (`0x03000845`): 演出互斥等待锁 1
  - `gActWaitBusy2` (`0x03000856`): 演出互斥等待锁 2
  - `gUnk_08393B28` (`0x08393B28`): 战斗动画特效资源表 (`0x2BE` 项)

---

## 3. 语义建模与业务流程 (Semantic Modeling)
`sub_8025DA8` 是战斗对象"协作/投掷/协同突进"演出状态机 (由 `sub_803F444` 通过 `0x0839CD5C` 函数指针表分发)。它与 `sub_8027D9C` 和 `sub_80260BC` 属于同一同构族。

### 状态机时序
1. **`case 0` (起手)**:
   - 直接转 `gObjActStep = 3`。
2. **`case 3` (主动画切入)**:
   - 备份 `headA.f_1E` 到 `gObjActSavedF2A`，`headA.palSlot` 到 `gObjActSavedPal`。
   - 调用 `sub_801CE80(obj, 1, 0x1B4, 0xB, 0)` 切换动作。
   - 调用 `sub_80444A4(obj)` 与 `sub_803F5B4(obj)`。
   - 重置 `gObjActStepTimer = 0`，转 `0x12`。
3. **`case 0x12` (等主动画装载)**:
   - 检测 `obj->headA.kindFlags & 0x800` (DMA/分片装载中标志)。落位后转 `0x13`。
4. **`case 0x13` (主动画预热)**:
   - 检测 `obj->headA.frameIdx > 0x40`，满足后转 `0x14`。
5. **`case 0x14` (次动画头 headB 装配)**:
   - 调用 `sub_801B81C` 为 `&obj->headB` 装配 `0x2BE` 号动画资源 (坐标 `0x5E, 0x38`)。
   - 置位 `obj->state |= 0x2000` (跳跃/动作标志)，置 `obj->headB.f_2A = 2`。
   - 重置 `gObjActStepTimer = 0`，转 `0x15`。
6. **`case 0x15` (等次头装载 & 起手音效)**:
   - 等待 `obj->headB.kindFlags & 0x800` 落位，播放音效 `Sfx_Play(0x20, 1, 0)`，转 `0x16`。
7. **`case 0x16` (等主动画帧触发音效)**:
   - 检测 `obj->headA.frameIdx > 0x6D`，触发音效 `Sfx_Play(0x64, 2, 0)`，转 `0x17`。
8. **`case 0x17` (双轴协同插值位移)**:
   - 若 `gObjActStepTimer <= 9`:
     - 使用 `sub_801768C` 将 `obj->headB.f_2B` 从 `0x76` 插值到位移增量 `arg1->posX - 0x99` (周期 10 帧，模式 1)。
     - 将 `obj->headB.f_2C` 从 `0x5A` 插值到位移增量 `arg1->posY - 0x5A` (周期 10 帧，模式 1)。
     - 帧计数 `gObjActStepTimer += 1`。
   - 超时后:
     - 设定 `obj->f_B6 = 0x2BF`。
     - 触发阶段事件 `sub_8044514(0x28)`。
     - 清除动作状态 `obj->state &= ~0x2000`。
     - 转入状态 `5`。
9. **`case 5` (协同目标受击/互动坐标同步)**:
   - 等待 `arg1->headB.kindFlags & 0x800` 落位。
   - 设定协同目标次头坐标：`arg1->headB.f_2B = arg1->posX - 0x23`，`arg1->headB.f_2C = arg1->posY`。
   - 转入状态 `6`。
10. **`case 6` (主对象主动画复位)**:
    - 检测 `obj->headA.kindFlags & 0x1000` (主动画播放完毕)。
    - 清位 `0xEFFF`，调用 `sub_801CE80(obj, 0, gObjActSavedF2A, gObjActSavedPal, 0)` 复原主动画与调色板。
    - 转入状态 `9`。
11. **`case 9` (互斥锁等待与终结)**:
    - 轮询 `gActWaitBusy0 == 0 && gActWaitBusy1 == 0 && gActWaitBusy2 == 0`。
    - 全清后置 `result = 1`。
12. **统一尾部 (Epilogue)**:
    - 每帧调用 `sub_803F658(obj)` 进行公共收尾。
    - 额外兜底检查 `obj->headA.kindFlags & 0x1000`，若置位则清理并调用 `sub_801CE80` 复原。
    - 返回 `result`。

---

## 4. GCC 2.91 CodeGen 关键点
- **头文件原型修正**: 原 `include/battle_menu_windows.h` 中为占位声明 `void sub_8025DA8();`，修正为真实签名 `u32 sub_8025DA8(BattleObj *, BattleObj *);`。
- **寄存器与自然分支**:
  - `result` 声明为 `u32 result; result = 0;` 自然分配进 `r8`。
  - `case 9` 中的三等待标志判断写为 `if (gActWaitBusy0 == 0 && gActWaitBusy1 == 0 && gActWaitBusy2 == 0) result = 1;`，严格复现连续的 `cmp r0, #0; bne` 结构。
  - `case 5` 的 `arg1->headB.f_2C = arg1->posY` 自动利用 `&arg1->posY` (偏移 `0xC0`) 减去 `0x58` 得到 `&arg1->headB.f_2C` (偏移 `0x68`)。

---

## 5. 验证确认
- `fncheck.py sub_8025DA8` -> OK (788 bytes @ 0x08025DA8, 45 池重定位已施加, 12 bl 槽忽略)。
- `bytecmp.sh sub_8025DA8 permuter/sub_8025DA8/base.c` -> 除去 12 处未链接绝对 BL 槽 (48B) 外，全部 740 字节指令与池数据 100% 完全相同。
- `sha1sum -c ll.sha1` 全量验证通过 (907/1059, 85.6%)。
