# sub_8047B1C 匹配记录 (2026-09-16)

## 1. 基础信息
- **地址**: `0x08047B1C` (ROM 偏移 `0x47B1C`)
- **大小**: 524 字节 (0x20C)
- **TU**: `src/battle_engine.c`
- **原型**: `u8 sub_8047B1C(BattleObj *obj)`
- **状态**: 1 (完全匹配, fncheck OK 524B, make 全量 SHA1 一致通过)

## 2. 机器契约与语义建模
该函数是战斗系统中属性变化/BUFF演出请求的状态机执行函数（与 `sub_8048B30` 设置请求、`sub_804753C` 属性结算、`event_hub.c:381/2676` 动画协同推进）。
通过 `gFxReqTimer` 驱动 12 个状态节点（0..11），协调精灵动画播放、音效、调色板槽位注销与复位、多段循环等待，最终在状态 11 调用 `sub_804753C` 计算属性增量并返回 1 通知调用者演出结束。

### 2.1 全局变量与字段生命周期
- `gFxReqTimer` (`0x030008F1`, u8): 状态机主计时器/状态阶段（0..11）。
- `gFxReqKind` (`0x030008F2`, u8): 演出请求属性类型 (0..4 对应 atc/def/agl/men/res)。
- `gFxReqFrames` (`0x030008F3`, u8): 增量百分比或帧数参数。
- `gFxReqCounter` (`0x03000904`, u8): 本次新定名。演出子计数器（Case 0 清零，Case 4 统计循环执行次数 0..2）。
- `gFxReqAnimIdx` (`0x03000906`, u16): 基础动画索引基址。
- `obj->headA.kindFlags` (`+0x24`, u16): 动画控制标志（`0x800` 等待完成标志，`0x1000` 阶段完成标志，`0x100` 激活标志）。
- `obj->headA.palSlot` (`+0x35`, u8): 动画精灵占用的调色板槽位。

### 2.2 状态流转流程
- **Case 0**: 调用 `sub_8020974(&obj->headA, gFxReqAnimIdx, 0x1B4, 0xD, 2)` 装载第一段动画；清零 `gFxReqCounter = 0`；流转到状态 1。
- **Case 1**: 等待 `!(obj->headA.kindFlags & 0x800)`；触发音效 `Sfx_Play(0x19, 1, 0)`；流转到状态 2。
- **Case 2**: 等待 `obj->headA.kindFlags & 0x1000`；调用 `sub_804C3A4` 释放调色板槽位；装载第二段动画 `sub_8020974(&obj->headA, gFxReqAnimIdx + 1, 0x1B4, 0xD, 0x102)`；流转到状态 3。
- **Case 3**: 等待 `!(obj->headA.kindFlags & 0x800)`；清位 `obj->headA.kindFlags &= ~0x100`；流转到状态 4。
- **Case 4**: 循环 3 次（`gFxReqCounter <= 2`）：
  - 每次等待 `obj->headA.kindFlags & 0x1000`，调用 `sub_804C3A4` 释放调色板槽位，`gFxReqCounter++`，清位 `obj->headA.kindFlags &= ~0x1000`。
  - 满 3 次后（`gFxReqCounter > 2`）：装载第三段动画 `sub_8020974(&obj->headA, gFxReqAnimIdx + 2, 0x1B4, 0xD, 0x102)`；流转到状态 5。
- **Case 5**: 等待 `!(obj->headA.kindFlags & 0x800)`；清位 `obj->headA.kindFlags &= ~0x100`；流转到状态 6。
- **Case 6**: 等待 `obj->headA.kindFlags & 0x1000`；调用 `sub_804C3A4` 释放调色板槽位；置位 `obj->headA.kindFlags |= 0x100`；流转到状态 11。
- **Case 11**: 调用属性数值结算 `sub_804753C(obj, gFxReqKind, gFxReqFrames)`；返回 1（通知 `event_hub.c` 推进到 step 20）。
- **默认/未完成**: 返回 0。

## 3. 验证结果
- `python3 scripts/fncheck.py sub_8047B1C`：OK (524 bytes @0x08047b1c, 26 池重定位已施加, 11 bl 槽忽略)。
- `make` 全量构建：SHA1 一致，全量校验通过。
