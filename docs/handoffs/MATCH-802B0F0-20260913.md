# MATCH sub_802B0F0 — 2026-09-13 (analyze-agent)

## 结果
- 地址: 0x0802B0F0 (thumb, module event_actor)
- 状态: **已匹配** (functions.tsv status=1)
- 字节: 1304 B, bytecmp 0 diff (施加 20 个绝对符号后链接 1448B 全等), fncheck OK
- 全量: `make` 通过, `sha1sum -c ll.sha1` 成功, 进度 853/1059 (80.5%)

## 机器契约
```
address: 0x0802B0F0
entry_kind: indirect (事件演出函数指针表, 同 sub_802C9E8 族)
callees: sub_80187E8, sub_80444A4, sub_803F5B4, sub_801CE80, sub_8020CC4,
         sub_801768C, sub_8044514, Sfx_Play, sub_803F658
args: r0 = BattleObj *obj (演出对象); r1 = BattleObj *arg1 (目标对象)
return: r0 (u8): 0 继续 / 1 结束 (仅 case9 返回 1)
```

## 语义 (gUnk_03000820 状态机)
0 → 0x12..0x1E → 9。
- case0: 暂存 posX/posY/headA.f_1E/palSlot; 计数=0; sub_80444A4; sub_803F5B4;
  sub_801CE80(obj,1,0x1B4,0xB,0); f_B6=f_B4=0; headA.kindFlags|=0x100 → 0x12。
- 0x12: headA 0x800 落 → sub_8020CC4(obj,(posX-6),(posY+0xA),0x27C,0xD,0x2F4,4);
  headB.kindFlags|=0x100 → 0x13。
- 0x13: headB 0x800 落 → Sfx(0x5A); 清 headA/headB 0x100 → 0x14。
- 0x14: headB 0x1000 → state&=0xDFFF → 0x15。
- 0x15: headA.frameIdx>0x5A → Sfx(0x31); headA.kindFlags|=0x100; 计数=0 → 0x16。
- 0x16: 计数<=0x1D → posX 插值到 arg1->posX-8, posY 到 arg1->posY,
  pad_C1=sub_801768C(0,0x46,0xF,计数,2), 计数++; 否则 pad_C1=0, 清 headA 0x100,
  posY=arg1->posY+2 → 0x17。
- 0x17: headA.frameIdx>0x60 → sub_8020CC4(obj,(posX-8),posY,0x27C,0xD,0x2F5,5) → 0x18。
- 0x18: headB 0x800 落 → sub_8044514(0x28) → 0x19。
- 0x19: headB 0x1000 → state&=0xDFFF; headB.kindFlags&=0xEFFF → 0x1A;
  headA.frameIdx>0x9B → headA.kindFlags|=0x100。
- 0x1A: headA.frameIdx>0x9B → headA.kindFlags&=0xFEFF → 0x1B。
- 0x1B: headA.frameIdx>0xA5 结束; 否则 headA.kindFlags|=0x100, 计数=0,
  gUnk_0300086E=posX, gUnk_0300086F=posY, state=0x1C, Sfx(0x31)。
- 0x1C: 计数<=0x1D → posX/posY 向锚点插值, pad_C1 插值; 否则 pad_C1=0,
  清 headA 0x100, posY=锚点Y, posX=锚点X, state=0x1D, Sfx(0x31)。
- 0x1D: headA 0x1000 → sub_801CE80 复位 → 0x1E。
- 0x1E: headA 0x800 落 → 9。
- 9: gUnk_03000844/45/56 全 0 → result=1。
- 尾: sub_803F658(obj)。

## 关键 GCC2.9 形状点
1. case22 第一段 `arg1->posX - 8 - gUnk_03000828` **不能**加 `(u8)` 强制截断
   (加了得多出 `lsls/lsrs #0x18` 2 条 = 4B); 但 case18 的 `(u8)(posX-6)/(u8)(posY+0xA)`
   和 case23 的 `(u8)(posX-8)` **必须**保留截断 (目标有 lsls/lsrs)。
2. case22/case28 的插值源坐标: case22 用 gUnk_03000828/829 (暂存), case28 用
   gUnk_0300086E/86F (case27 存的锚点)。
3. case27/case28-else 共享尾块, 均调 Sfx_Play(0x31,1,0)。

## 与姊妹 sub_802C9E8 的对照
- sub_802C9E8 是单程插值 (到 0x17 结束); 本函数两程 (0x16/0x17 与 0x1C/0x1D)。
- 本函数 case0 用 sub_801CE80(obj,1,0x1B4,0xB,0); sub_802C9E8 用 (..,0xA,0) 且 f_B4=0xF。

## 影响面
- include/code_0.h:711: `void sub_802B0F0();` → `u8 sub_802B0F0(BattleObj *, BattleObj *);`
- 未新增 iwram 全局 (gUnk_0300086E/86F 已在 sub_802D728 匹配时声明)。
- 未改动 linker.ld / 其它函数区段。

## 锁定状态
- 已释放 (analyze-agent)。
