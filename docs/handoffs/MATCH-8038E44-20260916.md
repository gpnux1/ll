# MATCH-8038E44-20260916

## 目标
- address: 0x08038E44
- current_name: sub_8038E44 (仅检索标签)
- physical_tu: src/battle_stage_dialogue.c
- match_status: 1 (fncheck OK 480B @0x08038e44; = 0x08039024 - 0x08038E44)
- entry_kind: NPC/战斗对象对话演出 handler, 与 sub_8038C84 同族 (间接 handler 表, 全仓无直接调用)
- args: r0 = BattleObj *arg0, r1 = u8 *arg1
- return: r0 = u32 ret (0 继续 / 1 完成)
- 原型: include/battle_stage_dialogue.h `void sub_8038E44()` → `u32 sub_8038E44(BattleObj *, u8 *)`

## 建模依据 (零信任复核)
参考 = 已匹配 sub_8038C84 (docs 见 functions.tsv 673 行, 8032548 家族 NPC 对话状态机)。
本函数 asm/nonmatchings/sub_8038E44.s 逐 case 与其对比后确认同骨架; 所有偏移/常数/掩码
以本函数 E1 字节为准, 不照抄参考的动画 id / mode。

## 机器契约
- callees: sub_80187E8 (起手采样, 参考无), sub_80444A4, sub_803F5B4, sub_803E58C,
  sub_8020974, Sfx_Play, sub_8044514, sub_801B954, sub_804C3A4, sub_8045B90, sub_803F658
- memory_reads: arg0 +0xBF posX, +0xC0 posY, +0x24 headA.kindFlags, +0x28 headA.frameIdx,
  +0x35 headA.palSlot, +0xA1 pad_A1; arg1 +0xBE; gObjActStep(0x03000820), StepTimer(0x25),
  Param(0x6B), gActWaitBusy0/1/2(0x03000844/45/56)
- memory_writes: arg0 headA.kindFlags(+0x24), f_B4(+0xB4), f_B6(+0xB6);
  gObjActSavedX(0x03000828)/SavedY(0x03000829)/StepTimer/Step/Param(0x0300086B)
- 只读数据: 无函数外数据表 (常量: anim 0x3C8, 0x1B4, 0xD, sfx 0x31, wait 0x1E/0x28)

## 语义 (gObjActStep 0/1/2/3/5/8/9)
- 起手: ret=0; sub_80187E8();  然后按 gObjActStep 分派 (默认态 4/6/7 走尾)。
- 0: 存 posX/posY→gObjActSavedX/Y; StepTimer=0; sub_80444A4; sub_803F5B4;
     f_B6=0; f_B4=0 (b6ptr 先写 f_B6 再 subs#2 写 f_B4); Step=1; gObjActParam=0。
- 1: if sub_803E58C(arg0,arg1,**2**)==1 { sub_8020974(&headA, **0x3C8**, 0x1B4, 0xD, 2);
     if arg1[0xBE]<=0xA: headA.kindFlags |= 0x20; Step=2; }
- 2: if headA.kindFlags & 0x800 break; Step=3;
- 3: if headA.frameIdx==**0x24** { Sfx_Play(0x31,1,0); sub_8044514(**0x1E**); }
     if headA.frameIdx>**0x43** { Sfx_Play(0x31,1,0); sub_8044514(0x28); Step=5; }
- 5: if headA.kindFlags & 0x1000 { sub_804C3A4(headA.palSlot, (u8)sub_801B954(&headA));
     gObjActParam=0xC; Step=8; }
- 8: if sub_803E58C(arg0,arg1,0)==1 Step=9;
- 9: if gActWaitBusy0/1/2 全0 { sub_8045B90(arg0, arg0->pad_A1); ret=1; }
- 尾: sub_803F658(arg0); return ret。

## 与 sub_8038C84 的三处差异 (本函数特有)
1. 起手 `bl sub_80187E8()` → 破坏 caller-saved, 分派 r2 无法保留 state 指针, 每个推进态
   改用 `ldr r1,=0x03000820; strb` 重载 (sub_8038C84 用 `strb r0,[r2]`)。
2. case1: sub_803E58C 第 3 参 =2 (参考 0); 开场动画 id =0x3C8 (参考 0x3C7)。
   0x3C8 直接以字面量传参 → agbcc `movs r1,#0xf2; lsls r1,#2`; 若经 u16 变量会退化成
   缺 r1 赋值 (首轮 bytecmp 实测丢失 arg1) → 必须传字面量。
3. case3: 两段独立帧判定 (==0x24 一次性音效+等 0x1E; >0x43 音效+等 0x28+推进)。
   中间隔 Sfx_Play/sub_8044514 (bl) → headA.frameIdx 必须被重读两次 (两个 ldrh [r5,#0x28]),
   不能缓存到局部变量。参考只有一个 `frameIdx<=0x39 break; …→5`。

## GCC2.9 形状要点 (实测)
- case1 arg1 常量必须直传 0x3C8 (见上)。
- case0 的 zero(r2)/zero2(r1)/b6ptr/b4 拆分沿用 sub_8038C84: `zero=0;zero2=0;b6ptr=&f_B6;
  b4=zero2;*b6ptr=zero2;f_B4=b4;` 生成 strh f_B6, subs#2, strh f_B4, 末 `gObjActParam=zero` 复用 r2。
- sub_803E58C 返回 u8 → `== 1` 比较前 agbcc 生成 `lsls/lsrs #0x18` 截断 (两 case1/8 一致)。
- sub_804C3A4(palSlot, (u8)sub_801B954(&headA)): palSlot 先 ldrb 入 r4 暂存, 再 `subs r0,#0x29`
  由 obj+0x35 反推 obj+0xC 作 &headA 入参 —— 与参考逐字节同。

## 验证
- scripts/fncheck.py sub_8038E44: **OK 480 bytes @0x08038e44** (11 池重定位, 14 bl 槽忽略)。
- 同 TU 邻居 sub_8038C84/sub_8039024/sub_8032948 + 前任务 sub_80260BC fncheck 全 OK → 无回退。
- 全量 make: **代码区 (offset>=0xC0) 零 diff**; 仅 ROM 头固定 logo/checksum 字节
  (0x21/0x28/0x29/0x31/0x3b/0x109) 与 baserom 不符 —— 由其他 agent 并行进行中的
  HEADER-SPLIT-CODE0 重构 (include/iwram.h, linker.ld 已在开工前为 M, 生成物 asm/crt0.s /
  asm/rom_header.inc) 引入, 与本函数无关, 且属全仓共享文件 (§2.3), 不应由本 agent 触碰。
- 候选: .scratch/opencode-8038e44/base.c; permuter/sub_8038E44/base.c。

## 遗留
- 全量 SHA1 终验需等 header-split 并行工作合入后由 owner 统一转绿; 本 agent 不改动共享 header/linker。
