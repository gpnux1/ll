# MATCH-802E234 — 战斗对象"横移演出"状态机

## 结论
- **address**: `0x0802E234` (269 asm 行, `.text` 616 字节)
- **current_name**: `sub_802E234` (status=1, byte 匹配)
- **physical_tu**: `src/event_actor.c`
- **match_status**: 1 — `fncheck.py` OK (616B, 25 池重定位已施加, 11 bl 槽忽略); `make` + `sha1sum -c ll.sha1` 全绿
- **entry_kind**: 函数指针表 `0x0839CE04` (thumb = 0x0802E235), 家族 handler 表
- **推理依据**: E0 (ROM 指针表 0x0839CE04 命中 thumb 值 0x0802E235) + E1 (ASM) + E2 (相邻 handler 原型一致)

## 机器契约
- **args**: `r0 = BattleObj *obj`
- **return**: `r0` = status (0 继续 / 1 完成); 尾部 `adds r0, r6, #0` (result 变量 r6)
- **callees**: `sub_80187E8`, `sub_80444A4`, `sub_803F5B4`, `sub_801CE80`,
  `sub_801768C`, `sub_8044514`, `Sfx_Play`, `__modsi3`, `sub_803F658`
- **indirect_tables**: `0x0839CE04` (handler 表)
- **memory_reads**: `obj->headA.kindFlags(+0x24)`, `obj->headA.frameIdx(+0x28)`,
  `obj->headA.f_1E(+0x2A)`, `obj->headA.palSlot(+0x35)`, `obj->posX(+0xBF)`, `obj->posY(+0xC0)`
- **memory_writes**: `obj->headA.kindFlags/frameIdx`, `obj->posX`, `obj->f_B4/f_B6`,
  `gUnk_03000820/22/24/25/28/29`

## 状态机 (gUnk_03000820)
`0 → 0x12 → 0x13 → 0x14 → 0x15 → 0x16 → 0x17 → 9`

### case 语义要点
- **case0**: 暂存 posX/posY/headA.f_1E/headA.palSlot, 计数=0, `sub_80444A4` 清同组伤害,
  `sub_803F5B4`; `f_B6=0`, `f_B4=0`, `sub_801CE80(obj,1,0x1B4,0xA,0)` 切动画 → 0x12。
- **0x12**: headA.kindFlags 的 0x800 落 → 0x13。
- **0x13**: headA.frameIdx > 0x27 → `Sfx_Play(0x63,1,0)` → 0x14。
- **0x14**: headA.frameIdx > 0x4C → 计数=0, `Sfx_Play(0x36,1,0)`, headA.kindFlags |= 0x100 → 0x15。
- **0x15** (向右横移插值): 计数<=0x13 时
  `if (计数==0xA) sub_8044514(0x14);`
  `headA.frameIdx = (headA.frameIdx + 1) % 0x15 + 0x4D;`
  `posX = sub_801768C(g8628, 0xF0 - g8628, 0x14, 计数, 2);` 计数++;
  到 0x14: headA.kindFlags &= 0xFEFF, frameIdx=0x64, posX=g8628 → 0x16。
- **0x16**: headA 到 0x1000 → `sub_801CE80(obj,0,g8622,g8624,0)` 复位 → 0x17。
- **0x17**: headA 的 0x800 落 → 9。
- **case9**: `gUnk_03000844==0 && gUnk_03000845==0 && gUnk_03000856==0` → result=1。
- **尾部**: `sub_803F658(obj); return result;`

## 验证
```
scripts/bytecmp.sh sub_802E234 permuter/sub_802E234/base.c $(cat permuter/sub_802E234/sym.ld)
# => DIFF: 44 bytes / 688; 归一化 bl 遮罩后 = 0 差 (11 bl 槽)
python3 scripts/fncheck.py sub_802E234
# => OK (616 bytes @0x0802e234, 25 池重定位已施加, 11 bl 槽忽略)
make && sha1sum -c ll.sha1   # => ll.gba: 成功
```

## 共享文件改动
- `include/code_0.h`: `void sub_802E234();` → `u8 sub_802E234(BattleObj *);` + 注释
- `src/event_actor.c`: 替换 `INCLUDE_ASM` 为 C 实现 + 状态机文档注释

## 锁
- claim by `oc_12615`; 完成, 待释放。
