# MATCH-802DA78-20260913

## 目标
- address: 0x0802DA78
- current_name: sub_802DA78
- physical_tu: src/event_actor.c
- match_status: **1 (已合入, bytecmp/fncheck 双绿, 全量 SHA1 绿)**
- entry_kind: task-table (handler 指针表 0x0839CD5C)
- args: r0 = BattleObj *obj
- return: r0 = u8 result (0 继续 / 1 动作完成)

## 机器契约
- callees: sub_80187E8, sub_80444A4, sub_801CE80, sub_803F5B4, Sfx_Play,
  sub_8020CC4, sub_8044514, sub_804BF14, sub_804C728, sub_803F658, `__modsi3` (来自 `% 0x1F`/`% 0x10`)
- globals: gUnk_03000820 (state) / 0822 (u16, headA.f_1E 暂存, 又被当 sub_801CE80 arg2 复用) /
  0824 / 0825 (u8 计数, 初值 0xA) / 0828/0829 / 0844/0845/0856
- obj 偏移: +0x24=headA.kindFlags, +0x28=headA.frameIdx, +0x2A=headA.f_1E, +0x35=headA.palSlot,
  +0x54=headB.kindFlags, +0x58=headB.frameIdx, +0xB0=state, +0xB4=f_B4, +0xB6=f_B6,
  +0xBF=posX, +0xC0=posY

## 语义 (状态机)
- 0: 存 posX/posY/headA.f_1E/palSlot; sub_80444A4; **sub_801CE80(obj,5,gUnk_03000822,0xD,0)**
     (arg2 复读刚写入的 g822); sub_803F5B4; 计数=0xA; f_B6=1; f_B4=0 → 0x12。
- 0x12: 等 headA&0x800 → 0x13。
- 0x13: headA.frameIdx>6 → sub_8020CC4(obj,(u8)(posX+0x3C),posY,0x1B4,0xE,0x329,5) +
       headA.kindFlags|=0x100 → 0x14。
- 0x14: 等 headB&0x800 → state=0x15 + Sfx(0x5B,1,0) + headA.kindFlags&=0xFEFF。
- 0x15: headA.frameIdx>0x5F → Sfx(0x5A,1,0) + sub_8044514(0x3C) + 计数=0 +
       headA/headB.kindFlags|=0x100 + sub_804BF14(0,3,0x10,0x1C,0x1F,4,4,-1,2) → **0x19** (跳过 0x16-0x18)。
- 0x19: headA.frameIdx=(frameIdx+1)%0x1F+0x60; headB.frameIdx=(frameIdx+1)%0x10+0x5A;
       计数<=3 → 计数++; 否则 计数=0 + sub_804C728(0,3,0x20) → 0x1A。
- 0x1A: 同 0x19 但 headA +0x61 / 阈值 0x1D; 否则 headB.frameIdx=0x6B + headB.kindFlags&=0xFEFF → 0x16。
- 0x16: headA.frameIdx=(frameIdx+1)%0x1F+0x61; headB&0x1000 → state&=0xDFFF +
       headA.kindFlags&=0xFEFF + headA.frameIdx=0x80 → 0x17。
- 0x17: headA&0x1000 → sub_801CE80(obj,0,0822,0824,0) + headA.kindFlags|=0x100 → 0x18。
- 0x18: 等 headA&0x800 → state=9 + headA.kindFlags&=0xFEFF。
- 9: gUnk_03000844/45/56 全 0 → result=1。
- 尾: sub_803F658(obj)。

## 关键形状
- case0 的 `sub_801CE80` arg2 用 **gUnk_03000822**（刚存的值），而非字面量 0x1B4。
- case25 的 `%0x1F`/`%0x10` 直接写 `%` → GCC2.9 出 `__modsi3` (无内联)。
- case25/26 的 `计数++` 写成 `if (计数<=N){计数++;break;} ...` 结构，让 GCC 合并到共享尾块 (`_0802DCF2`)。
- case21 → 0x19 (不是 0x16)，注意跳转目标。

## 验证
- `.scratch/bcfn.sh sub_802DA78 <base.c> permuter/sub_802DA78/sym.ld`: **OK 1088 bytes** (一次命中)。
- `python3 scripts/fncheck.py sub_802DA78`: **OK (908 bytes @0x0802da78, 15 bl 槽忽略)**。
- `make` + `sha1sum -c ll.sha1`: **通过 (839/1059, 79.2%)**。
- 原型: `void sub_802DA78();` → `u8 sub_802DA78(BattleObj *);` (仅任务表分发)。

## 改动文件
- `src/event_actor.c`: sub_802DA78 C 体 + 家族注释。
- `include/code_0.h:724`: sub_802DA78 原型。
- `functions.tsv:604`: status 0→1 + note。
- `permuter/sub_802DA78/{base.c,compile.sh,settings.toml,target.s,sym.ld}`: 候选与工具。
