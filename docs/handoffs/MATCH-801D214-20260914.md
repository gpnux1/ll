# Handoff: sub_801D214 (0x0801D214, 161 指令 / 356 字节) — 战斗我方 5 槽 UI/HUD 精灵属性装载与 DMA 上传

日期: 2026-09-14 | agent: opencode | 状态: ✅ 100% 字节完全匹配 (status=1)

## 1. 机器契约 (已验证事实)

```
address:         0x0801D214
current_name:    sub_801D214
physical_tu:     src/battle_obj_core.c
match_status:    1 (OK 356 bytes @0x0801d214, fncheck OK)
entry_kind:      direct (唯一调用点: src/sio_link.c:245 战斗 VBlank 流水线)
callers:         sub_8018070 (sio_link.c:245, `ret = sub_801D214(gObjPoolPtr, ret);`)
callees:         sub_804C2FC (battle_anim.c:687, 精灵调色板装载/占用); DMA3 为内联宏
indirect_tables: 无
args:            r0 = BattleObj* (战斗对象池首地址, 0xC8 步长)
                 r1 = u8 count (当前空闲 OAM 槽下标, 倒序分配)
return:          r0 = u8 count (分配后剩余 OAM 槽下标)
memory_reads:    [arg0 + i * 0xC8 + 0xBE] u8 (slot 槽号, 0xFF 为空)
                 [arg0 + i * 0xC8 + 0xB0] u16 (state 状态位, 0x20 为禁用/隐藏)
memory_writes:   gOamBuffer[count] (0x030035C0 + count * 8, 8 字节 OamData 写入)
                 REG_DMA3 (0x040000D4..0x040000E0, 0x02021040 拷 0x280 字节到 0x06012880)
persistent_state: gOamBuffer (0x030035C0), OBJ VRAM (0x06012880, tile 0x144 起 20 个 tile)
script_relation: 无直接脚本字节; 由战斗渲染循环逐帧调用
```

## 2. 功能语义 (E2 级证据)

**战斗我方 5 槽角色 HUD/UI 精灵装配与图块 DMA 上传**。
- 扫描前 5 个战斗对象 (`i = 0..4`, 对应我方 5 个参战槽位):
  - 若 `obj[i].slot == 0xFF` (未登场) 或 `obj[i].state & 0x20` (隐藏/禁用), 则跳过。
  - 为活跃参战角色在 `gOamBuffer[count]` 装载一个 OAM 实体:
    - `VPos = 0x90` (Y 坐标 = 144, 屏幕底部)
    - `HPos = i * 40 + 0x20` (X 坐标 = 32, 72, 112, 152, 192, 5 角色横向均匀排列)
    - `Shape = 1`, `Size = 1` (32×8 矩形精灵)
    - `CharNo = i * 4 + 0x144` (每个角色分配 4 个 8×8 tile, 对应 32×8 像素)
    - `Priority = 2` (显示优先级 2)
    - `Pltt = 6` (使用 OBJ 调色板槽 6)
    - 分配完成后 `count--`。
- DMA3 拷贝: 把 `0x02021040` (EWRAM 中的 5 槽 UI 图块数据) 传送到 OBJ VRAM `0x06012880` (tile 0x144 的显存地址: `0x06010000 + 0x144 * 32 = 0x06012880`), 传输大小 0x280 字节 (5 槽 × 4 tile × 32B = 640B)。
- 调色板加载: 调用 `sub_804C2FC((u32)gUnk_0861C744, 6, 1)` 为 OBJ 调色板槽 6 加载 16 色调色板。
- 返回剩余可用 OAM 槽下标 `count`。

## 3. 破局关键与 GCC2.9 匹配经验

1. **结构体类型确认 (Bitfield)**:
   - 前任 agent 卡在手写连续掩码位运算，IR 伪寄存器生命周期错乱，导致寄存器分配严重偏位（score 4625）。
   - 目标汇编中连续的掩码操作 (`0xFFFFFF00`, `0xFFFFFCFF`, `0xFFFFF3FF`, `0xFFFFEFFF` 等) 是 GBA SDK 标准 `OamData` 位域结构体赋值由 GCC2.9 编译展开的典型特征。
   - 使用 `OamData oam;` 逐位域字段赋值后，GCC2.9 自动生成完全相同的连续位操作序列。
2. **符号引用消去 adds r4, r4 差异**:
   - `buf = (OamData *)&gOamBuffer[count];` 通过引用全局数组符号 `gOamBuffer`，促使 GCC2.9 在循环内先物化符号基址到 `r0`，生成 `adds r4, r4, r0`，完美匹配。
3. **栈空间分配解释 (32 字节)**:
   - 函数 prologue 存在 `sub sp, #0x20`。由局部调色板缓冲声明 `u16 palette[16];` (32 字节) 引起，与尾部调色板加载 `sub_804C2FC` 的 16 色调色板大小一致。
4. **HPos 表达式与乘法优化**:
   - `oam.HPos = i * 40 + 0x20;` 经 GCC2.9 自动分解为 `((i * 4 + i) << 3) + 0x20`，既保持了可读性，又完美吻合目标汇编。
