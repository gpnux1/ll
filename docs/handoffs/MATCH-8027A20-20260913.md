# MATCH-8027A20-20260913

## 目标
- address: 0x08027A20
- current_name: sub_8027A20 (函数名保留; 真实语义为战斗对象前冲/回位演出状态机)
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 892B)
- entry_kind: task-table (handler 指针表 0x0839CD5C, 经 sub_803F444 对 slot 0xB..0x70 的 BattleObj 间接调用)
- args: r0 = BattleObj *obj
- return: r0 = u8 result (0 继续 / 1 动作完成)

## 机器契约
- callers: 间接 (指针表 0x0839CD5C); 同族可见调用者 sub_801BE34/sub_801C484 传入 GetObjPool() 槽的 BattleObj。
- callees:
  - sub_80187E8() — 每帧前置服务 (同族固定调用)
  - sub_80444A4(BattleObj*) — 清同组 dmgAmount
  - sub_801CE80(BattleObj*, u8, u16, u8, u8) — 动画切换
  - sub_803F5B4(BattleObj*) — 起手动作相位
  - Sfx_Play(u16, u8, u8)
  - sub_8044514(s16)
  - sub_801768C(s16, s16, s16, s16, u8) — 定点插值 (i16 参数, 第5参 u8)
  - sub_803F658(BattleObj*) — 共享动作相位收尾服务 (尾部每帧)
- memory_reads:
  - obj+0xBF posX (u8), obj+0xC0 posY (u8)
  - obj+0x2A headA.f_1E (u16), obj+0x35 headA.palSlot (u8), obj+0x24 headA.kindFlags (u16), obj+0x28 headA.frameIdx (u16)
  - gUnk_03000820 (u8, 状态), gUnk_03000822 (u16), gUnk_03000824 (u8), gUnk_03000825 (u8, 帧计数)
  - gUnk_03000828 (u8), gUnk_03000829 (u8) 暂存位置
  - gUnk_03000844/03000845/03000856 (u8, 其他演出占用标志)
- memory_writes:
  - obj posX/posY, headA.kindFlags (|= / &=), obj+0xBF/0xC0
  - gUnk_03000820 状态推进; gUnk_03000825 递增
  - gUnk_03000828/0829/0822/0824 暂存
- persistent_state: gUnk_03000820 跨帧状态 0/9/0x12..0x19; gUnk_03000825 每帧计数 (尾部 ++)。

## 语义 (状态机)
- 0: 存 posX/posY/headA.f_1E/headA.palSlot → sub_80444A4 清同组伤害 → sub_801CE80(obj,5,0x1B4,0xB,0)
     切动画 → sub_803F5B4 起手 → 计数=0 → 0x12。
- 0x12: kindFlags&0x800 落 → 0x13。
- 0x13: frameIdx==0x39 → Sfx(0x1D) + kindFlags|=0x100 → 0x14 + 计数=0。
- 0x14: 计数==0xD → kindFlags|=0x200; 计数<=0x13 → posY=interp(gUnk_03000829,-100,20,计数,2);
        否则 posX=0xB4 + 计数=0 → 0x15。
- 0x15: 计数==5 → kindFlags&=~0x200; 计数<=0x13 → posY=interp(0829-100,100,20,计数,1);
        否则 计数=0 + sub_8044514(0x14) + kindFlags&=~0x100 → 0x16。
- 0x16: frameIdx==0x64 → 计数=0 + kindFlags|=0x100 + Sfx(0x1D) → 0x17。
- 0x17: 计数==0xD → kindFlags|=0x200; 计数<=0x13 → posY=interp(0829,-100,20,计数,2);
        否则 posX=0828+0xA + 计数=0 → 0x18。
- 0x18: 计数==5 → kindFlags&=~0x200; 计数<=0x13 → posY=interp(0829-100,100,20,计数,1);
        否则 计数=0 + kindFlags&=~0x100 + posX=0828 + posY=0829 + Sfx(0x43) → 0x19。
- 0x19: kindFlags&0x1000 → 清位 + sub_801CE80(obj,0,0822,0824,0) 复位 + 位置还原 → 9。
- 9: 无其他演出占用 → result=1。
- 尾部: gUnk_03000825 += 1; sub_803F658(obj); return result。

0x14/0x17 与 0x15/0x18 是两趟出去/回来的位移: 前者下压 posY 并侧移 posX, 后者回中。

## 字节匹配关键点
- case25 必须用字面 `0` 传参, 不可定义共享 `u32 zero` 变量再在 case25 复用:
  共享 zero 会把该零常量提升到 r4, 抢走 obj 的 r5 (obj → r4), 目标却把 r4 给 case0 的零常量,
  造成 162 字节差 (寄存器全局分配差异)。
- 共享尾块 `_08027CA0`: case20/23 (mode=2) 与 case21/24 (mode=1) 合流, 写 obj+0xC0 (posY)。
- 分支极性与 `<= 0x13` / `> 0x13` 顺序按目标保留。

## 验证
- scripts/bytecmp.sh: 仅 11 个 bl 占位字节不同 (relocation), 归一化后 0 diff。
- python3 scripts/fncheck.py sub_8027A20: OK (892 bytes @0x08027a20, 27 池重定位已施加, 11 bl 槽忽略)。
- make + sha1sum -c ll.sha1: 通过 (匹配进度 812→813)。

## 改动文件
- src/event_actor.c: 仅 0x08027A20 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 该行原型 void sub_8027A20() → u8 sub_8027A20(BattleObj *) (无直接 C 调用者, 安全)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_8027A20/base.c: 候选。
