# Handoff: sub_804B654 (0x0804B654, 180 行 / 348 字节) — 启动/分配 OBJ 调色板动画槽 (BattleObj_StartWhiteTint/PalAnimAlloc)

日期: 2026-09-14 | agent: agent_sub_804B654 | 状态: ✅ 100% 字节完全匹配 (status=1)

## 1. 机器契约 (已验证事实)

```text
address:         0x0804B654
current_name:    sub_804B654
physical_tu:     src/battle_anim.c
match_status:    1 (OK 348 bytes @0x0804b654, 3 bl 槽忽略, 全量 make+SHA1 绿, 进度 883/1059)
entry_kind:      direct (src/battle_obj_core.c:1851/1863/1870/1878 对象白色着色动画启动 4 处调用)
callers:         sub_801EE6C 逆操作 / BattleObj_StartWhiteTint
callees:         sub_804C3E4 (调色板槽刷新/清空)
                 sub_804C364 (调色板槽标记占用)
indirect_tables: 无
args:            r0 = u8 arg0 (起始槽号)
                 r1 = u8 arg1 (槽位数量 count)
                 r2 = s8 *arg2 (RGB 增量数组 dR/dG/dB)
                 r3 = u8 arg3 (动画周期/总帧数 period/kind)
                 [sp, #0x30] = u8 arg4 (空槽扫描起始位 startBit)
                 [sp, #0x34] = u8 arg5 (分配模式 mode: 2=直接指定, 3=扫描空槽)
return:          s32/s8 (首槽的有符号目标调色板槽号 palSlot)
memory_reads:    arg2[0..2] (RGB 增量)
                 gObjPalAnim[arg0 + i].ctrl (检查是否正在运行流式动画)
                 gObjPalSlotUsed (mode 3 时位扫描查找空位)
                 gObjPalAnim[arg0].palSlot (末尾返回)
memory_writes:   arg2[0..2] (若大于 0x1F 则截断写回 0x1F)
                 gObjPalAnim[arg0 + i] 各字段 (ctrl, palSlot, period, counter, frameIdx, dR, dG, dB, shift)
persistent_state: gObjPalAnim (0x03000AE8), gObjPalSlotUsed (0x03000AE0)
script_relation: 由战斗演出对象效果触发，间接驱动调色板流水线
```

## 2. 语义与业务逻辑 (E2 级证据)

**启动/分配 OBJ 调色板动画槽**:
与 `sub_804B7B0`（停止 OBJ 调色板动画槽）配对，负责为对象分配动画槽位并初始化 `PaletteAnimEntry`：

1. **RGB 增量钳位**:
   对 `arg2[0]`, `arg2[1]`, `arg2[2]` 做 `> 0x1F` 判断，超限则写回 `0x1F`（RGB 5-bit 上限）。
2. **槽位遍历循环 (`i = 0..arg1 - 1`)**:
   - 若 `gObjPalAnim[arg0 + i].ctrl & 0xF == 1`（正在运行 opcode 1 流式调色板动画），则保护不打断，跳过当前槽。
   - `mode == 2`（直接指定槽）：
     调 `sub_804C3E4(arg0 + i)`，条目写入 `ctrl = 0x31`, `palSlot = arg0 + i`, `period = arg3`, `counter = 0`, `frameIdx = 0`, `dR/dG/dB = arg2[0..2]`, `shift = 0`。
   - `mode == 3`（动态扫描分配空位）：
     从 `arg4` 位开始逐位测试 `gObjPalSlotUsed`（`b = arg4..15`）：
     - 若 `b <= 0xF`（找到空槽）：调 `sub_804C364(b, 1)` 标记占用，调 `sub_804C3E4(arg0 + i)` 刷新，条目写入 `ctrl = 0x11`, `palSlot = b`, 其余同上；
     - 若 `b > 0xF`（槽位已满分配失败）：条目写入 `ctrl = 0xFF`, `palSlot = -1`, `period = 0`, `counter = 0`。
3. **返回值**:
   返回首个槽位 `(s8)gObjPalAnim[arg0].palSlot`。

## 3. GCC2.9 生成与破局关键

此前前任（2026-09-11）记录最佳 2720 分，卡在 `sl/sb`、栈槽分配和跳转结构上。本次分析直接定位关键：

1. **消除局部变量指针 `idx` 与 `entry`**:
   在 C 中声明 `idx = arg0 + i; entry = &...;` 会让 GCC 将其识别为跨越调用的长期变量，强制将栈帧由 `sub sp, #0x10` 扩大到 `#0x14`，并将参数从寄存器挤入栈。改成直接数组表达式 `gObjPalAnim[arg0 + i].field`，GCC 自动提取公共子表达式，栈帧自然收缩回 `#0x10`，`sl`（arg3）与 `sb`（arg5）完美归位。
2. **`switch (arg5)` 决策树替换 `if / else if`**:
   `mode` 判定必须写为 `switch (arg5)`，GCC2.9 会为其生成紧凑的 `cmp r0, #2; beq; cmp r0, #3; beq; b` 决策树，消除 `if/else if` 引入的多余 `bne` 与寄存器重载。
3. **分支极性对齐 (Branch Polarity)**:
   位扫描循环后必须写 `if (b <= 0xF) { 成功分支 } else { 失败分支 }`，使成功分支直落（fall-through），失败分支置于末尾 `bhi` 跳转处，与目标汇编分支拓扑 100% 吻合。

## 4. 验证

- `python3 scripts/fncheck.py sub_804B654` -> `OK (348 bytes @0x0804b654, 0 池重定位已施加, 3 bl 槽忽略)`
- `make && sha1sum -c ll.sha1` -> `ll.gba: 成功` (883/1059, 83.4%)
