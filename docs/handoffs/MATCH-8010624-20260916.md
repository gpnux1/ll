# Handoff: MATCH-8010624 (2026-09-16, agent_sub_8010624) — 菜单队伍实体/光标精灵动画设置 已匹配

## 结果

- `sub_8010624` (0x08010624, 162B/332B 汇编): **status 0→1**。
- `python3 scripts/fncheck.py sub_8010624` → OK (332 bytes @0x08010624, 10 池重定位已施加, 4 bl 槽忽略)。
- `make -j4 && sha1sum -c ll.sha1` 全绿。
- 合入位置: `src/menu_ui.c` (原 INCLUDE_ASM 占位处)。

## 机器契约与语义

- **入口参数**:
  - `u8 arg0`: 角色索引 (0..4，或 0xFF 代表全队/默认位置)。
  - `u8 arg1`: 模式 (0: 释放 5 个箭头/实体精灵并直接返回; 1: 释放 5 个实体精灵后继续重设当前角色; 2: 只释放指定角色的精灵并重设)。
- **操作对象**:
  - `gActors[19..23]` (即 `gDialogArrowActors` 附近的菜单光标/角色实体数组)。
  - `gSpriteNodePool`: 释放 `actor->sprNodeIdx` 对应的精灵链节点。
  - `gCameraPosX` / `gCameraPosY`: 实体基于摄像机视口位置的基准偏移计算。
  - `Chara_StartScriptAnim(target, 0)`: 激活并播放实体脚本动画 0。

## 关键代码生成与匹配细节

1. **循环控制与强度削减 (Strength Reduction)**:
   - 循环 `for (i = 0; i <= 4; i++)` 中的 `i` 必须声明为 `u16`。若声明为 `int`，GCC2.9 会触发强度削减将数组基址前移并以 `adds r4, #0x28; subs r5, #1` 指针迭代；使用 `u16` 能完美保住基于数组下标展开的乘法指令序列。
2. **32 位目标实体索引计算**:
   - `target` 在 `arg0 == 0xFF ? 0 : arg0` 后执行 `target += 19;`。此处必须为 32 位整型 (`int target;`)，生成单条 32 位无扩展加法指令 `adds r4, #0x13`。
3. **被调用子函数原型调和 (经验 114)**:
   - `Chara_StartScriptAnim` 在原调用点传入 `target` 时无 8 位截断 (`adds r0, r4, #0`)。在 `include/vram_transfer.h` 将其声明保留为无原型声明 `void Chara_StartScriptAnim();`，而在定义端 `src/vram_transfer.c` 改用 K&R 旧式定义，既规避了 C89 default-promotion 冲突报错，又使定义端与调用端指令均 100% 匹配。
4. **共享头文件原型升级 (经验 93)**:
   - 将 `include/menu_ui.h` 中 `void sub_8010624();` 升级为带参全原型 `void sub_8010624(u8, u8);`，消除与真实 C 定义的冲突。
