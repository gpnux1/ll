# Handoff: sub_8036564 (0x08036564, 918 字节) — 双参 NPC 剧情状态机

日期: 2026-09-13 | agent: opencode-agent | 状态: ✅ 字节完全匹配 (status=1)

## 1. 机器契约 (已验证事实)

```
address:         0x08036564
current_name:    sub_8036564 (仅检索标签)
physical_tu:     src/event_hub.c
match_status:    1 (fncheck OK 918B, 48 池重定位, 20 bl 槽忽略)
entry_kind:      indirect
args:            r0 = arg0 (BattleObj*), r1 = arg1 (同池目标对象, case22 插值目标)
return:          r0 = ret (0 或 1)
callees:         sub_80444A4, sub_803F5B4, sub_8020974, Sfx_Play, sub_804C3A4,
                 sub_801B954, sub_8020CC4, sub_801768C, sub_8044514, sub_80471AC,
                 sub_801CBA4, sub_8045B90, sub_803F658
persistent_state: gUnk_03000820 (0→1..0x18), gUnk_03000825, gUnk_03000822/24
```

## 2. 功能语义

NPC 剧情状态机 (25 态, 0..0x18), 比 sub_80368FC 多 headB 段落 (case19-24):
- case0: 存 palSlot/f_1E; sub_80444A4; sub_803F5B4; state=1; f_B6=0xAA; f_B4=0; g25=0。
- case1: sub_8020974(&headA, 0x3A0, 0x1B4, 0xD, 2) → 2。
- case2: 等 0x800 → Sfx(0xA5) → 5。
- case5: 等 0x1000 → sub_804C3A4(palSlot, sub_801B954(&headA)) + 清 0xEFFF +
  sub_8020974(&headA, 0x3A1, 0x1B4, 0xD, 0x102) → 0x12。
- case18: 等 0x800 + 清 0xFEFF → 0x13。
- case19: sub_8020CC4(arg0, (u8)(posX-0x10), (u8)(posY-0x18), 0x24A, 0xE, 0x3A7, 4) → 0x14。
- case20: 等 headB 0x800 → Sfx(0x31) → 0x15。
- case21: 等 headB 0x1000 → sub_804C3A4(headB.palSlot, sub_801B954(&headB)) +
  headB.kindFlags 清 0xEFFF/置 0x100; g25=0 → 0x16。
- case22: headB.frameIdx=(+1)%0x16+0x18; g25<=9 时把 headB.f_2B/f_2C 由
  (posX-0x10,posY-0x18) 向 arg1 位置插值 (sub_801768C), g25++; 否则 sub_8044514(0x14) +
  state&=0xDFFF → 0x17。
- case23: sub_80471AC()==0 → Sfx(0x64) → 0x18; g25++。
- case24: g44/45/56 全 0 → sub_801CBA4(arg0,0,g822,g824,0); state=6; g25++ (与 case23 共享尾块)。
- case6: 等 0x800 → 9。case9: sub_8045B90(arg0, pad_A1); ret=1。

## 3. 关键形状 (字节匹配必需)

1. `b4` 必须声明为 **`int`** (非 u8): u8 时 sub_804C3A4 的 palSlot 落到 r5 (ROM 为 r4),
   全函数 20 字节差; int 后降到 7。
2. case21 的 kindFlags 位操作必须拆成**两条独立 RMW**:
   `kindFlags &= 0xEFFF; kindFlags |= 0x100;`
   写成一条 `(kindFlags & 0xEFFF) | 0x100` 会让 0x100 常数占用 r2, 与 g25 的零冲突 (7 字节差)。
3. case22 插值坐标需 `(u8)` 语义 (posX-0x10 等), headB.f_2B/f_2C = sub_801768C(...)，
   `%0x16` 走 __modsi3。
4. case23/24 的 g25++ 写成同一处共享尾块。
5. case19/21 的 headB 访问: case21 `sub_801B954(&arg0->headB)` (非 headA)。

## 4. 验证

- bytecmp (忽略 bl 槽) non-bl 差异 0; fncheck OK (918B);
- make + sha1sum -c ll.sha1 绿 (858/1059, 81.0%)。

## 5. 命名/原型

沿用 event_hub.c 既有命名。原型由 `void sub_8036564()` 改为
`u32 sub_8036564(BattleObj *, BattleObj *)`。
