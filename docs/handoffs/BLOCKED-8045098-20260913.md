# BLOCKED sub_8045098 — 2026-09-13 (analyze-agent, 接管自 wb)

## 结果: **未匹配 (status=0)**
语义 100% 还原, 字节差 **5 字节 / 260** — 单一 GCC2.9 寄存器分配 (arg1 r5↔r6) 卡点。
源码保持 INCLUDE_ASM; 全量 `make` + `sha1sum -c ll.sha1` 绿 (853/1059)。

## 接管说明
- 用户指示接管; 原锁 wb (since 2026-09-11, note 空, 停滞)。
- 本次从"无跳转表"推进到 **只差 5 字节**, 并定位到唯一卡点。

## 机器契约
```
address: 0x08045098
module: battle_engine
callers: sub_804473C (0x08044xxx, fxKind==2 分支), 返回 u16
callees: Rng_LcgNext, __umodsi3, sub_8047DC8(obj0) -> u8, sub_8047D28(arg1, mask) -> u8
args: r0 = BattleObj *obj0; r1 = BattleObj *arg1
return: r0 (u16, 0..999)
```

## 语义
- switch (obj0->pad_A4[0]): 0x19..0x2F -> base=40, var=15; 0x30/0x31 -> base=70, var=10;
  default -> base=40, var=15。
- delta = -(((u32(*)(void))Rng_LcgNext)() % var); if (delta) base += delta。
- switch (sub_8047D28(arg1, sub_8047DC8(obj0))): 1 -> base*=2; 2 -> base=(s16)base/2; 0 -> 无。
- if ((s16)base < 0) base=0; if ((s16)base > 999) base=999; return base。

## 关键点
1. **跳转表必须靠显式 0x19..0x31 共 25 个 case** (每个单独 `base=..;var=..;break;`)。
   若写成 2 个 case + default, GCC2.9 生成 compare-chain, 前缀变 `bgt/blt` 而非
   目标的 `subs #0x19; cmp #0x18; bhi; lsls #2; 跳转表` (25 项)。
2. `mod/delta` 必须 `s16` 且写成 `delta = -(Rng() % var)` 再 `base += delta`
   (目标有 `negs r0,r0` 后 `adds`), 且 `base` 用 `s16` 中转 (`s16 t = base; base = t + delta;`)
   才出目标的 `lsls #0x10; asrs #0x10` 符号扩展与加序。
3. `base` 返回前经 `var = base;` 再判 `(s16)var < 0` (permuter 找到的形状)。

## 精确差异 (唯一卡点, 5 字节 @ 0x4/0x87/0x8d/0x92/0xba)
```
目标:  r6 = arg1 (adds r6,r1,#0 @0x4); var(w) in r5; base(dmg) in r7
候选:  r5 = arg1 (adds r5,r1,#0 @0x4); var(w) in r6; base(dmg) in r7
```
即 `arg1` 与 `var` 的 callee-saved 槽 **r5↔r6 对调**; 其余 255 字节完全一致 (size 260==260)。

## 已穷举无效 (全部 fndiff/bytecmp 字节判定)
- 变量声明顺序全排列 (base/var/delta 任意顺序, `s16`/`u16`/`int`/`u32` 混搭)
- 参数类型 (BattleObj/u8/void) 与参数名
- 表达式变体 (cast 位置、`% var` vs `% (s16)var`、neg 位置、`+=` vs `= t+delta`)
- block-scoped 声明、额外未用局部、`tgt = arg1` 中转
- decomp-permuter ~1 万迭代 (最佳 score 25 == 该 5B; 未继续下降)
- case 标签 `-0x19` 形式 vs 绝对值 0x19..0x31

## 下一步建议
1. 卡点是 GCC2.9 local-alloc 对 **参数 arg1 的伪寄存器编号** 早于 `var` 造成;
   可尝试: 在 prologue 用一条读取 `arg1` 的语句改变其活跃区间 (需保持语义与字节),
   或引入一个在 `var` 之后定义、含 arg1 的表达式。
2. 该函数与 `sub_8044F4C` (已匹配, 同族伤害计算) 共享 `sub_8047D28/sub_8047DC8/Rng`
   惯用法; 可对照 sub_8044F4C 的局部变量声明顺序寻找线索。
3. 候选已是可读最优 (permuter base.c / .scratch/analyze-agent/5098_cand.c)。

## 影响面
- include/code_0.h:861: 原型 `u32 sub_8045098();` → `u16 sub_8045098(BattleObj *, BattleObj *);`
  (签名更正确; 调用点 result 为 u16; 未合入, 不改生成物)。
- 未改动其它区段。

## 锁定状态
- 已按接管指令释放 (wb 的锁)。
