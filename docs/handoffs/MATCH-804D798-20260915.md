# sub_804D798 匹配与 battle_rewards 族全量结构体化重构 (2026-09-15)

## 1. 基础信息
- **地址**: `0x0804D798` (ROM 偏移 `0x4D798`)
- **大小**: 166 字节 (81 条指令/字)
- **TU**: `src/battle_rewards.c`
- **原型**: `void sub_804D798(BattleObj *obj, BattleObj *pool)`
- **状态**: 1 (攻破 opencode 历史挂起，完全匹配合入，fncheck OK 166B)

## 2. 攻破卡点记录 (opencode 历史挂起)
- **历史瓶颈**: opencode 曾挂起该函数，瓶颈为全文件编译时 GCC 2.9 生成 `ands r0, r1` 而 ROM 为 `ands r1, r0; strb r1, [r2]`，且 `&obj->animSubIdx` 出现调度寄存器选择差异。
- **根因分析**:
  - ROM 机器码:
    ```assembly
    34:   bl Rng_LcgNext
    35:   movs r1, #3
    36:   ands r1, r0
    37:   adds r2, r4, #0
    38:   adds r2, #0xc2
    39:   strb r1, [r2]
    ```
  - 若写作 `obj->animSubIdx = Rng_LcgNext() & 3;`，GCC 2.9 将 `r0` 作为运算目标寄存器生成 `ands r0, r1`。
  - 若显式使用临时局部变量 `v`:
    ```c
    v = Rng_LcgNext();
    v &= 3;
    obj->animSubIdx = v;
    ```
    GCC 2.9 将 `v &= 3` 编译为 `movs r1, #3; ands r1, r0; strb r1, [r2]`，与 ROM 字节 100% 逐指令严密契合！

## 3. battle_rewards.c 全族结构体化重构
结合用户要求（“不要强制转换和裸偏移，数组，而是使用结构体”），本次对 `src/battle_rewards.c` 的 16 个函数执行了彻底的纯结构体与自然类型重构：

1. **全面消除裸偏移指针解引用**:
   - 将全部 `*(u16 *)(*(u32 *)((u8 *)obj + 0x88) + 2)` 替换为 `((ObjAnimIdxBlk *)obj->animPtr)->mainAnim[1]`。
   - 将全部 `*(u16 *)(obj->animPtr + 8)` 替换为 `((ObjAnimIdxBlk *)obj->animPtr)->subIdx[0]`。
   - 将全部裸解引用 `*(u32 *)((u8 *)obj + 0x88)` 替换为直接字段访问 `obj->animPtr`。
   - 在 `sub_804D5B4` 中，将原始按字节展开的 `arg1[values[i] * 0xC8 + 0xAC/0xBC/0xC2]` 彻底替换为结构体数组访问：
     - `pool[values[i]].pad_AC[0]`
     - `pool[values[i]].fxKind`
     - `pool[values[i]].animSubIdx`

2. **全面消除畸形函数指针转换与零扩展强转**:
   - 依赖 `code_0.h` 中已规范的 `u32 Rng_LcgNext(void)`，将全文件内所有的 `((u32 (*)(void))Rng_LcgNext)()` 统一替换为自然的 `Rng_LcgNext()`。
   - 声明局部 `u8 rand;`，将所有的 `values[(u32)(u8)Rng_LcgNext() % count]` 替换为自然且易读的 `rand = Rng_LcgNext(); value = values[rand % count];`。

3. **次参类型规范**:
   - 统一次参为 `BattleObj *pool`，消除了向下透传至 `sub_80489E8` 时产生的全部 `incompatible-pointer-types` 警告，同时在 `include/code_0.h` 中同步更新了函数原型声明。

## 4. 全量验证
- `src/battle_rewards.c` 内全部 16 个函数（`sub_804D1B4` ~ `sub_804DCD8`）逐一通过 `fncheck.py`，全绿一致：
  - `sub_804D1B4`: OK (170 bytes)
  - `sub_804D260`: OK (174 bytes)
  - `sub_804D310`: OK (142 bytes)
  - `sub_804D3A0`: OK (170 bytes)
  - `sub_804D44C`: OK (174 bytes)
  - `sub_804D4FC`: OK (182 bytes)
  - `sub_804D5B4`: OK (340 bytes)
  - `sub_804D708`: OK (142 bytes)
  - `sub_804D798`: OK (166 bytes)
  - `sub_804D840`: OK (178 bytes)
  - `sub_804D8F4`: OK (272 bytes)
  - `sub_804DA04`: OK (182 bytes)
  - `sub_804DABC`: OK (168 bytes)
  - `sub_804DB64`: OK (190 bytes)
  - `sub_804DC24`: OK (178 bytes)
  - `sub_804DCD8`: OK (150 bytes)
- `make` ➜ `sha1sum -c ll.sha1`: **成功 (893/1059, 84.3%)**
