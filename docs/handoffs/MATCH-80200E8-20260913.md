# MATCH-80200E8 — 战斗对象数值装载 (2026-09-13, defcdgg-zcode)

## 结果
- `sub_80200E8` @ 0x080200E8 (318 B): **字节完全匹配**, fncheck OK, 全量 make+SHA1 绿 (804/1059)。
- 签名修正: `void sub_80200E8(u8*,u8*,u8)` → **`void sub_80200E8(BattleObj *, PlayerStats *, u8)`**
  (code_0.h; 唯一调用点 sub_801B964 为未匹配 .s, 无调用方字节影响)。

## 机器契约
- r0=obj(BattleObj*, 池对象或 0x03000248 独立实例), r1=&gPartyStats[n] (PlayerStats*, 调用点
  `lsls r1,r5,#6` 步长 0x40 + 基址 0x03004AC0), r2=u8 ← ROM 表 0x08393B14[memberId] → obj+0xAC。
- 流程: maxHp/maxMp/hp/mp ← stats; atc/def/agl/men/res ← base+equip 字节对; noa/lv;
  variantClass=0; +0x7E..0x86 连清 5×u16; equipSlots[0..5] ← equip_slot1..6;
  sub_8048B5C(obj, stats[0x33]); skills 循环 (0xFF/0x26=空 → |=0xFF, 否则 skills[i]=v-1);
  sub_8045BF4(obj); obj->pad_AC[0]=arg2; sub_8045EB8(obj); sub_801D12C(obj,0); f_C3=0x10; +0xC4=0x10。

## 关键匹配技巧 (前人 zcode-ll2 结构全解 + 本轮结构体视图重写收尾)
1. **结构体视图替代指针算术**解决"目标 RHS 先载" (BattleObj/PlayerStats 字段名见 code_0.h)。
2. **技能循环的 `s16 v` 是唯一命中形态**: u8/u16/int/register 全被 GCC2 前向传播消除
   `adds r1,r0,#0` 拷贝; s16 的家独立于 ldrb 结果 → 拷贝存活 (与目标一致)。
3. arg2 → r7 (callee-saved, 跨 4 个 bl 调用); u8 入参入口 lsls/lsrs 掩码。
4. bytecmp 对含 bl 函数的 16 字节差 = 4 个 bl 槽占位 (f7ff fffe) vs 真链接编码, 属工具固有;
   以 fncheck/全量 SHA1 为准。
