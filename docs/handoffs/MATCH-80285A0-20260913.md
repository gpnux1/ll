# MATCH-80285A0-20260913

## 目标
- address: 0x080285A0
- current_name: sub_80285A0
- physical_tu: src/event_actor.c
- match_status: 1 (fncheck OK 588B)
- entry_kind: task-table (handler 指针表 0x0839CD5C, idx 13 = 0x080285A1, sub_803F444 间接调用)
- args: r0 = BattleObj *obj, r1 = BattleObj *obj2 (同池目标对象)
- return: r0 = u8 result (0 继续 / 1 动作完成)

## 机器契约
- callees: sub_80444A4(BattleObj*), sub_801CE80(BattleObj*,u8,u16,u8,u8),
  sub_803F5B4(BattleObj*), sub_804B834(u8,u8,u8,s8,u8) [无原型, 实参 0xD,1,3,-0xB,5],
  Sfx_Play(u16,u8,u8), sub_801768C(s16,s16,s16,s16,u8), sub_8044514(s16),
  sub_804B8E8(u8,u8), sub_803F658(BattleObj*)
  (无 sub_80187E8, 无 sub_804B834 之外的额外收尾)
- memory_reads:
  - obj+0xBF posX, obj+0xC0 posY, obj+0x2A headA.f_1E, obj+0x35 headA.palSlot,
    obj+0x24 headA.kindFlags, obj+0x28 headA.frameIdx
  - obj2+0xBF posX, obj2+0xC0 posY
  - gUnk_03000820/0822/0824/0825/0828/0829, gUnk_03000844/45/56
- memory_writes:
  - obj posX/posY (插值/还原), headA.kindFlags 无写 (只读; 0x1000 处理不改 kindFlags)
  - gUnk_03000820 状态, gUnk_03000825 计数, gUnk_03000828/29/22/24 暂存

## 语义 (状态机, 0/9/0x12..0x17)
- 0: 暂存 posX/posY/headA.f_1E/headA.palSlot → sub_80444A4 清同组伤害 →
     sub_801CE80(obj,1,0x1B4,0xD,0) 切动画 → sub_803F5B4 起手 → 计数=0 → 0x12。
- 0x12: headA.kindFlags & 0x800 落 → sub_804B834(0xD,1,3,-0xB,5) + Sfx_Play(0x31,1,0) → 0x13。
- 0x13: headA.frameIdx <= 0x2E 时 obj 位置向 obj2 目标插值
       (posX 目标 = obj2->posX-0x30, posY 目标 = obj2->posY, 时长 0x2F,
        形参 4 = (s16)headA.frameIdx, mode 2); 超过 → 0x14。
- 0x14: headA.frameIdx > 0x4F → sub_8044514(0x14) → 0x15。
- 0x15: headA.kindFlags & 0x1000 → sub_804B8E8(0xD,1),
       sub_801CE80(obj,0,0822,0824,0), posX/posY 还原暂存 → 0x16。
- 0x16: headA.kindFlags & 0x800 落 → 0x17。
- 0x17: 无其他演出占用 (0844/0845/0856==0) → 9。
- 9: result = 1。
- 尾部 (每帧): gUnk_03000825 += 1; sub_803F658(obj)。

与 sub_8027760 的差异: 同样双参但动画类型 0xD (7760 是 0x12 无 CE80)、用 sub_804B834/B8E8
槽位恢复、插值时长为 0x2F、状态序列 0x12..0x17 (7760 为 0x12..0x19)。

## GCC2.9 形状要点
- case 19 的 frameIdx 必须显式 `(s16)obj->headA.frameIdx`; target 用 `ldrsh` 有符号读 +0x28,
  直接传 u16 会退化为无符号。
- gUnk_03000822 为 u16 (strh)。

## 验证
- python3 scripts/fncheck.py sub_80285A0: OK (588 bytes @0x080285a0, 45 池重定位已施加, 11 bl 槽忽略)。

## 改动文件
- src/event_actor.c: 0x080285A0 区段 INCLUDE_ASM → 真 C。
- include/code_0.h: 原型 void sub_80285A0() → u8 sub_80285A0(BattleObj *, BattleObj *)。
- functions.tsv: 对应行 status 0→1 + note。
- permuter/sub_80285A0/base.c: 候选。
