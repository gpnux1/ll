# INCIDENTS.md — 并发/构建事故记录

> 每次多 agent 事故追加一行 (时间 | 事件 | 根因 | 对策)。操作性铁律见 AGENTS.md §0。
> 历史来源: 原 INCIDENTS.md §3 (2026-09-01 拆分)。

## 事故表

| 时间 | 事件 | 根因 | 对策 |
|---|---|---|---|
| 20:14 | `(已删)ASSIGNMENTS` 里"智能体B 进行中: sub_80444A4"整行在我读后消失 | 并发编辑同一 md | 编辑前重读；状态表改由 `claim.sh --table` 生成 |
| 20:1x | `linker.ld` 报 `cannot move location counter backwards`（0x4A90 插在 0x4AA0 之后） | 手工插入未排序 | 方案 B 自动生成 + 排序 |
| 22:2x | 合入 sub_80528C8 后 SHA1 报 760 万字节差异，误以为是自己（r8 泄漏） | `make` 把别人 22:00-22:11 改的 code_1.c/code_1b.c/code_8005020.c 一起编进来 | `fncheck.py` 单函数自证 + `--blame` 归属 |
| 22:4x | 我用 `cp .scratch/linker.ld.orig linker.ld` 回退临时符号，**差点静默覆盖别人 22:38 的编辑** | 整体回退共享文件 | 铁律 3；改用逐行 edit |
| 22:2x | 我几次 `rm -rf .scratch` 可能删掉了别人的中间文件 | 共用临时目录 | 用 `.scratch/<agent>/`；已把 `.scratch/` 加入 .gitignore |
| 23:0x | `EXPERIENCE.md` 出现**两套重号的经验 30-34** | 两个智能体同时往编号列表尾部追加, 各自从 30 开始 | 已重排为连续 1-53。**根治建议**: 编号改用稳定 ID(如 `R-FLATEXPR`)而非裸序号, 或每人只往自己那半段加号 |
| 23:5x | `fncheck.py` 把一个**完全正确**的函数报成 FAIL（地址差 4 字节） | 它优先用 `ll.map` 取地址, 而别人的改动让 ROM 布局漂移了 | 改为 `code.s` > `linker.ld` > `ll.map`。→ 经验 57 |
| 23:5x | 合入 sub_8052580 后 ROM 又红了（首个差异 0x080003D4, 在 sub_80002A0 里） | 别人 23:50 改了 `include/code_0.h` 原型（`sub_8008124`/`sub_80187A8` 等 111 行）→ 改变了 code_0.c 的代码生成 + 各 .o 尺寸漂移 | `fncheck.py sub_8052580` 确认自己 OK 后照常提交; 不背别人的锅也不去改别人的文件 |
| 21:0x | `src/data_87E83F0.c` 突然 `multiple definition of gWalkMoveDirLut/gSpriteTileCountTable` + `.rodata is not within region rom`（溢出 0xE8=232 B） | **新故障模式**: 该文件 171–766 行是一个 `/* ... */` 大注释块（装草稿）。agent 删掉了块内 486 行草稿正文, 但**留下了块起始的孤立 `/*`** → 下一个 `*/` 提前闭合, 把后面所有草稿定义变成 live 代码 | 修法: `git show HEAD:<file>` 回底 + 只重新贴回有效 hunk（本次是 2 处语义改名）。
  **教训**: 改大注释块时必须 `grep -n '/\*\|\*/' <file>` 配对检查; 删完立刻 `make` 而不是只看 diff |
| 21:0x | 同一文件里另一个 agent 把重复定义改名成 `gUnk_087E94FC_bytes` 来绕过 `conflicting types` | 把**症状当根因**: 真正问题是注释块失效, 不是名字撞车 | 遇到"突然多出一堆重复定义"先查注释/预处理边界, 不要靠改名绕过 |
| 21:1x | `make` 链接失败: `gWalkMoveDirLut` / `gSpriteTileCountTable` 各被定义两次 (`src/data_805769C.c` 20:45 与 `src/data_87E83F0.c` 21:02), 且 `.rodata` 溢出 `rom` 区 | 两个智能体分别把同一批 rodata 落地到自己的 C 文件, 都没查对方 | 按 23:5x 行先例: `fncheck.py Save_LoadContinue` 已 OK, 照常推进不背锅。**建议**: rodata 落地前先 `grep -rn '<符号名>' src/` |
| 2026-09-01 | 本侧实装 sub_8008CC0 时发现 INCLUDE_ASM 行已被并发 agent 替换为 ChoiceMenu_ResolveDest 真 C, code.s 中途被对方重生成 | INCLUDE_ASM 行消失 + functions.yaml/ll.cfg mtime 变化 | 编辑前重读 + 不回退对方成果; ll.cfg 函数改名前先 rg 确认现名 (本会话×1) |
| 2026-09-01 | git stash pop 被 tools/gbadisasm/Makefile 的 CRLF 幻影修改卡住 (内容 diff 为 0 但 git 视为已修改) | `diff <(git show HEAD:f) f` 退出码 0 | `git add <file>` 标记当前内容后再 pop; 根治需统一 .gitattributes 行尾 |
| 2026-09-02 | `rename_fn.sh sub_800661C MapScene_Load` 终验失败后下次 make 找不到 `asm/nonmatchings/sub_800661C.s` | 脚本失败回滚恢复了 `ll.cfg/code.s/functions.tsv`，但未恢复已删除的 asm 切片 | 用 `python3 scripts/gen_asm.py` 按恢复后的权威源重建 asm; 改名期间改用定向补丁并先跑 fncheck |
| 2026-09-02 | 实装 sub_804EF50 时, 我的 code_0.h 原型 / functions.tsv note / progress.md 条目被并发 agent 的 `git add -A` 提交 (2140bb6/f9a308d) 扫入 HEAD, 唯独 code_8044394.c 真身未扫 (HEAD 一度处于"note 说✅ 但代码仍 INCLUDE_ASM"的不一致态) | 多 agent 各自 `git add -A` 会连带提交别人工作区里未完成的共享文件改动 | 收尾改用**定向 `git add <自己的文件>`** 而非 `-A`; 提交前 `git diff --stat` 核对只含本次函数; 事后单独 commit code_8044394.c 补齐一致性 (fncheck 64B + SHA1 绿) |
| 2026-09-02 | 实装 sub_8048764 提交时, `git add functions.tsv docs/progress.md` 把 gpnux(sub_80207DC)/opencode(sub_804EF50) 未提交的函数清单note+progress段落一并扫进我的 commit 2140bb6 | 共享文件用整文件 `git add` 会吞掉他人未提交改动; 只对 src/*.c 用了 `git add -p` 选块, 对 functions.tsv/progress.md 疏忽了 | 共享文件 (functions.tsv/progress.md/code_0.h/linker.ld) **一律 `git add -p` 逐块选**, 提交前 `git diff --cached <file>` 核对只含自己行; 本次扫入的是他人**已完成**成果且内容正确、ROM 绿, 不回退 (后续 03b0965/f9a308d 已各自补交代码, 函数清单一致) |
| 2026-09-02 | 第二次踩同一坑: 提交 code_8010F10 挂起记录时 `git add docs/progress.md` 整文件, 把他人未提交的 `0x08088D80 地图场景描述符表` 段扫进我的 commit bbc02b6 | 明知上次教训仍对 progress.md 用整文件 add; 且当时该 commit 已被他人 4608fb0 压在下面无法安全 reset | 扫入的是他人**已完成**文档内容、未损坏、ROM 绿, 不回退不重写历史; **铁律: progress.md/INCIDENTS.md/functions.tsv/code_0.h/linker.ld 提交前必 `git add -p` + `git diff --cached` 核对**, 本会话已两次违反, 下次提交共享文档前先 mental check |
| 2026-09-02 | 合入 sub_80113CC 时我的 functions.tsv status+note / progress.md 被 6ab44e2 (sub_80196D4 docs commit) 的整文件 add 先行扫入; 我的 commit 45d330c 只剩 progress.md+src, 函数清单内容本身无损 | 并发 agent 对共享文件整文件提交先落地, 我这边 diff 已空 | 无需处置: 校验 HEAD 与工作区 functions.tsv 第267行一致, 追加单独 commit 修 code_0.h 原型 (u8(void)); 整文件 add 吞并属既有事故模式 (第25/26条), 定向 add 为主, 遇吞并先核对内容再决定补交/不回退 |
| 2026-09-02 | 执行 rename_fn.sh sub_8012790 SaveUi_LoadScreen 首次失败后, 脚本回滚配置却未恢复已删除的旧 asm 切片；随后 make 报 can't open asm/nonmatchings/sub_8012790.s | 改名脚本的回滚覆盖了 ll.cfg/code.s/functions.tsv/源码，但不含 asm 生成目录 | 用 python3 scripts/gen_asm.py --sync 按恢复后的权威配置重建切片，再重试改名；重试后 fncheck 与 SHA1 均通过 |
| 2026-09-02 | 调试 sub_8048818 时执行 `git checkout HEAD -- src/code_8044394.c`, 连带把并发 agent gpnux(sub_804C890, 进行中) 未提交的真 C 转回 INCLUDE_ASM | 用 `git checkout HEAD --` 对整文件复原会销毁工作区里他人的未提交改动; 且当时未用 stash/备份验证 | WIP 副本在 permuter/sub_804C890/{base.c,v2.c} 完好, functions.tsv 仍 status=0, 复原后 ROM 反而转绿 (其 WIP 76B≠baserom 80B 本就红), 不回退不代修; **教训: 复原他人文件前先看 `git status` 区分自己/他人改动, 必要先备份** |
| 2026-09-02 | `make` 红在 `src/code_8005020.o`: `'gCameraMinY' undeclared` + `MapSceneDescriptor`/`gMapSceneDescriptors`/`gChoiceDataBase` 三处 `conflicting types` | ① `iwram.h` 并发追加的分节标题 `/* ==== 视口/摄像机滚动 ... ====` **漏写 `*/`**, C 注释直到下一行行尾的 `*/` 才闭合 → 紧跟其后的 `extern s16 gCameraMinY;` 整行被吞 (同一块还被两个 agent 重复追加两份); ② codex 给 `code_8005020.c` 加 `#include "data_805769C.h"` 时没删 .c 里的本地重复 typedef/extern (agbcc 里 typedef 重复 = 错误) | 补 `*/` 闭合分节注释 + 删 .c 本地重复声明 (逐字段核对与头文件布局一致才删, 改类型=改 codegen) + const 视图处用 `(u8 *)` 强转; 规律与注释配对扫描脚本见 EXPERIENCE 坑10; **搬符号进头文件与删 .c 重复声明必须同一次提交**。修后编译/链接全绿, 残余 107B 差异 `--blame` 归 `code_804F0B8.o` (opencode 进行中 sub_80526A0), 非本次改动 |
| 2026-09-02 | 我用 `git stash push <3个文件>` 临时验证"HEAD 是否本来就红", 期间**另一 agent 把我的工作区改动整体复原**（stash pop 已成功恢复、`git status` 已列出 src/code_8010F10.c, 下一条命令再 grep 已回到 INCLUDE_ASM） | 多 agent 共享同一工作区: 任何人 `git checkout -- <共享文件>` / `git stash` 都会连带吞掉别人未提交的成果; 而 `git stash` 本身就是把成果暴露在他人操作之下的窗口 | **不要用 `git stash` 做基线验证** —— 改用 `git show <commit>:<file>` 读 HEAD 版本, 或 `cp` 到 `.scratch/<agent>/` 后在临时目录单独 make; 恢复手段: `git diff <stash父> <stash提交> -- <文件>` 生成补丁 + `git apply --include=<自己文件>` 只回自己的块, 共享文件 (code_0.h/functions.tsv) 用 python 按行号重读后定点改; 本次成果全部找回 (fncheck 176B OK) |
| 2026-09-02 | `rename_fn.sh sub_8016C88 Sio_InitLink` 因**基线本来就红** (他人 WIP) 在 sha1sum 一步失败并自动回滚, 但 `asm/` 切片已被 gen_asm 改过 → 回滚后 `src/code_8010F10.o` 链接报 `undefined reference to Sio_InitLink` | rename 脚本第 2 步只备份 ll.cfg/code.s/functions.tsv/src+include, **不含 asm/ 生成物**; 回滚把权威配置还原了, 切片却留着新名字 (与 2026-09-02 sub_8012790 那次同一根因, 第二次踩) | 立即 `python3 scripts/gen_asm.py --sync` 按还原后的 code.s 重切 (写入 3 / 删除 1, 链接恢复); **教训升级: rename_fn.sh 失败后第一件事永远是 gen_asm --sync, 别手删 .s**; 且改名前应先确认 `sha1sum -c` 是绿的 (基线红时脚本必然走到回滚分支), 基线红就只做匹配不改名, 把语义名写进 TSV note 待绿后补 |
| 2026-09-02 | `rename_fn.sh sub_800A664 Stats_RebuildEquipBonuses` 在全量构建阶段因并发 agent 的 `sub_804D798.s` 生成切片缺失而失败 | 脚本回滚了配置和源码，但不回滚 `asm/` 生成物，留下新名切片并删除旧名切片 | 运行 `python3 scripts/gen_asm.py` 按回滚后的权威 `ll.cfg/code.s/functions.tsv` 恢复切片；之后以定向补丁完成改名并单独 `fncheck` |
| 2026-09-02 | 编译 code_8044394.o 时 `asm/nonmatchings/sub_804D798.s` 缺失: functions.tsv 已被另一 agent 置 status=1 且 .s 已生成到 asm/matchings/, 但 src 里 INCLUDE_ASM 还指向 nonmatchings | gen_asm 按 TSV 状态决定切片落点, 而 src 的 INCLUDE_ASM 路径未同步 (坑7 函数清单修正漏做) → 装配器找不到 nonmatchings 切片, C 文件 全绿受阻 | 按既有先例 (line 443 sub_80462E4) 把该行 INCLUDE_ASM 路径改成 asm/matchings 并注明坑7; fncheck sub_804D798 仍 OK 168B; 不影响他人成果 |
| 2026-09-02 | gen_asm 报 `code.s 无函数块: Stats_RebuildEquipBonuses (0x0800a664)`: ll.cfg 已改名但 code.s 还是旧名 sub_800A664 | rename_fn 第 1 步 (ll.cfg/code_0.h/src) 完成、第 2 步 (gbadisasm 重出 code.s) 未落, 期间并发构建又删了 sub_804D798 切片 | 重新 `tools/gbadisasm/gbadisasm baserom.gba -c ll.cfg > code.s` (确定性生成) 后 gen_asm 正常; 随后 code_80002A0.c 的调用点 sub_800A664 也被并发补成 Stats_RebuildEquipBonuses, 链接恢复 |
| 2026-09-03 | 会话开场 make 红: `can't open asm/nonmatchings/sub_80488CC.s` — functions.tsv status=1/`.s` 在 matchings/ 但 src 仍是 INCLUDE_ASM (合入遗漏第3例, 同坑7); 修好后又暴露 sub_804F8D8 同病, 但其真 C 就在 index 里 (工作区被并发 agent 的 sub_805008C 变体实验覆盖回 INCLUDE_ASM, stash@{0} 留有实验副本) | codex/ll-agent 合入时漏替换 INCLUDE_ASM 行; 并发编辑 code_804F0B8.c 时工作区互相覆盖 | sub_80488CC: 按 matchings/.s+progress 语义重建真 C (指令级 bytecmp 一致, fncheck 102B OK) + code_0.h 原型 void→u8; sub_804F8D8: `git checkout -- <file>` 恢复 index 版真 C (实验在 stash, 无损失) |
| 2026-09-03 | 红基线定性: 纯净 HEAD worktree (补 data/raw_data + gen_asm 后) 同样 SHA1 红, 首差 0x080003d4 与主工作区完全一致 → 红为**已提交状态**, 非任何在途未提交改动 | 双根因: ① sub_804D708 (commit 898cb17) 编译 140B ≠ baserom 槽位 144B, sub_804D798 起 322 符号统一位移 -4 (bl槽/池值大面积散差, fncheck 仍可过); ② System_Init 池内容真差异 (+0x90 目标 0x0805769C vs 我方 0x040000D4, 无重定位的字面量级分歧) — 早于本会话 (INCIDENTS 23:5x 已有同签名记录) | 未修未回退 (他人成果+深水区); 本会话所有函数以 `fncheck.py <fn>` 自证; 接手者提示: sub_804D708 差 12B 内容+4B 尺寸 (fncheck +0x3b..0x87), System_Init 是独立第二处 |
| 2026-09-03 23:xx | gpnux 匹配 sub_8045A10 期间发现 HEAD 基线本身红: 12 个 status=1 函数 (HBlankWave_BuildTables/Rand_TableNext/System_Init 等, 多为近期改名/合并引入) 字节 FAIL, ll.gba SHA1 不匹配 | 他人在提交窗合入了带字节回归的改动 (functions.tsv status=1 但字节不符), 基线带伤 | 按 AGENTS 铁律 3: 先 `fncheck --blame` 归属, 非自身锅照常提交不回退; 已在 TSV/progress 记录 sub_8045A10 真身字节级 FAIL 的完整证据链 (mk 脚本+20 个变体+probe), 交接给后续 agent 接手 |
| 2026-09-04 03:5x | claude-196d4 匹配 sub_80196D4 后 make 红: `can't open asm/matchings/sub_801A2AC.s` — src/code_8010F10.c:2499 `INCLUDE_ASM("asm/matchings", sub_801A2AC)` 但 sub_801A2AC status=0 (gpnux 2026-09-03 挂起), gen_asm 只生成 nonmatchings/ | gpnux 挂起 sub_801A2AC 时把 src 的 INCLUDE_ASM 路径写成 matchings (HEAD 该行原为 BgLoad_Finish, 是其未提交并发编辑引入的坏路径), 阻塞整 C 文件 汇编 | 改 2499 行 `asm/matchings`→`asm/nonmatchings` (与 status=0 一致, 同 2026-09-03 sub_80488CC 先例); 修正后 make 绿 + sha1 通过, 证明 sub_80196D4 字节完美 (fncheck 116B OK)。sub_801A2AC 本体仍挂起未动 |
| 2026-09-05 08:3x | opencode 匹配 sub_8052F44 后 make 红 (sha1 失败): 首差 0x08020b59, `fncheck --blame` 归 `src/code_801A3C4.o` 4 字节 | 另一 agent sensenova (sub_801A3C4, 进行中, since 08:34 前) 在 08:31 改 `src/code_801A3C4.c`, 其 WIP 回归了**已匹配**的姊妹 `sub_8020B54` (status=1, 60B @0x08020b54) — 正是 rule 116 记录的 "r5/r6 互换 tiebreak" 敏感点; 本会话 08:12 开场 make 尚绿, 故红为在途引入, 非已提交 | 按铁律 3: 归属明确非自身锅, 照常合入不回退不代修; 自身三段自证 (bytecmp OK 104B / gen_asm 后 fncheck sub_8052F44 OK 102B+3池 / sub_8020B54 与 sub_801A3C4 亦 fncheck OK) → 我的 C 文件 `code_804F0B8.o` 在 blame 中 0 字节差异; **提示 sensenova: 你的 WIP 动了 sub_8020B54 的 4 字节, 需自查** |
| 2026-09-05 11:xx | 改 TU→C 文件术语时发现 `functions.tsv` 第 439 行 (0x0801a6f4) 只有 6 字段, 缺 `name` 列 → `scripts/remaining.py` 按 module 过滤时 KeyError、`scripts/audit.py` 崩溃 (`'NoneType' object has no attribute 'strip'`) | csv.DictReader 按位置映射, 少一列时 note 被塞进 name、note 变 None; 此前全表 388 行无过滤时 note 有值故未暴露 | 按 `ll.cfg:445` 权威名补回 `sub_801A6F4`, 全表回到 7 字段; 改 TSV 一律手工 `split('\t')`/`join('\t')`, 禁用 csv.writer |
| 2026-09-05 14:1x | match_bot 匹配 sub_8045A74 后 make 红: `linker.ld:177 cannot move location counter backwards (from 03002c44 to 03000310)` | 并发 agent 登记 `gUnk_03002C44` 时把 `. = 0x00002C44` 插在 `. = 0x00000248` 之后、`. = 0x00000310` 之前 (未按地址序), 破坏 iwram 段单调性; 为他人 WIP 引入, 非本会话 | 按地址序把该行移到 `0x00002C40` (gUnk_03002C40) 之后、`gPartyFollowFlags`(同址 0x2C44) 之前并标注同址别名; 修正后 make 绿 + sha1 通过 |
| 2026-09-05 23:5x | make 红 (sha1 失败): `fncheck --blame` 报"整体位移 -8", 真实差异仅 15084B, 首差 0x080003d4 起 `bl` 目标全偏 +8; 13 个 status=1 函数 FAIL (11 个是位移连带) | 已提交状态就红: 提交 d4fcb74 ("match 6 functions") 把 `sub_801768C` 从 INCLUDE_ASM 换真 C 时顺手把共享原型 `u16 (s16,s16,u8,u8,u8)` 改成 `s16 (s16,s16,s16,s16,s8)`; 第 5 参 s8 迫使 code_80264C0.c 两个**已匹配**调用点 sub_80405A4/sub_8042AB4 各多一条 `ldrsb` 符号扩展 (+4B), 合计 +8B, 0x08040690 起全表位移 | 原型改回 `s16 sub_801768C(s16, s16, s16, s16, u8)` (前 4 参必须 s16: 定义体走有符号 float 转换, u8 会转 __fixunsdfsi, 288B→404B), 定义侧形参声明 `u8 mode` + `switch ((s8)mode)` 显式转换恢复 `asrs r4,r4,#24` 截断 → sub_801768C 288B / sub_80405A4 236B / sub_8042AB4 220B / SoundMain_Frame 196B 全 OK, audit 689/689, `sha1sum -c` 通过。验证手法: 纯净 HEAD worktree (/tmp/ll-head, 补 tools/data 软链 + gbadisasm 重出 code.s + gen_asm) 先复现同签名红基线, 再在此试修 → 证明与本会话在途改动无关。定位法: `nm -n ll.elf` 对 functions.tsv addr 找第一个 actual>expected 的符号 = 漂移起点, 阶梯 +4→+8 直接指认元凶。详见 EXPERIENCE 160 |
| 2026-09-06 06:3x | claude06 匹配 sub_80526A0 后跑 audit 崩: 954 行 sub_804FA04 只剩 6 字段 (⏸ note 整列被清), `audit.py` `'NoneType' object has no attribute 'strip'` | 非本会话所为 (git diff 证明 claude06 在 TSV 只动了 sub_80526A0 一行), 推测某次 TSV 批量回写丢尾列/尾 tab; audit.py 对 NF<7 行无容错 | 从 HEAD 恢复 sub_804FA04 整行 (含原 ⏸ note); audit.py 117/118 行改 `(r["note"] or "")` 容错; 批量回写工具须逐字段保留尾列, 改 TSV 仍禁 csv.writer |
2026-09-06 08:xx | zcode-ll2 用 split("\t",5) 更新 functions.tsv note 列, 把 name 列覆写为 note 文本 (TSV 实为 7 列, maxsplit=5 时 parts[5]="name\tnote") | TSV 列序理解错误; 4 行 note 丢失 (tsv_init 回写 name 时未保留), 2 行塌缩成 6 列致 gen_asm 漂移误报 | 修复: 全列 split("\t") + 按下标写 name/note; 教训: 更新 TSV 行必须先校验 NF==7, note 写 $7; AGENTS.md 的 split("\t",5) 示例应理解为"前5列+剩余"
| 2026-09-06 13:3x | `git stash pop` (stash@{0}: sub_801CBA4 挂起笔记) 冲突: 自动合并把 code_801A5EC 的 88 行函数按拆分前旧模块名 code_801A3C4 整块重复插入, functions.tsv 1059→1147 行、88 个地址重复 | stash 基于拆分前的 TSV (dbf6953), 3-way merge 只看到"我加了 88 行"看不到"对方重命名了 module 列", 逐行语义冲突无法自动识别 | 手工按 addr 主键去重保留 code_801A5EC 版本, stash 唯一实质内容 = sub_801CBA4 ⏸ note 移到该行; progress.md 两侧全保留 (按日期 CBA4 段前置), remaining.md 直接 `gen_reports.py` 重生成; 教训: stash pop 触及 functions.tsv 后必查 `awk -F'\t' 'NR>1{print $4}' \| sort \| uniq -d`, 非空即重复; 同批发现 data/raw_data 缺 130+ bin (data.json 已扩至 4297 条), `scripts/dumpraw.py` 重建后 make+sha1 绿 |
2026-09-06 13:xx | zcode-ll2 挂起 sub_801ED40 时用 text.index("// @ 0x0801ED40...") 到 text.index("// @ 0x0801F3FC") 的切片回退, 误假设两函数相邻, 实际中间还有 sub_801EE6C(真C)与 sub_801EEE4(INCLUDE_ASM), 二者被整段删除; 带"洞"的源继续编译, ROM 布局后移, sha1 失败 | 切片回退前未校验区间内容; 且中间多次只跑单文件 make 未跑全量 sha1, 损伤潜伏了一轮 | 立即从 /tmp/full_bak.c 备份恢复缺失段(EE6C 真C + EEE4 INCLUDE_ASM), 全量 make+sha1+audit 恢复 700/700; 教训: ①index 切片回退必须先 diff 校验区间只含目标函数 ②每次源文件结构性修改后必须立刻全量 make+sha1 ③/tmp 备份救了命, 结构性编辑前必须先 cp
2026-09-06 14:xx | zcode-ll2 挂起 sub_80200E8 时 AGAIN 用 [0x080200E8, 0x0802031C) 切片, 区间内还有 sub_8020228 的 INCLUDE_ASM 被删, 链接 undefined reference (调用方 sub_801B964.s), gen_asm 总数 1059→1058 暴露 | 同一错误的第二次重犯: 区间边界按"下一个我认识的地标"而非"紧邻下一个函数"选取 | 修复: 在 0x0802031C 锚点前补回 20228 INCLUDE_ASM, tsv_init 恢复行(按 addr 保留 note), gen_asm 重建切片, 700/700 全绿。铁规则: 挂起函数用 sed 单行替换 INCLUDE_ASM 行本身, 永不做跨行区间切片; 切片后必须 grep 函数数守恒
2026-09-06 15:xx | claude-8030D9C 批量合并收尾时发现 src/code_80264C0.c 被截成 0 字节 (HEAD 基线 1683 行, 含当日未提交的 15+6=21 个函数合并), 链接期 undefined reference 暴露 | 时序: 本 agent 与 glm5 并行向同一 src 追加合并; 中途某次写入竞态/中断留下空文件 (open('w') 截断后未写完即中止), 且此前一轮 make 只看到 conflicting-types/undefined-ref 未立刻意识到文件已空 | 恢复: git show HEAD 取基线 + 逐函数重放全部 21 个合并 (每个函数的验证 C 都在 permuter/<fn>/base.c), struct ObjFadeSeq 插到首个使用者 sub_802DFDC 前 (insert 位置按地址序, 首次插在 F00 前导致 incomplete-type), INCLUDE_ASM 计数 122→101 精确校验 + F658/FF54 等锚点存在性检查, fncheck 21×OK + make exit=0 + SHA1 绿 + glm5 的 6 个合并 (8037E14/7FE8/81BC/8390/8568/874C) 一并复原 | 教训: ①共享 src 的合并必须"读-改-写"原子化且写前备份 cp (本轮 /tmp/src_head.c 救命) ②合并脚本必须打印 INCLUDE_ASM 计数守恒断言 ③发现 undefined reference 时第一时间查源文件完整性而非逐个对参数
| 2026-09-06 18:3x | 第二次 `git stash pop` 冲突 (stash@{0}: sub_8051AEC 匹配 + sub_805063C 挂起): 上游 4bbec27 已删除 `docs/reports/remaining.md` 并把 `docs/reports/` 进 .gitignore, 与 stash 的报表重生成三方冲突; 且 EXPERIENCE.md 双方各自新增一条都编成 168 | ①stash 基于 f04342c, 上游在 4bbec27 改变工具链产物归属 (报表转 untracked), stash 无法感知 ②EXPERIENCE 编号是人工顺序号, 双方并发追加必然撞号 | `git rm docs/reports/remaining.md` 接受上游删除 (生成物可随时 `gen_reports.py` 重建); EXPERIENCE 保留双方内容, stash 侧改编 169, 内部引用 "经验 368 同条"(笔误)一并改成 169; progress.md 两侧全保留; 教训: EXPERIENCE 新增条目前先 `grep -oP '^\d+\.' \| sort -n \| tail -1` 取当前最大号, 并发窗口内追加须再取一次 |
| 2026-09-06 20:5x | 第三次 `git stash pop` 冲突: EXPERIENCE 撞号第二次重犯 —— 上一轮把 stash 侧 168 改为 169, 但上游 ff416a4 本轮新增 169-172 (BattleFx 家族 4 条), 我们的条目又与上游 169 同号 | 编号是人工顺序号, 改号只在 stash 时取了一次 max; 上游在 stash 之外又追加了 4 条, 3-way merge 不校验编号唯一性 | 取当前 max=172 后把本条移到文件末尾重编 **173**, 内部交叉引用 "经验 169 同条" 一并改 173; 教训: 每次解决冲突后都要 `grep -oP '^\d+\.' docs/EXPERIENCE.md | tr -d . | sort -n | uniq -d` 全量查重复, 不能只查自己改的那一条 |
| 2026-09-06 08:2x | zcode 跑 ./setup.sh 终验 make 红: `data_805769C.i:3626: Failed to open "data/raw_data/unk_80798A8.bin"` (预处理阶段, data_805769C.o 未产出) | 已提交状态的坏 (非并发在途): 提交 bb8e6af 把 0x080798A8 数据改名 `gDialogPortraitPalettes` 时同步了 scripts/data.json 的 name 字段, 但漏改 src/data_805769C.c:646 的 INCBIN 路径; setup.sh 每次经 dumpraw.py 按 data.json 全量重提取 → 旧名 .bin 不复存在; 全量核对 146 处 INCBIN 引用仅此一处失配 | INCBIN 路径改回与新名一致 `data/raw_data/gDialogPortraitPalettes.bin` (纯数据符号文件名对齐, 零链接风险, 不涉函数匹配/permuter); 修后 make rc=0 + `sha1sum -c ll.sha1` 通过。教训: 数据符号改名除 data.json/ll.cfg 外, 必须 `grep -rn 旧名 src/ include/` 扫 INCBIN 引用点 |
| 2026-09-06 22:58 | 第四次 `git stash pop` 冲突 (新 clone 后 autostash pull dbf6953→5b5ce03, stash 基点落后上游 3 提交): functions.tsv 16 地址重复插入 (1060→1076 行), EXPERIENCE 本地 8 条编号 167-170 内部各自重复两次且与上游 167-170 撞号, INCIDENTS/progress 为单侧新增 | 上游 4bbec27/ff416a4/5b5ce03 已含 stash 部分同源内容 (TSV 拆分行/data_805769C INCBIN 修复), 3-way merge 只认行级增删不识别语义等价 | 上游原位 + 本地按原顺序后接: EXPERIENCE 本地 8 条重编号 174-181 并修 3 处内部交叉引用; progress 本地侧 8 处编号引用同步改; TSV 按 addr 去重 16 行 (双 note 取本地 glm-batch: 0804b7b0,0804b8e8,0804bd54,0804be90,0804d840,0804dabc,0804db64,0805063c; DC24 取上游; 7 地址双方同空取首个; DABC 保留本地 status=1); remaining.md 维持上游删除 (gitignore 生成物, gen_reports.py 重建); 修后全量 make+sha1+audit+fncheck 复验 |
| 2026-09-06 23:14 | 收尾体检: audit 报 status=1 字节核验 728/744, 16 个 FAIL 全为「未解析符号」型 (gWaveSineTable/gIntrTable/gMainTasks/IntrMain 等), 但 make+SHA1 全绿 (ll.gba 逐字节=baserom) | fncheck 从 ll.elf 取符号值施加池重定位, 这批符号实际存在 (nm 可见 0x080000fc/0x0805769c/0x087e83f0/0x080576d0) 却被判未解析 → fncheck 解析器对部分符号类别 (R 段数据/asm 对象符号) 有盲区; 16 个函数所在源文件与 HEAD 零差异, 为上游提交带入的固有基线, 非本会话引入 | 按铁律 3 归属: 非自身锅照常收尾, 不回退不代修; ROM 逐字节一致 (SHA1) 为终验权威, fncheck FAIL 须先查是否未解析符号误报再定性; 待后续修 fncheck 符号解析 (疑似漏认部分 section 类型) |
| 2026-09-07 14:3x | make clean 后全量 make 红在 `build/src/data_805769C.o`: `gLevelUpExpTable` 冲突类型 — 头文件 `extern const u32[]` (include/data_805769C.h:71) vs 源文件定义 `const u8[] INCBIN_U8` (src/data_805769C.c:1726) | fb30364 模块化拆分时把符号带进新头文件, extern 类型 (u32, 与唯一消费者 player_stats.c:1006 的 `gUnk_08092248[]` u32 索引访问一致) 与 INCBIN 定义 (u8) 失配; 增量编译下旧 .o 一直复用, Makefile 不追头文件依赖, 错误只在 clean 重建暴露 | 源定义改为 `const u32 INCBIN_U32` 对齐头文件 (纯类型视图修正, 400B 数据内容不变); 修后 make rc=0 + sha1sum -c ll.sha1 通过 (746/1059)。教训: 拆分 TU 抽 extern 声明时必须与源定义类型逐个核对; 数据 .bin 为 4 倍数长度时优先按消费者访问宽度定类型 |
| 2026-09-07 18:1x | opencode-scriptset 首次跑 rename_fn.sh Bgm_Request→Script_SetEnvSet: 第 7 步链接报 `linker.ld:682 cannot move location counter backwards (087f8cbc→087e83f0)`, 脚本自动回滚; 但**回滚后单独 make 仍同错**, 且 grep 发现 src/include/linker.ld 无任何残留旧名 | 并发构建污染: 多 agent 同时全量 make (本会话 18:0x 连续 3 次 rename+make 与他人构建交叠), 中间还 `rm -rf build` 后与在途构建抢写 build/; 部分中间产物 (data/data.o 等) 处于半新半旧状态, .rodata 输入集合与任一源码状态都不对应, 计数器顶过 rodata 锚点。rename 管线本身无问题 (EXPERIENCE #185 的「.text 体积变化」是另一诱因, 本案 .text 未变) | 全量 `make clean`+静置重跑 make → 全绿; 4 个 rename 逐个重跑全部一次通过 (fncheck OK + SHA1 绿 + audit 746/746)。教训: rename_fn.sh 第 7 步全量 make 失败且报 rodata 锚点错时, 先 `git diff` 排除自身改动残留, 再查**并发构建交叠** (ls build/*.o 时间戳离散>5min 即实锤), 不要反复回滚改名 |
| 2026-09-08 | claude-8050434 会话内 `.venv/bin/python` 被环境挟持 (sys.executable=AppImage), permuter/asm-differ 首次运行静默失败 (缺 toml/找不到 cc1); 且 Python3.14 forkserver 默认使手拷 compile.sh 缺 cd 行报 "preproc 没有那个文件" | 双因: ① venv python symlink 被 AppImage 替换生效 ② 3.14 多进程默认 start method 变 forkserver, worker cwd 不继承 | fndiff.sh 打 PYTHONPATH 补丁用系统 python3 + 手拷 compile.sh 补 `cd "$(dirname "$0")/../.."`; 修法已入 EXPERIENCE 190 | 
| 2026-09-10 20:3x | 全量 make 红在 `Assembler: can't open asm/nonmatchings/sub_80246E8.s` (src/scene_obj_fx.c 引用); 同时 `asm/matchings/MenuUi_DrawItemList.s` 亦缺失 | `functions.tsv` 被并发写坏 **2 处行粘连** (非本 agent 造成, 提交前已在工作区): 0x0800f3ac/0x0800f4a8 与 0x08024618/0x080246e8 两组相邻行各自并被合成一行 (行内 note + 换行丢失, name 变 `sub_800F3AC1` / `sub_80246180`), gen_asm 据此把这两行对应地址当"无主"→**删除其 asm 切片** (日志 `删除 2`); 另 0x08024618 行 status 字段还带前导空格 (`" 0"`) | 以 `git show HEAD:functions.tsv` 为基准逐行修复: 两组粘连行按 **addr 主键**拆回两行; 0x08024618 的 note 从 HEAD 恢复 (zcode 2026-09-10 的新 note 随粘连丢失, **不可复原, 已在其 lock/progress 中另有记录**); 0x0800F3AC 保留工作区已匹配的 status=1 并据 `progress.md §sub_800F3AC` 重建 note; status `" 0"→"0"`; 重跑 `gen_asm.py` 重建 2 个被删切片; 全量 make+sha1 绿 (776/1059) + audit 776/776 | 
| 2026-09-10 20:3x | 改 `include/code_0.h` (补回调原型) 后 `make` 仅 714ms 完成并报 sha1 成功 —— **旧产物假绿** | Makefile 的 .o 规则**不依赖头文件** (`grep -E 'C_HEADERS|depend|-MMD' Makefile` 为空), 改任何 `include/*.h` 都不会触发重编; 我当时未核对时间戳即认绿 | 必须 `touch src/*.c` 强制全量重编, 再用 `find build -name '*.o' ! -newer include/<改过的头>` 断言 C 目标全部新于头文件 (asm/data 的 8 个 .o 除外, 它们不含 C 头) 才认 sha1 绿。教训: **凡本轮改过 include/*.h, 收尾必须 touch + 时间戳断言**, 否则等于没验。 | 
| 2026-09-11 14:2x | zcode-main 匹配 BattleTask_Run: 改 `include/code_0.h` (8 个被调函数原型由 `void f();` 改为带返回值) + `include/iwram.h`/`linker.ld` (新增 3 符号) 后直接 `make` 报 sha1 成功, 但只编了 714ms | 同 2026-09-10 20:3x 事故: Makefile .o 规则不依赖 include/*.h, 改头文件不触发重编 → 旧产物假绿 | 按既有教训 `touch src/*.c` 强制全量重编后 make rc=0 + sha1 绿 (789/1059), 确认原型/符号改动未破坏已匹配函数 (被调函数均为 INCLUDE_ASM, 原型不影响其汇编) |
| 2026-09-12 00:5x | nova 匹配 sub_8018070 时向 `include/code_0.h` 追加 4 条原型修正, 工具返回成功但其中 2 条 (`void sub_801D214();` → `u8 sub_801D214(u8*,u8);`、`void sub_8022458();` → `u8 sub_8022458(u8);`) 随后在文件里"消失"; 第一次 `make` 报 `src/sio_link.c:221/245: void value not ignored as it ought to be` | `code_0.h` 是多 agent 高频共享文件, 本轮有他方并发写入; 我的 Edit (读-改-写整文件) 与他人写入交叠, 后写者整文件覆盖把我的 2 行还原 (另 2 条恰好在未被覆盖的时间窗内留下) | 原地重读 (`sed -n '<行号>p'`) 确认原文后**用带上下两行的更大 old_string 重做** (降低与他人改动的字符串冲突面), 并重 `grep -n` 断言 4 条全部在位再 make; 本次未造成实际损失 (改的是无人调用的 K&R 原型), 但**凡是 Edit 共享头之后必须 grep 复验, 不能只信工具返回 success**。另: 本轮同时改了 `include/iwram.h`, 收尾按 2026-09-10/09-11 同类事故教训 `touch src/*.c` 强制全量重编, 并用 `find build -name '*.o' ! -newer include/<头>` 断言只剩 8 个 asm/data .o 后才认 sha1 绿 |
| 2026-09-12 01:3x | zcode-engine 批量匹配 battle_engine: 用 `split('\t',5)` 更新 functions.tsv 两行 status/note 时, 原行尾是 `\t\n` (name+空note列), parts[5] 含 `\t\n` 被整体替换丢掉换行 → 下一行被吞并成一行 (8046558 行消失、8046480 行 name 变 `sub_80464800`), `gen_asm --sync` 又改写了 name 掩盖现场 | TSV 部分行有 `\t\n` 尾 (name 后接空 note), maxsplit=5 时 parts[5] 连带换行符; 直接替换 parts[5] 丢 `\n` | 用整行重建 (全 split 后按列拼) 或替换后补 `\n`; 已按 git 基线重建两行 (1060 行与基线一致), 事故后 `awk -F'\t' NF` 全表核验 |
| 2026-09-12 06:1x | 陈旧认领锁接管: `sub_8010300` 被 claude-300 自 2026-09-05 15:31 持有 7 天(note 空、status 仍「进行中」), 新 agent nova 无法认领 | 锁无过期机制; 原 agent 会话大概率已结束, 但 TSV 仍是 status=0 且 note 停在 2026-09-05, 属于「死锁」 | 经用户确认后: 先 `cp` 原锁到 `.scratch/nova/sub_8010300.lock.bak` 备份, 再 `rm .claims/sub_8010300.lock` 并重新认领; 前人候选同步备份为 `permuter/sub_8010300/base_claude300.c.bak`。**接管前必须先取得用户同意** (并发真在跑时会撞车, 参见 2026-09-12 00:5x 的 code_0.h 覆盖事故)。建议后续给 claim.sh 加 `--takeover` 或锁的 TTL |
| 2026-09-12 07:4x | nova 跑 sub_8010300 时先后启动了 4 个后台 permuter (F8p5Ig/Va817I/uq7OCS/N5DsIx), 全部指向**同一个** `permuter/sub_8010300/` 目录; 每个 shell 都先 `rm -rf output-*` 再启动, 互相删掉对方的产出、互相覆盖 base.c。其中 permuter 爬到 33 字节的产出 (output-475-1, = R3 屏障 + `new_var=0x03004AA0` ids 基址变量两处共用) 被后续 run 的 `rm -rf output-*` 删除, **丢失**; 事后按 diffsem 记录重建失败 (当时 diffsem 输出被 head 截断, 漏了后半段差异), 现存最优回退到 42 | ①误以为前面的 permuter 已被 pkill 杀掉, 实际多个 venv python 进程同时在跑同一目录 ②`--better-only` 只保证各自相对自己的 base 更好, 多进程并发下 base.c 被互相覆盖后, 「更好」就失去意义 ③permuter 的 score 与掩码 bl 槽字节差**不单调**, 只能事后用 score300b.py 逐个复测 | ①一个 permuter 目录同一时间只允许一个进程, 链式爬山必须 `cp output-<best>/source.c base.c && rm -rf output-* && 重跑` **串行**执行, 且每轮先确认 `pgrep -f permuter.py` 为空 ②事后逐 pass diffsem 不要用 head 截断, 或直接把 diff 全文落盘 ③本次未影响共享文件/仓库 (make+sha1 仍绿), 损失仅限本函数的 33 字节候选; 已把 base.c 恢复为 42 字节的 R3 屏障版, 66 字节的干净版在 .scratch/nova/v300c/L5_local_hp.c |
